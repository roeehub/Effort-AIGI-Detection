# TIMELINE — R13 Relaunch Master Index

> Append-only chronological index. One line per session, packet, or load-bearing finding. Slice agents append to the bottom during build; working agents append at session end per `AGENTS.md` update protocol.
>
> **Format** (single line, pipe-friendly):
>
> ```
> YYYY-MM-DD — <what happened> — owning thread(s): <thread1>, <thread2> — outcome: <one phrase>
> ```
>
> **Rules:**
>
> - Append only. Do not edit prior entries — historical fidelity matters more than tidiness.
> - Owning thread(s) must be paths under `threads/` (without the `.md` extension is fine for terseness; readers can resolve the path).
> - The outcome phrase should make it clear whether the entry shipped, was abandoned, opened a loop, or closed one.
> - One line per entry. If you need more space, add a thread; the timeline is an index, not a record.
>
> **Read protocol**: agents read the last ~10 entries on session start (per `AGENTS.md` step 3) to load the recent arc.

---

## Entries

<!-- Slice 1 (2026-04-17 → 2026-04-19) appends here -->
2026-04-17 — task board + agent prompts shipped on `teams-relaunch-root-2026-04-17`; six parallel worktree tracks claimed (WT-A/B/C/D/E/F) — owning thread(s): gate_alignment_story, weak_signal_hints_track — outcome: coordination scaffold landed (commits 6c0d1fb..609071d)
2026-04-17 — WT-A data-policy truth freeze: 4904 ignored / 480+202 retained as hints; corrected 10,388 / 8,932 train pairs; Track-A configs labeled `old-semantics`; merged 19:05 CEST — owning thread(s): gate_alignment_story — outcome: ✅ root artifact for every later gate-alignment claim (commits b208d17, 521d231, 829d0ba)
2026-04-17 — WT-E promotion-contract scaffold: eight-suite manifest + five-checkpoint shortlist + local/Vertex wrappers + fixed-`0.5` scorecard demoted to diagnostic-only; `promotion_winner.json` emission deferred; merged 19:05 CEST — owning thread(s): promotion_contract_evolution — outcome: ✅ contract foundation in place; readout deferred to RLP3+ (commits 213df81, 3dabc13, c1556fe)
2026-04-17 — WT-F proper-data schema: six canonical exact lanes (`proper_real_{clean,teams}`, `proper_visomaster_{clean,enhanced_clean,teams,enhanced_teams}`); future suite template + builder; merged 19:06 CEST — owning thread(s): gate_alignment_story — outcome: ✅ forward-deployment lane names frozen (commits 22b6819, d07f06d, 4b2b0c5)
2026-04-17 — WT-C augmentation truth: GammaUp dead-config drop discovered (`gamma_up_p` never consumed; first-preset allowlist filtered keys); truthful sidecars added; Teams-passthrough special-aug opt-in block; tests `46 passed`; merged 19:07 CEST — owning thread(s): calibration_vs_training_aug — outcome: ✅ pre-WT-C augmentation claims should be read as dead-knob drift (commits 6661bbb, e918397, 3df2430, 47cea27, 7c01467, cf8e114)
2026-04-17 — WT-D decision-system tooling: threshold-sweep / abstain-band / temporal / hysteresis primitives + tests; minimal-baseline runbook scoped; merge-time honesty noted no calibrated five-checkpoint promotion winner could be named locally; merged 19:09 CEST — owning thread(s): promotion_contract_evolution — outcome: ✅ primitives shipped; readout artifacts gap acknowledged (commits e30becb, 2c7fc04, 05e5767, 5fe095d)
2026-04-17 — WT-B claimed 20:36 CEST; shipped draft-only weak-signal package (no runnable hints arms in tracked tree); merged 20:41 CEST as `blocked` on source integration — owning thread(s): weak_signal_hints_track — outcome: ⚠ draft-only blocked; honest non-fake delivery (commits 733b343, 44743be, 5533985, 1fa0360)

<!-- Slice 2 (2026-04-19 → 2026-04-22) appends here -->
2026-04-18 — WT-B re-investigation lands locally on `teams-relaunch-root-2026-04-17`: explicit `combined_paired.visomaster_hints` / `visomaster_hints_teams` / `teams.apply_bad_data_policy` runtime, runnable `R13_WTB{1,2,3}_*.yaml`, smoke config, tracked policy bundle, `.gitignore` exception for `data/sources/*.py`; status `analysis-done` 22:49 CEST — owning thread(s): weak_signal_hints_track — outcome: ✅ runtime path closed; remote smoke pending
2026-04-19 — WT-B remote smoke proofs both succeed: startup smoke (Vertex `9012653657048481792` / W&B `8bjcadyr`) and 100-step integration smoke (Vertex `2921535161029885952` / W&B `g6f8fovi`, `val_in_dist/overall/auc=0.97534`); WT-B merged on board 12:31 CEST — owning thread(s): weak_signal_hints_track — outcome: ✅ closes Slice-1 loop `wt-b-april-17-source-integration` (commit f9303eb, 7cde78c)
2026-04-19 — WT-F April-19 follow-up: `combined_paired.proper_data` runtime loader + WT-F provisional inventory/manifest/suite under strict `16/16` clean-and-Teams contract; proper-data startup smoke (Vertex `2064725331922649088` / W&B `bxay0n66`) succeeds with all four `proper_visomaster_*` lanes nonzero; `RELAUNCH_UPGRADE_REVIEW_PACKET_2026-04-19.md` consolidates the WT-A→WT-F state — owning thread(s): gate_alignment_story — outcome: ✅ proper-data path is real training input (commit 77facfc)
2026-04-19 — RLP1 8-slot launch on `dtect-vision/phase2r13-rlp1-overnight-20260420`: hints ladder (`01→02→03`), proper-data add-ons (`04`/`05`), WT-C sidecars on `05` (`06`/`07`), scratch hedge (`08`); R12_G_FP32 base, ArcFace s 6→12, 10k steps; legacy `shuffle` identity-split — owning thread(s): weak_signal_hints_track, identity_split_mode — outcome: ✅ first relaunch packet under April-17 contract
2026-04-20 — RLP1 mid-flight findings: live proper-data is larger than written plan (`RLP1_04` 684 vs planned 442; `RLP1_05/06/07/08` 3186 vs planned 2412); legacy `shuffle` identity-split moves identities between train/val/test when later arms add identities — `combined_paired.identity_split_mode: "hash_stable"` lands in-tree; rule "cite builder reports + startup W&B summaries, not prose counts" — owning thread(s): identity_split_mode, gate_alignment_story — outcome: ⚠ opens loop `split-mode-delta-unquantified`; foreshadows RLP2's fresh-control pattern
2026-04-20 — RLP1 best-checkpoint ranking: `RLP1_01=0.98915` > `04=0.98773` > `05=0.98542` > `02=0.98524` > `07=0.98500` > `06=0.98492` > `03=0.98442` > `08=0.98164` (best_ood_composite, AUC-based) — control wins; unenhanced proper-data the only promising new signal; hints fail at 8/8 matched holdouts (Δ −0.0077..−0.0115); WT-C sidecars under base — owning thread(s): weak_signal_hints_track — outcome: ✅ closes Slice-1 loop `hints-as-supervision-hypothesis` (negative)
2026-04-21 — RLP2 6-slot plan: hints removed from main slate, no scratch (RLP1_08 still live), no WT-C sidecars; `RLP2_02 = WTB1+unenhanced_proper` is main bet; `RLP2_03/04` test moderate enhanced add-ons; `RLP2_05/06` are stability probes (spatial / low-ArcFace); switches to `hash_stable` identity-split with fresh-snapshot control `RLP2_01`; introduces required post-train WT-D frame-policy stability pass — owning thread(s): identity_split_mode, promotion_contract_evolution — outcome: ⚠ "enhanced hurts" called in-session, later downgraded to muddled
2026-04-21 — RLP2 result: `RLP2_02 = 0.98937` (packet leader, +0.00275 over `RLP2_01` fresh control 0.98662); `RLP2_03/04` enhanced arms underperform but val pool was unenhanced-only and dose was 2.2× unenhanced (confound); cross-packet `RLP2_01 − RLP1_01 = −0.00253` carries unquantified split-mode component — owning thread(s): gate_alignment_story, identity_split_mode — outcome: ⚠ opens loop `enhanced-vs-unenhanced-val-pool-confound` (RLP3 instruments address it)
2026-04-21 — `R13_RELAUNCH_TRAINING_SOURCE_OF_TRUTH_2026-04-21.md` and `R13_RELAUNCH_PACKET1_RESULTS_AND_PLANNING_CONTEXT_2026-04-21.md` published: codify split-mode caveat (A5), establish "fresh same-snapshot control" pattern, name the canonical doc stack (April-6 plan / April-17 relaunch report / April-19 review packet / packet-2 plan); promotion contract is **optional parallel work**, not in-path — owning thread(s): identity_split_mode, promotion_contract_evolution — outcome: ✅ methodology rules pinned for downstream packets

<!-- Slice 3 (2026-04-22 → 2026-04-24) appends here -->
2026-04-22 — RLP3 lands A1–A10 OOD instrumentation (commit `c6f1034`, image `1.3.190`): per-video jitter (max/p95/spike_rate) on val_holdout AND OOD; dual-checkpoint A2 (best_ood_composite + best_value_composite); A3/A3b deterministic lighting + spatial stress lanes; A6 enhanced-proper OOD lanes carved OUT of `ood_composite` but logged into `value_composite`; A8 first-OOD-at-step-1000 for FT runs; A9 `value_composite = 0.6·teams_fakes_tpr + 0.3·other_fakes_tpr + 0.1·stability` at τ meeting `mean_FPR ≤ 0.02 AND max_FPR ≤ 0.04`; A10 `blake2b(video_id)` 90/10 held-out partition — owning thread(s): promotion_contract_evolution, gate_alignment_story — outcome: ✅ instrumentation operational every packet thereafter
2026-04-22 — RLP3.5 launches as parallel canary in us-east1 (commit `477b00b`, image `1.3.192`): single-lever scan over arcface_m {0.10, 0.15, 0.20} + stability_lambda + label_smoothing + family_rebalance; gate-definition shift `(0.02, 0.04, max) → (0.03, 0.05, p95)` applied globally; `anneal_steps: 15000 → 8000` so ArcFace anneal completes inside 10k steps — owning thread(s): promotion_contract_evolution, gate_alignment_story — outcome: ⚠ launches; silent-fallback bug surfaces ~hours later
2026-04-22 — `train_sweep.py` value_composite config-propagation bug detected and fixed (commit `872502c`, image `1.3.193`): `wandb.init` flattens nested dicts so `wandb.config.get('value_composite')` returns None despite yamls declaring the new gate; trainer init logs of RLP35 slots 01/02 print legacy `(0.02 / 0.04 / max)` despite NEW gate yamls; 8-line per-block re-apply added to existing allowlist (~lines 170–290); 6 RLP35 slots cancelled and relaunched under 1.3.193 across us-east1 / asia-southeast1 / us-east4 / us-west1 / europe-west4 — owning thread(s): wandb_yaml_propagation_bugs, promotion_contract_evolution — outcome: ✅ silent-fallback caught before invalidating RLP3.5; pattern flagged for recurrence
2026-04-22 — slot-06 region relocation europe-west4 → us-central1 (commit `bcf4c61`): `RLP35_06_family_rebalance_proper_up` was stale PENDING in europe-west4; us-central1 had 5/8 A100 free; relaunched at 16:01 UTC as job `9185612453914869760`; original `3309882805399322624` cancelled — owning thread(s): (operational) — outcome: ✅ slot recovered; foreshadows GCS-region-locality finding (Slice 5)
2026-04-22 — RLP3 retro-score lands under NEW gates (`(0.03, 0.05, p95)`, image `1.3.195`): retro NEW VC slot 02=`0.7027`, slot 04=`0.7215`, slot 05=`0.7232` ★; legacy-vs-NEW ranking flips slot 04↔05 (inside σ_seed); sanity drift `−0.0067` retro-vs-training (vs ≤1e-3 spec) is *systematic* and cancels in P3.5-vs-P3 Δ; retro runner has same nested-dict flattening bug class as `train_sweep.py`, patched separately — owning thread(s): promotion_contract_evolution, wandb_yaml_propagation_bugs — outcome: ⚠ opens loop `rlp3-retro-score-systematic-drift`
2026-04-22 — RLP3.5 leader settles `m=0.15` at value_composite `0.7442` (`+0.0415` over retro-P3_02 baseline, but only `+0.021` over retro-P3 leader RLP3_05 `0.7232`); slot 03 `m=0.20` blocked at max_fpr ≈ 0.055 for 22+h (first contract-side gate refusal in production); slot 07 stack_top3 `DON'T FIRE` decision — only arcface clears +0.03 threshold; "~5× smaller deltas under new gates" is the first clean separation of metric-vs-training mass — owning thread(s): promotion_contract_evolution, gate_alignment_story — outcome: ✅ `m=0.15` adopted as control through RLP4+; ✅ first-evidence "gate is doing more mass than knob"
2026-04-22 — RLP4 plan: 8 overnight slots, half control / half spatial backbone, full margin scan `m∈{0.125, 0.15, 0.175}` × `{control, spatial}` + Teams-reweight `1.0→3.0` × {control, spatial} + seedB replication of leader; C1 (`proper_visomaster_teams` as OOD lane) deferred (~2-4h schema work in `combined_paired.py`); C1→C2 swap on slot 8 — owning thread(s): promotion_contract_evolution, gate_alignment_story — outcome: ⚠ all 8 jobs FAIL at startup
2026-04-22 — RLP4 8-of-8 launch failure forges image-rebuild discipline: yamls authored after image `1.3.195` was built, never committed before launch; all 8 containers `FileNotFoundError: /workspace/experiments/phase2_round13/R13_RLP4_*.yaml` ~30s after start; zero trained value_composite; RLP5 carries `m=0.15 + spatial` forward on RLP3.5 evidence alone; canary-first pattern + `./dev.sh build-prod -y` rule enters operational discipline — owning thread(s): image_rebuild_discipline — outcome: ✅ founding incident; memory `reference_image_rebuild.md` created in response

<!-- Slice 4 (2026-04-24 → 2026-04-26) appends here -->
2026-04-23 — VisoMaster-Teams pool diagnostic landed (`R13_RELAUNCH_VISOMASTER_TEAMS_POOL_DIAGNOSTIC_FINDINGS_2026-04-23.md`): per-frame fake-side accuracy 67% on `proper_visomaster_teams` (in training) vs 18% on `proper_visomaster_enhanced_teams` (excluded from training); identity-split confound and preprocessing confound ruled out; lane contamination (uniform bucket sampling drew 83% from excluded enhanced lane) was the headline-inflation root cause; named the E1/E2/E3 levers — owning thread(s): gate_alignment_story, processing_signature_shortcut — outcome: ✅ motivates RLP5 E3 launch; first explicit "the model never trained to detect this lane" articulation
2026-04-23 — RLP5 8-slot launch (session `cd19ddfb`, image `1.3.196` after fresh `./dev.sh build-prod -y` post-RLP4 wipeout): seven slots fork the E3 recipe (enable `proper_visomaster_enhanced_teams` lane + `proper_visomaster_teams_fake` family weight 1.0 → 2.5), slot 8 is E1 control (weight lever alone); regions us-west4×2 / us-east1×2 / asia-southeast1×2 / us-central1×2; all 8 RUNNING; slot-07 (seed 742, otherwise = slot-01 seed 737) leads at `best_value_composite=0.7736 @ step 20500` — owning thread(s): gate_alignment_story, processing_signature_shortcut — outcome: ✅ training-side breakthrough real (67%/18% → 94%/96% on `proper_visomaster_teams_fake` / `proper_visomaster_enhanced_teams_fake` per `analysis/feature_space_2026-04-23/per_source_summary.csv:12-15`); checkpoint at `gs://training-job-outputs/phase2r13_experiments/6jwwb526/value_composite_effort_20260423_step20500_auc0.9908_eer0.0304.pth`
2026-04-23 — RLP5 slot-07 sanity check on calibrated promotion contract is the first live `fpr-minimization-no-budget-tau-collapse` (carrier `arena/checkpoint_maps/teams_target_domain.r13_rlp5_07_sanity_2026-04-23.yaml`): selected τ = 0.9953, dev real FPR = 0.0 across 3 real suites, but `teams_fake_all_dev` recall = 27.6%, `visomaster_enhanced_macro_dev` = 1.6%, `deeplive_enhanced_dev` = 0.0%, `lockbox_fake_recall` = 15.8%, `dev_fake_macro_recall` = 9.7%; trainer-side `value_composite=0.7736` vs contract-side macro recall 9.7% on the same checkpoint, same data, different τ; bug not yet diagnosed in-session (filed as "expected sanity passes FPR axis") — diagnosis is later (memory `project_contract_policy_bug.md` traces three rediscovery attempts 04-23 / 04-27 / 04-29) — owning thread(s): promotion_contract_evolution — outcome: ⚠ open loop `fpr-minimization-no-budget-tau-collapse` moves from `open` → `in-progress`; first dated inflection point of the policy-bug whack-a-mole
2026-04-23 — RLP5 slot-07 `dor_shkedi` vs `real_dor` flip on the Dor-annotated sanity map (`teams_target_domain.r13_rlp5_07_sanity_dor_2026-04-23.yaml`): same person, different `identity_key`, model gives mean prob_fake = 0.457 (14.6% > 0.9) on `dor_shkedi` vs mean = 0.038 (0% > 0.9) on `real_dor`; the camera/ISP-signature shortcut goes from RLP3.5 suspicion to fact (memory `project_signature_shortcut_finding.md`); 90/90 lockbox is **not** threshold-reachable on slot-07 (at τ catching 90% fakes, real FPR is 63.6%) — owning thread(s): processing_signature_shortcut — outcome: ⚠ slot-07 framing: "training-side breakthrough real, deployment interpretation shortcut-driven"
2026-04-23 — RLP6 plan landed (`R13_RELAUNCH_PACKET6_EXPERIMENT_PLAN_2026-04-23.md`): three corrective gate levers stacked — (1) drop `external_youtube_avspeech_real` (54.2%/45.8% FPR at natural τ, not deployment-distribution) + `zoom_vcd_real` from `external_real_sources`; (2) add `path_exclude_contains: ["/visomaster_"]` to `teams_ood_fake` (drops ~535 failed-deepfake hint folders); (3) drop `wma_failure_fake` from gate (cleanup; 14.55 Fréchet from `deeplive_enh_fake`, real-pool-only `worst_pool_fpr` per `trainer/trainer.py:182-216` makes it a category-error driver); 8 slots all forked from `R13_RLP5_07_E3_seedB`; the prior "wma drives `worst_pool_fpr`" framing explicitly marked superseded — owning thread(s): gate_alignment_story, promotion_contract_evolution — outcome: ✅ corrected-gate model lands; permanent contract invariant: `worst_pool_fpr` is real-pool-only by construction
2026-04-23 — RLP6 base code change landed (uncommitted in slice but referenced at `R13_RELAUNCH_PACKET6_EXPERIMENT_PLAN_2026-04-23.md:128-145`): `data/validation_sources.py` and `data/sources/combined_paired.py` accept opt-in `path_exclude_contains: Optional[Iterable[str]]` on external real/fake + stress-OOD loaders; legacy yamls unaffected — owning thread(s): gate_alignment_story — outcome: ✅ enables yaml-only RLP6 packet
2026-04-23 — RLP6 8-slot launch (yaml-only + the path_exclude code change): all 8 forked from RLP5_07_E3_seedB; `R13_RLP6_04_add_enh_clean` adds `proper_visomaster_enhanced_clean` lane at `family_weights=2.0` on top of the gate-alignment canary changes — owning thread(s): gate_alignment_story — outcome: ✅ all 8 RUNNING
2026-04-24 — RLP6_04 wins packet at `value_composite=0.9006 @ step 23500` (`auc=0.9942`, `eer=0.0169`; checkpoint `gs://training-job-outputs/phase2r13_experiments/h2pdu6i5/value_composite_effort_20260424_step23500_auc0.9942_eer0.0169.pth`): +0.127 over packet-5 leader (0.7736); biggest within-packet jump of the relaunch sequence; gate-alignment hypothesis confirmed (slots 1/4/5 cleared the canary; slots 2/3 stayed near 0.77 confirming hint-clean and wma-drop are hygiene-only) — owning thread(s): gate_alignment_story, promotion_contract_evolution — outcome: ✅ ceiling reframed: RLP5's 0.7736 was gate-bound, not representation-bound; gate-alignment lift is orthogonal/additive to the still-unfixed shortcut
2026-04-24 — RLP6_04 2-camera controlled test on Dor + Roee (`R13_RELAUNCH_PACKET7_CAMERA_SIGNATURE_HANDOFF_2026-04-24.md:5-19`): Dor laptop no-VB → 0.02; Dor webcam same scene no-VB → 0.94 (with VB → 0.88); Roee Mac webcam VB → 0.90; Roee Windows laptop → 0.01; Dor laptop yellow lighting (control) → 0.04 — same subject same lighting, different camera flips score; cross-subject replication confirms the pattern is camera/ISP-signature, not identity-specific — owning thread(s): processing_signature_shortcut — outcome: ⚠ shortcut promoted from "lockbox curiosity" to **deployment blocker**; opens loop `shortcut-deployment-block` (severity: critical)
2026-04-24 — WS-P0 preprocessing-parity fix landed (commit `855871e`): `cv2.INTER_AREA → cv2.INTER_LINEAR` at `batch_inference_gcs.py:407` and `arena/model_arena.py:472` matching training at `data/sources/combined_paired.py:3455`; the load-bearing path is `arena/model_arena.py:472` (the retro-score code that produced the 0.94 Dor false-flag); guarded by `tests/test_inference_train_preprocessing_parity.py` (5/5 pass); every retro-score number before this commit had silent kernel drift — owning thread(s): preprocessing_parity_bug — outcome: ✅ in-repo paths fixed; deploy-server (`http://34.16.217.28:8999`) untouched (separate workstream, opens loop `deploy-server-preprocessing-drift`)
2026-04-24 — RLP6_04 post-fix re-score is the pre-launch gate (`analysis/rlp6_04_postfix_rescore_2026-04-24.summary.json`): anchor pool `dor-real-webcam-false-flag-no-virtual-bg` moved only −0.008 (pre-fix 0.940 → post-fix 0.932); clean pools <0.05; false-flag pools 0.93–0.96; `decision_tree.verdict = "fully_structural__launch_full_subset"`; preprocessing drift was noise, the camera/ISP shortcut is the dominant residual — owning thread(s): preprocessing_parity_bug, processing_signature_shortcut — outcome: ✅ rules out "WS-P0 alone descopes Packet-7" branch; structural shortcut survives the fix
2026-04-24 — WS-P1 calibration probe landed (commit `deac44e`, `analysis/calibration_probe_2026-04-24.summary.json`): per-camera calibration closes 30.1% of the cross-pool FPR gap on average across {5,10,15,25}% target FPRs; per-target {5%: 0.000, 10%: 0.333, 15%: 0.286, 25%: 0.583}; verdict field `mixed_both_levers_needed`; lands at the ≤0.30 = training-aug-dominant threshold by 0.001 (boundary) — owning thread(s): calibration_vs_training_aug — outcome: ✅ training-aug is dominant lever; calibration is complement, not cure; pre-fix inputs caveat opens loop `ws-p1-rerun-on-post-fix-inputs`
2026-04-24 — WS-P2.b per-identity reducer shipped (commit `deac44e`, `arena/postprocess_per_identity.py`): groups `videos_report.csv` by `group_key` and recomputes `real_fpr` / `fake_recall` per identity with `--verify`; guarded by `tests/test_postprocess_per_identity.py`; `HANDOFF.md:233-240` defines "narrowest per-identity FPR spread" as the explicit RLP7 leader-pick — owning thread(s): calibration_vs_training_aug, promotion_contract_evolution — outcome: ✅ readout primitive ships; integration as contract gate (vs sort key) opens loop `per-identity-reducer-not-a-contract-gate`
2026-04-24 — WS-P4.a Teams-passthrough spatial-aug wiring verified (no code change): `teams_passthrough_special_*` knobs at `data/augmentations/pipelines.py:787` (`_TEAMS_PASSTHROUGH_DEFAULTS`) and `:1386` (`_build_teams_passthrough_pipeline`) survive the `combined_paired.py:4772` preset-key filter; enablement is a yaml choice — owning thread(s): gate_alignment_story — outcome: ✅ unblocks `RLP7_04`/`_05` as minimal yaml-delta from RLP6_04
2026-04-24 — RLP7 5-yaml slate authored (commits `0f797a1`, `c4affa1`, `edd6f50`, `ed607b9`): all fork RLP6_04 step-23500 checkpoint; `_01_lighting_aggressive` (CCT/gamma/shadow), `_02_codec_aggressive` (webcam_codec_p=0.35 + teams_codec_sim_p=0.40), `_03_combined`, **new this session** `_04_teams_spatial_only` (ShiftScaleRotate ±3%/±5%/±3°), `_05_teams_spatial_plus_codec` (slot-04 + teams_codec_sim_p=0.25); also `_06_teams_cct_only` and `_07_combined_+CCT` exploratory siblings; `_08_codec_aggressive_from_step4500` drafted as fork-point control (commit `ed607b9`); pre-launch gate verdict `fully_structural__launch_full_subset`; recommended subset `{RLP7_05, RLP7_02, RLP7_04}`; image bumped to `1.3.205` (commit `7c3f6d0`) — owning thread(s): processing_signature_shortcut, calibration_vs_training_aug — outcome: 🟡 launch pending user pick; seed collisions on 742 across `_01/_02/_03` flagged as user decision point
2026-04-24 — RLP6B side-branch authored (8 yamls `R13_RLP6B_0{1..8}_*.yaml`, untracked): representation-robustness sweep (quality_lambda 2x/4x, ArcFace m=0.50, mixup_alpha=0.5, label_smoothing=0.1, feat_norm_reg_lambda=0.05, two combos) targeting low-norm/enhancement shortcut; **never launched** — RLP7's camera-signature reframing pulled attention; slot 01/02 quality_lambda values are identical (both 0.4) despite "2x/4x" naming — likely copy-paste bug — owning thread(s): processing_signature_shortcut — outcome: 🟡 hypothesis partly superseded before launch; recommendation in `RLP6B.md`: fold into `RLP6.md` or correct yamls before launch


<!-- Slice 5 (2026-04-26 → 2026-04-28) appends here -->
2026-04-25 — RLP8 launches: P8A (`R13_RLP8_01_unfreeze_clip_codec`, run `9lmvb5b4`, image 1.3.206 via `d5be7ce`) and P8B (`R13_RLP8_02_fresh_head_plain_clip`, run `n8yk2hox`); P8A single-variable delta from RLP7_02 = `unfreeze_final_proj` + `unfreeze_final_ln` + `apply_svd_to_mlp` all `true`; P8B = scratch on plain CLIP-DataComp-XL (30k steps, LR 2e-4, m=0); P8A trained 21:41-23:48 UTC us-east1 ~2h7min — owning thread(s): processing_signature_shortcut, in_proj_svd_gradient_bug — outcome: ✅ P8A finishes; P8B will hang at step 12000 / 30k overnight (commits `d047da9`, `d5be7ce`)
2026-04-25 — P8A anchor-pool rescore (`analysis/pool_rescore_rlp8_a.summary.json`) breaks the P7 anchor ceiling: `dor-real-webcam-no-VBG` Δ=−0.188 vs RLP6_04 (best P7 was −0.097 ≈ 2× improvement); Roee-mac Δ=−0.399 (best P7 was −0.228 ≈ 1.75×); all three real-correct pools also moved closer to zero — owning thread(s): processing_signature_shortcut — outcome: ✅ first run to break the camera-signature ceiling; memory `project_p8a_breakthrough.md` written
2026-04-25 — P8B anchor result: anchor Δ=+0.066 at step 11000 / 30k (WORSE than RLP6_04); real pools also regressed (Dor-correct whiteish 0.013 → 0.141); P8B refutes "shortcut is in R12g/R13 weight chain" — confirms shortcut lives in data mix; CLIP-DataComp-XL FT prior is load-bearing as regularizer that scratch lacks; P8B hung at step 12000 on `teams_ood_fake` data loader 00:58 UTC; cancelled at 03:13 UTC without explicit user authorization (process mistake; memory `feedback_no_cancelling_vertex_jobs.md`) — owning thread(s): processing_signature_shortcut — outcome: ✅ "shortcut is data-bound, capacity helps, scratch is wrong" framing locks; ⚠ cancellation-without-authorization process feedback
2026-04-25 — P8A promotion-contract scorecard reveals the trade (`gs://training-job-outputs/test_results/teams_promotion_contract/p8a-review-scorecard-20260425/`, job `2162379556655202304`): real-pool FPR all three slices lead RLP6_04 + RLP7_05 (P8A 12.11% / 8.13% / 11.13% on `teams_real_all_dev` / `_poor_quality` / `_lighting_extreme`; ~2× FPR halving on `_poor_quality_dev`); per-method fake recall regresses on `teams_fake_all_dev` aggregate 83.9% → 70.2% (Δ=−13.6 pp), with deeplive_enhanced −26.6 pp, xiang_xiang flat −33.3 pp, visomaster_enhanced_macro −21.6 pp; failure-mode analysis (`analysis/p8a_fake_failure_analysis_2026-04-25/`) shows 329 regressions with **zero compensating wins**; ~80% of misses below τ-recoverable band (separability collapse, not threshold drift) — owning thread(s): processing_signature_shortcut, value_composite_semantics, promotion_contract_evolution — outcome: ⚠ P8A is muddled; ✅ first concrete proof `value_composite=0.99+` is deployment-blind; opens loop `value-composite-semantics-undefined` (which closes resolved this slice via the operationalization)
2026-04-25 — state-of-detector + R13 full-story doc landing (commits `1573c59`, `e326b81`, `f766ccd`): `R13_RELAUNCH_STATE_OF_THE_TEAMS_DETECTOR_2026-04-25.md` with P7 matrix + P8A/B detail + OOD aug-stress trajectory; `R13_FULL_STORY_PRE_PACKET_9_2026-04-25.md` second-opinion narrative explicitly demoting `value_composite` to non-deployment-grade; W&B per-method + OOD stress slices added — owning thread(s): value_composite_semantics, processing_signature_shortcut — outcome: ✅ first verbatim doc-level "value_composite is not deployment-grade" framing; loop `value-composite-semantics-undefined` resolves via canonical thread synthesis
2026-04-25 — Packet-9 design + plumbing lands: `backbone_lr_mult` plumbing in `utils/setup.py::choose_optimizer` (commit `0f2f342`, default 1.0 preserves prior behavior); `real_codec_uplift` flag in `data/augmentations/pipelines.py` (commit `ebce585`); 5 single-variable yamls drafted (`P9_01` softened, `P9_03` no-MLP-SVD, `P9_05` real-codec uplift, `P9_R` reseed, `P9_freeze` native) commits `dc0478f`/`d509188`/`c0a91f7`/`5af88c7`/`926af00`; image 1.3.208 (`009ae58`) and 1.3.210 (`c9997c0` launch image); 5 longshot pack added evening (`94d80d4`, image 1.3.211 via `cbfac08`) — owning thread(s): processing_signature_shortcut, value_composite_semantics — outcome: 🟡 P9 launches single-variable disentangle of magnitude / topology / data
2026-04-25 — P8A step 2500 ood_composite anchor rescore (commit `3426bdf`); `ood_composite` introduced as parallel directional metric to `value_composite` — operational rule "quote both at in-flight; neither is deployment-grade" — owning thread(s): value_composite_semantics, promotion_contract_evolution — outcome: ✅ dual-metric in-flight reporting becomes standard
2026-04-26 — Packet-10 anti-shortcut packet drafted (commit `2c9778b`): symmetric augmentation router + GRL slate; orthogonal intervention class to P9's "soften P8A"; symmetric-router yaml `R13_P10_SYM_on_P8A.yaml` to be used as Phase C.1 canonical recipe — owning thread(s): processing_signature_shortcut — outcome: 🟡 GRL half not launched in slice; symmetric-router half runs as Phase C
2026-04-26 — wandb artifact name >128 chars bug fix (commit `f366368`, image 1.3.214 via `d609e39`); long `log_prefix` scorecards triggered W&B artifact-name length validation — second wandb-side hygiene fix in two slices (Slice 3 had train_sweep allowlist re-apply `872502c`); pattern recurs across distinct wandb surfaces — owning thread(s): wandb_yaml_propagation_bugs — outcome: ✅ artifact-name fix lands; opens loop `wandb-side-surface-hygiene-not-systematic` (broader pattern unresolved)
2026-04-26 — **in_proj-SVD silent zero-gradient bug discovered + fixed** (commits `2feea58` + `7af72b1`, image 1.3.216 via `ac83ba1`): `forward_pre_hook` did `module.in_proj_weight.data.copy_(module._svd_in_proj.weight)` — `.data.copy_` writes without autograd graph, so q/k/v residuals received zero classification gradient since whenever `apply_svd_to_in_proj=true` landed; affects entire R12/RLP/P-* lineage; verified via `analysis/probe_battery_2026-04-26/grad_audit.py` (9/9 in_proj residuals + 3/3 SVDResidualLinear residuals receive non-zero gradient post-fix); P8A breakthrough re-attributed to MLP-SVD + visual.proj + ln_post unfreeze ALONE — owning thread(s): in_proj_svd_gradient_bug, processing_signature_shortcut — outcome: ✅ bug fixed; opens loop `apply-svd-in-proj-attribution-revision-needed` (high severity); memory `project_p8a_breakthrough.md` + `project_shortcut_is_upstream.md` get CORRECTION/CAVEAT blocks
2026-04-26 — Phase 0 commit-isolation discipline forged: `detectors/effort_detector.py` working tree contained TWO unrelated changes (in_proj-SVD fix +130 lines + older feat_norm-reg ~38 lines from packet-6B 2026-04-23); split via patch-extract-and-apply (saved working version → reset to HEAD → applied in_proj-only patch → committed → restored); feat_norm preserved uncommitted indefinitely (user authorized) — owning thread(s): in_proj_svd_gradient_bug — outcome: ✅ split-commit pattern documented; feat_norm tracked in LOG without plan-text change
2026-04-26 — Phase A.2 image-currency bug + recovery: A.2 v1 (job `8294349582570094592`) sat PENDING because checkpoint map was created 21:15 UTC after image 1.3.216 build at 21:13 UTC; `/workspace/arena/checkpoint_maps/...` would FileNotFoundError at container start; cancelled with user authorization before GPU consumed (zero spend wasted); image rebuild → 1.3.217 (commit `f9ddf3a`); A.2 v2/v3 relaunched on 1.3.217; A.2 v3 (`889157804793790464`, us-east1) succeeded 23:46 UTC — owning thread(s): in_proj_svd_gradient_bug — outcome: ✅ recovered; pre-launch image-currency guard authored as discipline (commit `ad76cd8`)
2026-04-26 — Phase C overnight slate launches (5 jobs, image 1.3.218 via `04710ab`, commit `6665910` for yamls): C.1 canonical (`8252691286016917504`), C-ablation (`3815519753150136320`, `apply_svd_to_in_proj: false`), C.3 codec hedge (`5934463377827954688`, `teams_codec_sim_p: 0.60`), C.4a (`9199573107671564288`, base RLP6_04 step 23500), C.4b (`7708881631011930112`, base RLP6_04 step 8000); 5-corner read planned to disentangle "in_proj fix is the lever?" / "data axis is the lever?" / "P8A recipe is necessary?" / "FT-depth crystallization matters?" — owning thread(s): in_proj_svd_gradient_bug, processing_signature_shortcut — outcome: 🟡 5 jobs all RUNNING us-west4
2026-04-26 — Phase C overnight readout: **C.1 vs C-ablation tied within noise** (in_proj fix produced no material lift in canonical configuration; lever exists but not exercised); **C.4b vs C.4a tied within noise** (Δ=0.0007 best_anchor/composite — plain-CLIP-scratch §C-γ trigger NOT warranted); **C.3 codec_hedge is the slate's positive direction** — best_anchor/composite +30% vs canonical with similar value/ood composite + heldout_ood AUC; A.2 v3 readout at τ=0.5 (calibrated τ ≈ 0.99 fired the contract policy bug as predicted) shows P8A reference dominates P10_SYM-on-P8A step 2500 on both lockbox FPR (6.2% vs 10.4%) + recall (65.2% vs 59.3%) — owning thread(s): in_proj_svd_gradient_bug, processing_signature_shortcut, promotion_contract_evolution — outcome: ✅ codec is live lever; ⚠ in_proj-SVD lever not exercised by canonical recipe; opens loop `in-proj-svd-residual-capacity-not-exercised`
2026-04-26 — pre-launch image-currency guard ships (commit `ad76cd8`, `scripts/launch/check_image_currency.sh` ~95 lines + integration into `launch_experiment.sh` + `arena/launch_teams_promotion_contract.sh`): future "yaml/checkpoint-map newer than image" mistakes fail-fast at submission; smoke-tested both pass and fail cases; `SKIP_IMAGE_CURRENCY_CHECK=1` escape hatch — owning thread(s): in_proj_svd_gradient_bug, image_rebuild_discipline — outcome: ✅ operational discipline shipped
2026-04-26 — Packet-9 mid-flight handoff written (`PACKET_9_MID_FLIGHT_HANDOFF_2026-04-26.md`): unusually loud about premise risk (*"Roee has new findings that may invalidate the entire direction of Packet-9 ... wait for him to share before recommending any next action"*); first handoff to verbatim-name "Trainer's `value_composite` is **NOT** deployment-grade — multiple times we've crowned the wrong checkpoint by trusting it"; pre-commits P9 crowning protocol with lockbox per-method recall floors and explicit exclusion of trainer composite — owning thread(s): value_composite_semantics, promotion_contract_evolution — outcome: ✅ P9 crowning protocol pre-committed; the "user has new evidence that may overturn the packet" listen-first pattern formalized

<!-- Slice 6 (2026-04-28 → 2026-04-29) appends here -->
2026-04-27 — codec_hedge A.2-style validation lands (job `4993397216769998848`, image 1.3.220, 32 reports + 5 contract artifacts, ~$3): P8A reference dominates BOTH lockbox axes vs all 3 C-slate checkpoints at τ=0.5 (FPR 6.2% vs 13.3-17.7%, recall 65.2% vs 41.9-61.7%); all 4 hit τ ≈ 0.97-0.99 contract policy bug regime — owning thread(s): contract_policy_bug, processing_signature_shortcut — outcome: ✅ codec_hedge axis NOT a Phase D promotion candidate; ⚠ contract policy bug fires as predicted; PCP forks (i)/(ii)/(iii) filed
2026-04-27 — Contract-policy bug fix attempt #2 designed + end-to-end validated, not committed (LOG `:425-495`): `+61/-16` patch to `arena/score_teams_promotion_contract.py` adds `target_real_fpr`/`target_stress_fpr` budget infra + tier-based sort; `analysis/policy_reruns_2026-04-27/{default,recall_floor_30}/` re-scores codec-hedge slate; P8A operating point shifts τ=0.991 → τ=0.916, lockbox fake recall 24% → 38.7% — owning thread(s): contract_policy_bug, promotion_contract_evolution — outcome: ⚠ second fix attempt in 6 days; lost to /tmp wipe between sessions; NOT committed; whack-a-mole pattern explicit (memory `project_contract_policy_bug.md`)
2026-04-27 — Contract scorer crash-fix lands (commit `ed6f53b`): unblocks `arena/score_teams_promotion_contract.py` when readout-only suites are absent; verified end-to-end on Vertex via codec-hedge job — owning thread(s): promotion_contract_evolution — outcome: ✅ scorer hygiene fix shipped; this is NOT the policy-bug fix
2026-04-27 — Source-bucket linear probe verdict (LOG `:441-449`): RLP6_04 control 0.97 (saturation), P8A 0.461 (4.6× chance, FAIL), C3_codec_hedge 0.416 (4.2× chance, FAIL); all candidates fail Plan §6 ≤ 0.25 PASS gate — owning thread(s): processing_signature_shortcut — outcome: ⚠ P8A NOT shortcut-clean; codec aug pulls some source-pipeline signal (~9% relative reduction P8A → C3) but not enough to clear gate
2026-04-27 — Identity-corruption audit lands (`analysis/identity_corruption_audit_2026-04-27/`): training-set identities are video-derived (DF40/DeepLive/Visomaster) NOT session-derived; lockbox/dev session-aggregation corruption does NOT propagate to training; PC_Generator__s15 = 91 fakes onto person A + 29 reals of person B with same identity_key — owning thread(s): processing_signature_shortcut — outcome: ✅ training-set identity construction VERIFIED clean; P11_TARGETED launch decision shifts HOLD → GREEN
2026-04-27 — Crop-tightness shortcut empirically confirmed (LOG `:750-817`, `analysis/crop_shortcut_2026-04-27/`): 47-frame × 5-tightness sweep flips 53% of frame predictions; FAIL regime in "FAKE valley" at native crop, any perturbation drops FPR ~30-40pp; 2 of 3 FAIL tags are crop-shortcut artifacts — owning thread(s): face_size_label_leak, processing_signature_shortcut — outcome: ⚠ third shortcut axis surfaces (geometric crop tightness)
2026-04-27 — Face-pixel-area training data leak measured (LOG `:825`, memory `project_face_size_label_leak.md`): per-method face-size signatures (deeplive_enhanced 22.6-24.6k single-point, teams_capture 14k-154k); Cohen's d ≈ 0.37 dev_real vs dev_fake; 12× under-rep (small reals) to 1.7× over-rep (large fakes) — owning thread(s): face_size_label_leak — outcome: ⚠ opens loop `face-size-label-leak` (severity high); same shortcut from a different axis as camera/ISP signature
2026-04-27 — Webcam-harden aug primitive built (commit-not-isolated, LOG `:973-1031`): `data/augmentations/pipelines.py` gets `_WEBCAM_HARDEN_DEFAULTS` + 4-transform fake-only chain (MotionBlur + ImageCompression + HueSaturationValue + GaussNoise); local pipeline-construction sanity passes (4 transforms added to fake pipelines, 0 to real) — owning thread(s): processing_signature_shortcut, webcam_fpr_dominance — outcome: ✅ primitive shipped; will fail at training step 5683 with NaN
2026-04-27 — Smoke test passes + 4 P11 overnight runs launch (image 1.3.221, LOG `:1033-1086`): smoke test val_holdout AUC=0.9906 at step 200 with all 4 flags ON; MILD/HEAVY/HEAVY_DEEPLIVE/WEBCAM_HARDEN launched in 3 distinct US regions (us-east1/us-west4/us-central1); all 4 RUNNING within 4 min — owning thread(s): processing_signature_shortcut, face_size_label_leak — outcome: ✅ first 4-run portfolio with face-size + codec axis disruption
2026-04-27 — Modern_lockbox_v2 subset built (`analysis/modern_lockbox_v2_2026-04-27/`, LOG `:1088-1133`): v2_recommended definition (drop webcam/screen + face_area_ratio≥0.10 + not pose_extreme + not no_face); P8A baseline FPR 4.59% → **0.71%** at τ=0.9741 (6.5× reduction, no retraining); dor_shkedi-skewed (211/281 = 75%) — owning thread(s): webcam_fpr_dominance — outcome: ✅ FPR side of 90/5 target solved by data hygiene at P8A baseline; opens loop `webcam-mode-fpr-dominance-headline-misleading` (severity medium)
2026-04-28 09:00 — P11 morning verdict: 3/4 SUCCEEDED, WEBCAM_HARDEN FAILED (NaN at step 5683), HEAVY_DEEPLIVE deeplive recall regressed to 75.76% with 6× weight; HEAVY's anchor_mean continuously improves through step 6000 (0.392 final); trainer's AUC-ranked save policy froze at step 1000 = WORST anchor moment — owning thread(s): processing_signature_shortcut, face_size_label_leak — outcome: ⚠ recipe direction confirmed (HEAVY); checkpoint policy artifact distorts deployment readout; opens trigger for `periodic_saves` patch
2026-04-28 09:25 — P12_HEAVY_LONG launches (job `1785944433577951232`, us-west4, image 1.3.223): identical recipe to P11_HEAVY but `total_training_steps: 8000` + new `periodic_saves` block; `c7dc828` trainer patch added at lines 2319-2362 (~50 lines additive, `or {}` defensive but no `isinstance(dict)` check) — owning thread(s): processing_signature_shortcut, wandb_flattening — outcome: 🟡 launched as parallel-to-P11-inference work
2026-04-28 11:53 — P11 inference verdict β (job `7291147820403261440`, 159 min, 160/160 reports): HEAVY beat P8A by **+16/+25/+9 pp** recall on viso/deeplive/teams_fake at τ=0.5, at the cost of 4× v2 FPR — owning thread(s): processing_signature_shortcut, face_size_label_leak, webcam_fpr_dominance — outcome: ⚠ Plan v3 §6 verdict β/γ ASSESS — none ship; recipe direction confirmed but step-1000 save artifact distorts readout
2026-04-28 12:25 — P12_HEAVY_LONG dud (LOG `:1275-1336`): early-stopping fired at step 6000 (AUC patience metric saturated at step 1000); `periodic_saves` patch silently failed (no `periodic_*.pth` saved despite `enabled: true` in yaml); root cause = wandb-flattening recurrence (`self.config.get('periodic_saves')` returns None, `or {}` falls through silently); P12 produced no new info vs P11_HEAVY step 1000 — owning thread(s): wandb_flattening, processing_signature_shortcut, promotion_contract_evolution — outcome: ❌ DUD; pure-FT-extension confirmed bounded (anchor recovery rate ~0.04/1000 steps too slow); third instance of "added-but-not-firing" pattern (after in_proj-SVD zero-gradient + anchor_aware DISABLED)
2026-04-28 13:30 — Option A subagent verdict γ (LOG `:1337-1383`): 85 calibration + ensemble configurations on existing P8A + 4 P11 predictions all fail 90/5 gate; AUC(viso fake vs modern_v2 real) caps at ~0.65; calibration moves probabilities not AUC — owning thread(s): promotion_contract_evolution, processing_signature_shortcut — outcome: ⚠ inference-time stack exhausted as recipe lever; substrate confirmed bounded; Plan v4 authored (G — substrate redesign / H — data-axis / I — honest report)
2026-04-28 ~17:50 CEST — P13_FROM_SCRATCH launches with three new top-level nested-dict yaml blocks (jobs `99039952980934656` + `8345130870696312832`, both us-east1, image 1.3.224 then 1.3.225): trainer init logs show `AnchorAwarePenalty DISABLED (enabled=False weight=5.0)` + `FaceScaleJitter DISABLED` despite yaml asserting `enabled: true`; ~$3.50 burned across 2 cancelled jobs; first wrong-layered fix attempt (trainer-side `_to_plain_dict()` helper) loses ~$1.40 — owning thread(s): wandb_flattening, processing_signature_shortcut, face_size_label_leak — outcome: ⚠ wandb-flattening recurrence; load-bearing handoff `WANDB_FLATTENING_BUG_HANDOFF_2026-04-28.md` written
2026-04-28 — Wandb-flattening fix lands (commit `c366026`, image 1.3.226 via `6c9a320`): 3 new keys added to `train_sweep.py:283-313` allowlist (anchor_aware, face_scale_jitter, periodic_saves); `tests/test_train_sweep_reapply_allowlist.py` adds the **first generalized regression-test guard** for the bug class (fails CI if any TRAINER_NESTED_KEYS entry is not re-applied); memory `project_wandb_flattens_nested_dicts.md` enumerates 14-key allowlist post-fix — owning thread(s): wandb_flattening, wandb_yaml_propagation_bugs — outcome: ✅ structural fix shipped (mechanical patch + test guard); the Slice-3 loop `train-sweep-allowlist-not-closed-under-schema-changes` STAYS resolved because the closure mechanism now exists; the broader `wandb-side-surface-hygiene-not-systematic` loop accumulates a third datapoint
2026-04-28 — P13 anti-shortcut interventions yaml drafted (commit `cab2909`): three new training-time levers stacked — anchor-aware loss (penalty on anchor_pool_mean_prob > 0.10 with weight 5.0), pipeline-random aug (symmetric chain of jpeg/downscale/chroma_blur/yuv_roundtrip/gamma applied to both real and fake), face-scale jitter (symmetric crop variation, scale_limit 0.25); all three blocks were silently DISABLED on first launch by the wandb-flattening bug; post-fix verdict belongs to Slice 7 — owning thread(s): processing_signature_shortcut, face_size_label_leak, wandb_flattening — outcome: 🟡 build-only this slice; first packet whose intervention class is loss-side (anchor-aware) + symmetric aug + geometry — the substrate-redesign track of Plan v4

<!-- Slice 7 (2026-04-28 EOD → 2026-04-29) appends here -->
2026-04-28 EOD — P13_FROM_SCRATCH post-`c366026` relaunch fires all three new yaml blocks live (anchor_aware ENABLED, face_scale_jitter ENABLED, periodic_saves ENABLED at trainer init); first packet where every Plan-v4 substrate-redesign lever actually fires — owning thread(s): processing_signature_shortcut, wandb_flattening, face_size_label_leak — outcome: ✅ instrumentation-clean run; verdict belongs to 2026-04-29
2026-04-29 02:30 UTC — P13_FROM_SCRATCH Day-4 verdict γ computed automatically (`docs/relaunch_handoffs/P13_DAY4_VERDICT_2026-04-29.md`; scorecard job `978674200371789824` us-west4 succeeded ~02:14 UTC): all 9 candidates fail Axis 1 by ≥15pp; Axis 3 Δ best step18000 = 0.520 (vs gate 0.15, P8A baseline 0.659 ≈ 14pp directional improvement); cross-domain capability collapsed (the from-scratch trade); buggy contract crowned `P13_step2000` rank-1 with 3.6% lockbox fake recall — owning thread(s): processing_signature_shortcut, contract_policy_bug — outcome: ❌ γ verdict; Day-5 fallback P14 (FT-from-P8A + interventions) recommended; verdict makes contract-policy bug load-bearing for P-series scorecard reads
2026-04-29 ~10:00 CEST — Bucket-gap diagnosis triggered by P13 γ verdict (`docs/relaunch_handoffs/TRAINING_EVAL_VISO_BUCKET_GAP_2026-04-29.md`; `april-26-training-master-plan-v2.LOG.md:1387-1454`): user pushed back on "narrow identity diversity" hypothesis; agent counted buckets and found training viso has 429 unique identities (no enhancers, no Teams transport) while eval `visomaster_enhanced_macro_dev` reads `gs://teams-faces-data-test-2914-fake-4420-real-feb-28/fake/` (GAN-enhanced + Teams-recapture); deeplive control case (3-5× higher recall consistently) is exactly what the bucket gap predicts since deeplive training matches its eval distribution — owning thread(s): viso_bucket_gap, identity_audit, clean_teams_identity_pairing — outcome: ✅ dispositive finding; reframes 3 weeks of recipe-tuning history; Layer-2 closure is yaml-only (wiring fully built)
2026-04-29 ~10:30 CEST — Identity-diversity hypothesis refuted (memory `project_data_inventory_identity_diversity.md`): 429 viso + 1,916 deeplive identities in active training; +2,300 unused identities sit in `live-...-teams-v2` (997), `visomaster-enhanced-face-cropped-v2` (many), `hdtf_visomaster_*_teams` (705) — the "N=8 per method" mental model was wrong by 2 orders of magnitude — owning thread(s): identity_audit, viso_bucket_gap — outcome: ✅ closes off the "more identity diversity" class of recommendations; corollary action lives in viso_bucket_gap thread
2026-04-29 ~10:45 CEST — Clean and Teams buckets confirmed paired-transport of same identities (memory `project_clean_teams_same_identity.md`): explicit join reports at `arena/reports/{hdtf,quickclips}_visomaster_clean_vs_teams_join_2026-04-19.json`; `companion_bucket` field at `data/sources/visomaster.py:748,1085,1105,1176,1523`; `_load_merged_teams_enhanced_paired` function exists; the unused +2,300 identities are largely the same people in a different transport — owning thread(s): clean_teams_identity_pairing — outcome: ✅ structural argument that bucket-fix retrain is cheap; opens `(clean[id_X], teams[id_X])` as a contrastive-loss design lever for Move 4
2026-04-29 ~11:00 CEST — Frame-level AUCs computed from cached P8A_step5000 predictions (memory `project_p8a_frame_level_auc_2026-04-29.md`): viso 0.7527 / deeplive 0.8614 / teams_fake 0.9106 — meaningfully high cross-domain signal, dispositive evidence the buggy contract scorer's "viso recall = 1.1%" reports were a τ-tail-collapse artifact, NOT a model failure; under corrected policy P8A reports viso 13.6% / deeplive 23.9% / teams_fake 52.6% — consistent with the AUCs — owning thread(s): processing_signature_shortcut, contract_policy_bug, viso_bucket_gap — outcome: ✅ load-bearing reframing of "low recall" as scorer artifact; AUC ceiling bounds Layer-2 closure expected lift to ~24% (NOT 90% headline target)
2026-04-29 — Contract policy v3 fix re-derived in working tree, uncommitted (third "fix" attempt in 6 days): `target_fake_recall_min=0.30` recall floor + bumped FPR budgets 0.02/0.05 → 0.07/0.10 + tier-based sort at within-ckpt AND cross-ckpt summary levels; `+207 net lines` across `arena/score_teams_promotion_contract.py` + `arena/run_target_domain_validation_sequential.py` + `tests/test_score_teams_promotion_contract.py` (2 new tests); 6 contract tests pass locally; `_promotion_summary_sort_key` tiering is the new addition that prevents `P13_step2000` rank-1 selection — owning thread(s): contract_policy_bug, promotion_contract_evolution — outcome: ⚠ third attempt designed-not-committed; the wiki was built to prevent attempt #4
2026-04-29 — `R13_P14_DATA_FIX.yaml` drafted (untracked) + `tests/test_visomaster_enhanced_wiring.py` added (3 tests, untracked): bucket-fix retrain wiring + tests; the Layer-2 closure precondition. ~$70 / 1-2 days when committed + launched. Move-1 (frozen-feature linear probe, ~$0) is the proposed pre-spend gate — not yet executed — owning thread(s): viso_bucket_gap, identity_audit, clean_teams_identity_pairing — outcome: 🟡 drafted-not-launched; the cheaper-first lift in priority sequence
2026-04-29 — `R13_P14_FT_FROM_P8A.yaml` launched on Vertex (FT-from-P8A_REFERENCE_step5000 + all three P13 anti-shortcut interventions; the planned-for fallback per Plan v6 priors; ~$60 / ~24h expected); checkpoint map `arena/checkpoint_maps/teams_target_domain.p14_ft_from_p8a_2026-04-29.yaml` (untracked) — owning thread(s): processing_signature_shortcut, viso_bucket_gap — outcome: 🟡 in flight at end of Slice 7; verdict pending
2026-04-29 — `R13_P15_GRL_FROM_P8A.yaml` drafted (untracked); `docs/relaunch_handoffs/P15_GRL_READINESS_NOTE_2026-04-29.md` written: GRL on quality-domain head (DANN), the only structurally-different anti-shortcut lever the codebase already supports; smoke-test required; launch decision contingent on P14 verdict (α: don't / β: launch / γ: launch); Slice-7 bucket-gap finding revises P15's priority — much higher gain expected after Layer 2 closes — owning thread(s): processing_signature_shortcut, viso_bucket_gap — outcome: 🟡 drafted-not-launched; decision-tree decision-owner is user
2026-04-29 — Wiki Slice 7 build authored: 3 new threads (`viso_bucket_gap`, `identity_audit`, `clean_teams_identity_pairing`); 3 new packet retros (`P13` result retro, `P14` plan retro, `P15` plan retro); `STATE_2026-04-29.md` landing page; multiple thread extensions (`contract_policy_bug` v3 fix subsection, `processing_signature_shortcut` Slice-7 frame-level AUC reframing + γ verdict integration, `face_size_label_leak` Slice-7 update, `webcam_fpr_dominance` Slice-7 update, `wandb_flattening` Slice-7 verification, `promotion_contract_evolution` Slice-7 verification on `fpr-minimization-no-budget-tau-collapse`); 3 new open loops (`p14-data-fix-not-launched`, `frame-level-vs-clip-level-scorer-mismatch`, `shortcut-block-criterion-may-be-scorer-artifact-bound`) — owning thread(s): all — outcome: ✅ wiki build complete; Slice 7 closes the build scaffold; canonical anti-stale-state surface in place

<!-- Working-agent entries follow, post-build -->
2026-04-29 afternoon — Visual audit of eval-substrate slices (very-sharp-FP n=421 + is_no_face n=219, the 2026-04-27 investigation's two open follow-ups) lands via local HTML viewer at `~/audit_slices_2026-04-29/index.html` (640 symlinked frames); audit lineage doc at `~/Documents/repos/vault/wiki/ml-training/investigations/2026-04-27-lockbox-dev-property-investigation.md` (external to canonical wiki) — owning thread(s): eval_production_crop_tightness_gap, sharpness_metric_bug, eval_substrate_data_hygiene — outcome: ✅ post-Slice-7 maintenance pass; 6 audit findings cataloged
2026-04-29 afternoon — Audit Finding 5: eval-vs-production crop-tightness gap (eval frames carry more background context around the face than production crops do); structurally upstream of multiple shortcut/FPR investigations the wiki tracks — owning thread(s): eval_production_crop_tightness_gap, processing_signature_shortcut, webcam_fpr_dominance, face_size_label_leak — outcome: ⚠ opens loop `eval-production-crop-tightness-mismatch` (severity high); cross-thread caveat added to `processing_signature_shortcut`/`webcam_fpr_dominance`/`face_size_label_leak`
2026-04-29 afternoon — Audit Finding 2/3: `sharpness_laplacian` at `analysis/lockbox_tagging/layers/quality.py:82` is computed on full grayscale frame not face crop; reproduction case (dor_shkedi 494×393 + PC_Generator__s15 99×110) shows full-image laplacian dominated by non-face content; very-sharp-FP slice mixes two populations — owning thread(s): sharpness_metric_bug — outcome: ⚠ opens loop `sharpness-metric-computed-on-full-image-not-face` (severity high) + `very-sharp-fp-slice-mixes-two-populations` (severity medium); 2026-04-27 investigation's per-quartile FPR table now flagged as partially confounded
2026-04-29 afternoon — Audit Finding 1: is_no_face slice (n=219, 174 real + 45 fake) is data degeneracy where MediaPipe's "0 faces" verdict is correct; 2× FN lift from 2026-04-27 reading was data degeneracy, not model weakness — owning thread(s): eval_substrate_data_hygiene, webcam_fpr_dominance — outcome: ⚠ opens loop `is-no-face-slice-is-data-degeneracy` (severity low); modern_v2 already excludes is_no_face so v2 number unaffected; baseline lockbox interpretation changes
2026-04-29 afternoon — Audit Finding 4: eval substrate contains 99×110-pixel source crops (e.g. PC_Generator__s15_1754.4_frame_058496_crop_002) that pass `face_pixel_area > 1k` floor but no production deployment would feed the model; the 2026-04-27 `face_pixel_area > 57k` cut catches the small-face problem but misses the small-source-image problem — owning thread(s): eval_substrate_data_hygiene, face_size_label_leak — outcome: ⚠ opens loop `source-image-resolution-floor-not-applied-to-eval` (severity medium); a `min(width,height) >= 200` floor is the proposed parallel cut to the existing face-area filter
2026-04-29 afternoon — Wiki post-Slice-7 maintenance pass: 3 new threads (`eval_production_crop_tightness_gap`, `sharpness_metric_bug`, `eval_substrate_data_hygiene`); 3 thread extensions (`face_size_label_leak`, `webcam_fpr_dominance`, `processing_signature_shortcut`); STATE_2026-04-29.md gets new "Eval-substrate caveats" section + post-audit caveat on contract-recall reframing + 2 new entries in top-priority loops + Move 1 reconsideration in Recommended next actions; 5 new open loops registered — owning thread(s): all 2026-04-29-afternoon threads — outcome: ✅ post-build maintenance per AGENTS.md update protocol; not a Slice-N build entry; build remains closed
2026-04-29 22:11 UTC — Three-run overnight slate launches on Vertex (image 1.3.232 via Cloud Build `6b3ec06b`, all US regions per CLAUDE.md): `R13_P14_DATA_FIX` (us-west4, run `xan4dfto`, seed 737, bundle + visomaster_teams_enhanced fw=8.0), `R13_P15_GRL_FROM_P8A` (us-east1, run `w5tky6ss`, seed 1501, bundle + GRL λ=0.20 quality_domain_count=3), `R13_P14_FACE_SCALE_JITTER_ISOLATED` (us-central1, run `mclioexb`, seed 2273, jitter@0.50 ONLY — anchor_aware + pipeline_random DISABLED, sister-variant ablation added at last moment to test whether the bundle's lift is mostly jitter); P15 GRL is the first-ever production exercise of the GRL infrastructure (commit `2c9778b`'s P10 GRL slate had drafted six yamls that were never launched) — owning thread(s): processing_signature_shortcut, face_size_label_leak, viso_bucket_gap, anti_shortcut_bundle_decomposition — outcome: 🟡 all 3 RUNNING within 3 min; smoke gates verified (P15 quality-domain head ENABLED, P14_DATA_FIX visomaster_teams_enhanced loaded 54 teams_v2_companion entries with 8 enhancers)
2026-04-30 ~00:42 UTC — Three-run slate SUCCEEDED (~2.5h each, FT cadence + early stopping): trainer-side value_composite ranking is **`mclioexb` (jitter@0.50 isolated) 0.661 > `w5tky6ss` (P15 GRL) 0.516 ≫ `xan4dfto` (DATA_FIX) 0.126 ≈ prior P14_FT bundle 0.116**; jitter@0.50 alone wins by 5.7× over the bundle, demonstrating the bundle was net-negative against its single load-bearing component; DATA_FIX collapsed cross-method generalization (other_fakes_tpr=0.047) confirming Move-1 prediction; P15 GRL preserved cross-domain head room (other_fakes_tpr=0.659, the highest of the three) but is materially weaker than jitter-isolated — owning thread(s): face_size_label_leak, viso_bucket_gap, anti_shortcut_bundle_decomposition, processing_signature_shortcut — outcome: ✅ packet leader is `mclioexb`; ✅ closes `p14-data-fix-not-launched` loop as superseded (negative empirical result); 🟡 face-size leak close criterion not yet directly measured on Slot 3 — analysis script queued; ⚠ opens new `anti-shortcut-bundle-needs-single-lever-discipline` loop in new bundle-decomposition thread
2026-04-30 morning — Promotion-contract scorecard launched for slot-3 leader `mclioexb` on us-east1 (image 1.3.233 via Cloud Build `46ef62da`, checkpoint map `arena/checkpoint_maps/teams_target_domain.mclioexb_2026-04-30.yaml` untracked); first deployment-grade verdict pending; ETA ~3h — owning thread(s): promotion_contract_evolution, contract_policy_bug — outcome: 🟡 in flight; will fill the "what reading the scorecard looks like" half of the slot-3 verdict
2026-04-30 morning — Wiki maintenance pass for overnight results: P14.md + P15.md updated with Results (2026-04-30) sections; new thread `anti_shortcut_bundle_decomposition.md` authored; threads `face_size_label_leak`, `viso_bucket_gap` extended with 2026-04-30 update sub-sections; 1 loop closed-as-superseded (`p14-data-fix-not-launched`); 1 new loop opened (`anti-shortcut-bundle-needs-single-lever-discipline`); STATE_2026-04-30.md drafted; README verdict table updated; memory `project_face_scale_jitter_load_bearing.md` written with Why/How-to-apply structure — owning thread(s): all 2026-04-30 threads — outcome: ✅ working-agent update per AGENTS.md protocol; OPEN_LOOPS.md regenerated
2026-04-30 afternoon — Three CPU analysis scripts ran (face-size invariance probe over 180 frames × 5 tightnesses × 4 ckpts; domain-confusion linear probe P8A vs SLOT2_GRL vs SLOT3_JITTER; embedding triptych t-SNE 3×3 grid). Results refute both design-intent mechanisms: jitter@0.50 (the value_composite winner) WORSENED face-size flip rate 39.4% → 43.9% with median |Δprob_fake| 2.4× steeper; GRL macro-OVR AUC 0.9994 ≈ P8A 0.9999 (no-bite by ~7σ); triptych shows gross [CLS] geometry conserved across all three rows. Triptych required a torch-free postprocess script (`triptych_postprocess.py`) because sklearn TSNE silently SIGSEGV'd on Apple Silicon when called in-process with torch — workaround documented in new thread's Environment note — owning thread(s): jitter_winner_mechanism_unknown (NEW), face_size_label_leak, processing_signature_shortcut — outcome: ⚠ both design-intent mechanisms refuted; ⚠ opens 2 new loops (`jitter-winner-mechanism-unknown` medium + `jitter-on-training-substrate-not-yet-tested` low); face-size leak's flip-rate close criterion AFFIRMATIVELY NOT MET on leader; empirical jitter@0.50 recommendation survives, causal story does not
2026-04-30 afternoon — Wiki revision pass for direct-probe findings: memory `project_face_scale_jitter_load_bearing.md` "Why (1)" interpretation revised as REFUTED with measurement table; memory `project_face_size_label_leak.md` frontmatter `last_verified` bumped + status note added; thread `face_size_label_leak.md` extended with "2026-04-30 afternoon update — Direct measurement: jitter@0.50 did NOT close the leak" subsection + open-loop block updated to reflect affirmative no-close; new thread `jitter_winner_mechanism_unknown.md` authored with two open loops + three candidate mechanism probes catalogued; P14.md "What the lever is NOT" subsection added with refutation table; P15.md "Did GRL bite the [CLS] domain manifold?" + "Unexpected: GRL incidentally halved face-size flip rate" subsections added; STATE_2026-04-30.md "What changed" extended; this TIMELINE entry appended — owning thread(s): jitter_winner_mechanism_unknown, face_size_label_leak — outcome: ✅ working-agent update per AGENTS.md protocol; OPEN_LOOPS.md regenerated
2026-04-30 evening — Promotion-contract scorecard verdict for `mclioexb` LANDED: does NOT promote (Vertex job `2402672943822798848`, us-east1, image 1.3.233; succeeded 08:59:05Z; local artifacts `analysis/scorecard_mclioexb_2026-04-30/`). At calibrated τ=0.975031 under DEFAULT policy: per-method dev recalls collapsed (visomaster 0.013, deeplive 0.024, teams_fake 0.347), macro 0.128 vs 0.70 floor. Pareto-frontier scan of 5549-point τ grid: even under the in-tree relaxed v3 policy (recall_min=0.30, FPR=0.07, stress=0.10), zero τ qualifies — to hit recall=0.30 the model needs FPR≥0.085. Launcher omitted `--promotion_target_fake_recall_min 0.30` so the v3 fix was NOT exercised on this run; `contract-policy-bug-fix-not-committed` close criterion's component 3 still NOT MET. Trainer-side `value_composite=0.661` does not translate; FT-from-P8A + augmentation tuning has hit a substrate ceiling below the contract — owning thread(s): jitter_winner_mechanism_unknown, promotion_contract_evolution, contract_policy_bug — outcome: ⚠ deployment-grade FAIL; ⚠ direct evidence for jitter-winner candidate mechanism (1) decision-boundary/calibration shift; jitter-winner-mechanism-unknown loop downgraded medium → low (deployment half settled, mechanism is now diagnostic-only); successor packet design needs architectural / data-axis levers, not augmentation tuning alone
2026-04-30 evening — Wiki revision pass for scorecard verdict: NEW memory `project_mclioexb_does_not_promote_2026-04-30.md` (load-bearing scorecard verdict + Pareto-frontier table); memory `project_face_scale_jitter_load_bearing.md` "How to apply" revised (empirical jitter@0.50 recommendation survives but framed as "strictly less bad", successor packet caveat added); P14.md extended with "Production-grade scorecard verdict — does NOT promote (2026-04-30 evening)" subsection + Status-card verdict 🟢 → 🟡; thread `jitter_winner_mechanism_unknown.md` extended with scorecard evidence subsection + "Current stance" updated + open-loop severity downgraded medium → low; thread `promotion_contract_evolution.md` extended with "first deployment-grade verdict under v3-fix-era infrastructure: model-substrate is the bottleneck" subsection + `fpr-minimization-no-budget-tau-collapse` last_verified bumped; thread `contract_policy_bug.md` `contract-policy-bug-fix-not-committed` last_verified bumped + close-criterion update note that 04-30 run did NOT exercise v3 fix; STATE_2026-04-30.md production-grade verdict table populated + "What changed" extended with evening update + Recommended next actions revised; MEMORY.md index updated; this TIMELINE entry appended — owning thread(s): jitter_winner_mechanism_unknown, promotion_contract_evolution, contract_policy_bug — outcome: ✅ working-agent update per AGENTS.md protocol; OPEN_LOOPS.md regenerated
2026-04-30 evening — Independent reviewer memo authored at `plans/INDEPENDENT_REVIEW_SHORTCUTS_AND_NEXT_MOVES_2026-04-30.md`, synthesizing post-dashboard conclusions on shortcut learning, crop regimes, GRL/augmentation, and scratch-vs-P8A initialization choice for master-review decision support — owning thread(s): processing_signature_shortcut, face_size_label_leak, jitter_winner_mechanism_unknown, anti_shortcut_bundle_decomposition, promotion_contract_evolution — outcome: advisory artifact only; no open-loop state changed
2026-04-30 evening — Independent review consolidated with `analysis/quality_gate_2026-04-30` crop results: corrected tightness convention (`t>1` tighter), recorded P8A lockbox/dev recrop-rescore as material but not deployment-positive, and revised recommendation from "tight crops may help" to "do not deploy P8A inference-time recropping; treat crop policy as train/eval substrate" — owning thread(s): eval_production_crop_tightness_gap, face_size_label_leak, processing_signature_shortcut — outcome: advisory memo updated; crop-tightness open loop last_verified bumped, still open

<!-- Note: 2026-05-01 → 2026-05-03 packet activity (P17 / P18 / P22 / S1-S3 / E1 / E2b / E3) tracked in `docs/relaunch_handoffs/` and memory entries; not back-filled to TIMELINE here. Entries below pick up at the 2026-05-04 CPU diagnostic pass. -->

2026-05-04 morning — Diagnostic pass Job 1 (Suite Composition Audit) ran (`analysis/suite_composition_audit_2026-05-04/`): parsed `arena/target_domain_suites.teams_promotion_contract_2026-04-23_with_dor.yaml` + `arena/manifests/teams_target_domain_manifest_2026-04-23_with_dor.json`; tagged 68,180 frames across 9 suites × 5 ckpts with `(suite, ckpt, source_method, enhancement, teams_passthrough, identity)`; coverage `source_method`=100%, `teams_passthrough`=100%, `enhancement`=82.6% (17.4% unresolved teams_capture session-captures); FACTS: clean variants of deeplive/visomaster are NOT in the production scorecard substrate (only enhanced variants scored); `teams_fake_all_dev` is 40.5% non-teams-captured (1,230 / 3,039 frames are flat-upload deeplive_enhanced + visomaster_enhanced + Xiang); `teams_fake_all_lockbox` collapses to 2 sessions / 2 identities — owning thread(s): viso_bucket_gap, eval_substrate_data_hygiene — outcome: ✅ artifacts produced; surfaces critical eval-substrate gaps; downstream Jobs 2/3/4/5/6 keyed off this audit
2026-05-04 morning — Diagnostic pass Job 3 (FP-tail real-frame characterization) ran (`analysis/fp_tail_characterization_2026-05-04/`): per-ckpt FP-cohort + concentration analysis at τ@FPR=10% on `teams_real_all_dev`; FACTS: top-5 of 24 dev identities own 80–95% of FPs per ckpt; six repeat offenders (`bla_bla_chow`, `bla_bla_chow__s2`, `PC_Generator__s22`, `PC_Generator__s45`, `roy_d`, `Q__s6`) dominate every ckpt; 90.1% of P8A FPs come from frames with `min(W,H)<200`; shared-FP-tail = 59.6% (same reals trip ≥2 ckpts) — owning thread(s): processing_signature_shortcut, eval_substrate_data_hygiene, webcam_fpr_dominance — outcome: ✅ FP tail is concentrated, not diffuse; refutes calibration-loss-as-primary-lever for P-class proposals; new memory `project_eval_substrate_reframe_2026-05-04.md` captures the reframe
2026-05-04 morning — Diagnostic pass Job 2 (Per-cell catch patterns) ran (`analysis/per_cell_catch_2026-05-04/`): re-tagged the catch-pattern Venn from `cpu_followups_2026-05-04` by (source_method × enhancement × teams_passthrough); 5 cells with n≥30; FACTS at FPR=10% (calibrated on `teams_real_all_dev`): viso_enhanced 550 frames → P8A 0.269 / E2B 0.084 / E3 0.138 (P8A_only Venn slice = 16.9%); deeplive_enhanced 545 → P8A 0.424 / E2B 0.939 / E3 0.908 (E2B_only = 8.8%); teams_capture/dev 1,809 (12 identities) → all 3 ckpts 0.94–0.98 (90% Venn-agreement); per-method specialization is Venn-supported, not score-shift artifact — owning thread(s): processing_signature_shortcut, jitter_winner_mechanism_unknown — outcome: ✅ Hypothesis "calibration-only" rejected for viso/deeplive (Venn-unique catches inconsistent with monotone calibration shift); 3-way union recall on viso enhanced caps at 33.8%
2026-05-04 afternoon — Diagnostic pass Job 4 (Subtype-stratified viso) ran (`analysis/viso_subtype_analysis_2026-05-04/`): 550 viso frames split 275 raw (flat-upload) + 275 teams (viso → teams transport); FACTS at FPR=10%: union recall raw 43.3% / teams 24.4% (Δ −18.9pp); transport shifts image-quality features in webcam-failure direction (luma_p90 Cohen's d = −5.23, laplacian_var d = +0.80, sharpness drops 60→36); L1 logistic on 7 image-quality features predicts caught/uncaught at 72% (raw) and 76% (teams) CV accuracy; sharpness coefficient flips +1.00 (raw) → −0.11 (teams) — owning thread(s): processing_signature_shortcut, eval_production_crop_tightness_gap, viso_bucket_gap — outcome: ✅ verdict (HIGH confidence): viso → teams is image-quality-compounded shortcut, NOT transport-specific signature; new memory `project_viso_subtype_iq_compounded_2026-05-04.md`
2026-05-04 afternoon — Diagnostic pass Job 5 (Inventory mining for clean-variants and viso → teams) ran (`analysis/inventory_audit_2026-05-04/`): 49 candidate eval cells enumerated; FACTS: only 16 cells (the 16 method-folder sub-cells of `visomaster_enhanced_v2`, ~2,073 frames) are NOT in any training yaml; `proper_visomaster_teams_dev` (262 vids / 4,192 frames / 9 swap models / 229 HDTF identities) and `proper_visomaster_enhanced_teams_dev` (1,182 vids / 18,912 frames / 564 identities) exist but are P8A training data; df40 → teams variant does NOT exist in inventory; `arena/target_domain_suites.teams_manifest.breakdown_2026-04-07.yaml` exists but is NOT loaded by the production scorecard; pre-RLP6_04 ckpts (R12g, RLP1–RLP5) NEVER saw proper viso (proper_data added at RLP6_04); 705 HDTF proper viso identity_ids are clean held-out for those checkpoints — owning thread(s): viso_bucket_gap, identity_audit, clean_teams_identity_pairing, eval_substrate_data_hygiene — outcome: ✅ inventory definitive; pre-RLP6_04 path identified as $0–$15 lever for the universal-vs-trajectory test
2026-05-04 afternoon — Diagnostic pass Job 6 (Frozen-feature region analysis) ran (`analysis/viso_frozen_feature_probe_2026-05-04/`): 5-fold StratifiedKFold logistic regression on cached frozen P8A features (`analysis/clip_vs_p8a_viso_2026-05-03/outputs/p8a__features.npz`, 1100 frames) predicting `is_caught` at FPR=10% by ≥1 of {P8A, E2B, E3} on 550 viso fakes; FACTS: P8A frozen probe AUC = 0.969 ± 0.011 at C=1.0, holds 0.936 at C=0.001 (1000× ℓ2 stress); CLIP-B16-raw probe = 0.870 ± 0.029; Δ(P8A − CLIP) = +0.099; region purity for uncaught viso fakes = 0.924 (k=10 NN, prior 0.66); subtype-stratified probes: raw 0.973, teams 0.971 — owning thread(s): processing_signature_shortcut, jitter_winner_mechanism_unknown, viso_bucket_gap — outcome: ✅ encoder DOES separate caught vs uncaught at AUC=0.97; existing head doesn't pick up that distinction; refutes "representation gap requires encoder change" framing; new memory `project_viso_head_boundary_finding_2026-05-04.md`
2026-05-04 afternoon — User eyeball verification on `visomaster_enhanced_v2` substrate (`analysis/inventory_audit_2026-05-04/v1_vs_v2_eyeball.html`): user (Roee) confirmed via Flask viewer (`viewer/server.py --no-auto-discover`) that v2 contents are Dor in standard setup × 16 swap-model families × small-N swap-target masks; v2 is NOT identity-fresh (Dor is heavily in training as real and fake); v2 IS swap-model-fresh (16 model families, ≥4 not in v1 manifest by name: inswapper variants, instyle_swapper, simswap); v2 visually less sharp than v1 enhanced — owning thread(s): viso_bucket_gap, identity_audit, eval_substrate_data_hygiene — outcome: ✅ partial revision of Job 5's "$0 fresh substrate" framing; v2 reframed as cross-swap-model generalization test on a known-difficult identity, NOT identity-fresh substrate; new memory `project_v2_substrate_is_dor_diverse_swap.md`
2026-05-04 evening — Job C (Wire `teams_manifest.breakdown_2026-04-07.yaml` per-session breakouts) audit COMPLETE (`analysis/job_c_breakdown_wiring_2026-05-04/`): production scorecard config identified as `arena/target_domain_suites.teams_promotion_contract_2026-04-23_with_dor.yaml`; schema compatibility verified (extra top-level keys ignored by `_parse_simple_suite_manifest_yaml` in `arena/run_target_domain_validation_sequential.py`); validation script PASSES for 9 production suites preserved + 20 net-new (5 family + 13 per-session + 2 lockbox-stress-real); proposed merge diff +166 lines append-only; 6 chronic FP-prone identities are real-side and would need a separate real-side breakdown yaml (out-of-scope for this audit) — owning thread(s): promotion_contract_evolution, calibration_vs_training_aug — outcome: 🟡 audit complete; READY FOR USER AUTHORIZATION TO MERGE (no production code/yaml modified)
2026-05-04 evening — Job 7 (head-only retrain on frozen P8A) DISPATCHED, in-flight (`analysis/job7_head_retrain_2026-05-04/`): tests Job 6's hypothesis that a head-only retrain can lift viso recall at FPR=10% materially above the current head's 27% by breaking the IQ shortcut; first agent timed out mid-extraction (4 of 7 suites cached: `visomaster_enhanced_macro_dev`, `deeplive_enhanced_dev`, `teams_fake_all_lockbox`, `teams_real_all_lockbox`); continuation agent dispatched in background to finish `teams_real_all_dev` (mandatory for FPR calibration) + v2 held-out + `teams_fake_all_dev`, then run 5–6 candidate heads (LR baseline, IQ-residualized, adversarial-IQ-debiased, FP-prone-identity-reweighted, MLP, optional XGBoost) with 5-fold CV on broadened frozen P8A features — owning thread(s): jitter_winner_mechanism_unknown, processing_signature_shortcut — outcome: 🟡 in-flight; results expected within 30–45 min via background-agent notification
2026-05-04 evening — Job B (pre-RLP6_04 ckpts on held-out HDTF proper viso) PAUSED at Phase 1 (`analysis/job_b_pre_rlp604_2026-05-04/` — empty pending decisions; no Vertex spend incurred): tests whether viso ceiling is FT-trajectory-induced or universal across the FT chain's earlier members; Phase 1 located 4 ckpts with canonical paths (R12g step14000, RLP3_05, RLP3_02, RLP5_07, RLP6_04); 3 RLP leader run_ids unrecoverable from committed maps (RLP1_01, RLP2_02, RLP3.5_02 — paths cited in retro docs by metric values only); RLP4 had 0 successful runs; R13_FT11 turns out to be a recipe yaml not a checkpoint; preprocessing-parity caveat applies (pre-RLP6_04 ckpts are pre-fix `INTER_AREA`, ~0.7pp drift) — owning thread(s): preprocessing_parity_bug, viso_bucket_gap, processing_signature_shortcut — outcome: ⚠ blocked on user decisions: (1) WandB API query to recover 3 missing run_ids vs ship-with-4 vs skip; (2) image-rebuild authorization for new suite yaml + checkpoint_map yaml or use no-rebuild fallback via `proper_data_future.provisional_2026-04-19.yaml`
2026-05-04 evening — Codec aug verification independent re-test (`analysis/codec_aug_verification_2026-05-05/`): 30 paired (raw, teams) viso frames where the same source frame exists in both clean and teams-transported buckets; applied `TeamsCodecSimulation` (`data/augmentations/teams_simulation.py:104`, calibrated against original 18-video × 132-frame sample) to raw frames; computed 8 IQ feature deltas (laplacian, sobel_edge, luma_mean, luma_std, saturation, contrast, hf_energy); compared aug delta vs actual transport delta — owning thread(s): processing_signature_shortcut — outcome: ✅ aug is faithful simulation: cosine 0.87-0.99 across all 8 features, magnitude ratio 97-106% vs actual transport. Calibration independently re-validated on different sample. Doesn't change Slice-6 codec_hedge non-promotion finding (independent).
2026-05-04 evening — Pair-loss scoping (`analysis/pair_loss_scoping_2026-05-05/FINDINGS.md`): identified `trainer/mixins/stability.py` as closest existing analog (computes `KL(perturbed || clean.detach())` with stability_lambda); companion infrastructure exists (`VisoMasterTeamsEnhancedSample.companion_bucket`); current `_iterate_visomaster_teams_enhanced_sample` picks ONE branch per iteration (original OR teams), never both jointly; implementation effort estimated at 4-6h focused dev work for the missing pair-aware loss + dataloader change — owning thread(s): clean_teams_identity_pairing — outcome: ✅ scoping doc; not a code change yet; verification (next entry) gates whether to proceed
2026-05-04 evening — Pair-loss effect verification (`analysis/pair_loss_effect_verification_2026-05-05/FINDINGS.md`, sub-agent dispatched with minimal context): 275 paired (raw, teams) viso fakes with seq_id matching, P8A frozen features as proxy for E2B features (E2B not cached on disk), E2B scores from `analysis/cpu_followups_2026-05-04/raw_reports/visomaster_enhanced_macro_dev_e2b_top_n_step3200_frames_report.csv`; FACTS: (1) sign of effect REVERSED on E2B for viso — mean E2B raw_score 0.086 < mean E2B teams_score 0.172, Wilcoxon p=0.0019 (teams transport HELPS E2B catch viso fakes, opposite of premise); (2) at deployment-style τ=0.5 the cohort pair loss could help is 3/275 pairs (1.1%) while the wrong-way cohort is 29/275 (10.5%) — symmetric pair loss is structurally net-negative across all τ tested (-13pp at τ=0.05, -9.5pp at τ=0.5); (3) Pearson r(P8A feat distance, E2B abs score gap) = -0.089 (p=0.14, LOW); (4) E2B per-pair score correlation r=0.62 — model already exhibits implicit pair coherence — owning thread(s): clean_teams_identity_pairing — outcome: ⚠ verdict LOW — pair-loss premise empirically refuted on E2B for viso; pair-loss packet NOT drafted; DEBATE block added to `clean_teams_identity_pairing` thread; new low-severity loop `pair-loss-asymmetric-variant-untested`
2026-05-04 evening — Substrate cleaning eval pipeline reusable tool authored (`analysis/substrate_cleaning_eval_2026-05-05/`): `run_clean_eval.py` (single-ckpt mode + reference-run regression mode), `cleaned_substrate_manifest.json` (frozen F4 contract: chronic-6 identity drop list `bla_bla_chow`, `bla_bla_chow__s2`, `pc_generator__s22`, `pc_generator__s45`, `roy_d`, `q__s6` + `min(W,H)<200` + `is_no_face`), reference runs for P8A and E2B, USAGE.md; reference runs reproduce Job 14 numbers within 0.5pp on every checkpoint tested (P8A viso 26.9→67.1%, E2B viso 8.4→30.9%, E3 viso 13.8→77.6%) — owning thread(s): eval_substrate_data_hygiene — outcome: ✅ reusable tool ready for Packet A / Packet C-codec checkpoints; thread extension added to `eval_substrate_data_hygiene` 2026-05-04 evening update
2026-05-04 evening — Per-substrate τ-calibration tool authored (`analysis/per_substrate_tau_calibration_2026-05-05/`): `run_calibration.py` joins frame_path against `analysis/lockbox_tagging/full_tags_2026-04-27.parquet`'s `clip_capture_mode` to bucket reals into 5 substrates; sweeps 100-point τ grid + quantile anchors; reports per-substrate FPR + per-suite recall at each τ; picks deployable single τ at worst-substrate FPR ceiling AND reports oracle per-mode τ for offline analysis; reference run on P8A confirms Job-7 lift (24.5pp at 4× FPR via oracle per-mode τ); deployable single-τ-substrate-aware results bleak (P8A viso 0.18% at 5% FPR ceiling, 21% at 20% ceiling) — owning thread(s): webcam_fpr_dominance, calibration_vs_training_aug — outcome: ✅ reusable tool; user-constraint clarified (per-mode τ NOT deployable in production — Teams does not surface mode); thread extensions added to both threads' 2026-05-04 evening update sections
2026-05-04 evening — Packet A drafted + launched: `experiments/phase2_round13/R13_PA_VISOMASTER_ENHANCED_DATA.yaml` (untracked), seed 3023, single-lever data-availability test (enable `visomaster_enhanced` + `visomaster_teams_enhanced` at fw=4.0 on E2B baseline, NO bundle, NO codec aug); Vertex job `3140330896851206144`, us-east1, image 1.3.256, JOB_STATE_RUNNING; intentional clean retest of the data-axis lever after the prior two confounded tests (P14_DATA_FIX bundle-stacked, P16 fw=2.0) — owning thread(s): viso_bucket_gap, clean_teams_identity_pairing, eval_substrate_data_hygiene — outcome: 🟡 in-flight; new in-progress loop `data-axis-clean-single-lever-retest-in-progress` (high severity) added to `viso_bucket_gap`; new packet retro stub `packets/PA.md` authored
2026-05-04 evening — Packet C-codec drafted + first launch FAILED + relaunched: `experiments/phase2_round13/R13_PC_CODEC_PLUS_DATA.yaml` (untracked), seed 3024, stacked data + Teams codec aug on E2B baseline; first attempt Vertex job `318825730303590400` FAILED in 30s with `wandb entity roeehub not found` 404 because I exported `WANDB_ENTITY=roeehub` (a wandb username, not the entity that exists in this project — the launcher default is `dtect-vision`); workaround `unset WANDB_ENTITY` and let launcher default; relaunch Vertex job `1202657157175050240`, us-east1, image 1.3.257, JOB_STATE_RUNNING — owning thread(s): processing_signature_shortcut, viso_bucket_gap, anti_shortcut_bundle_decomposition, wandb_yaml_propagation_bugs — outcome: 🟡 in-flight; new packet retro stub `packets/PC.md` authored; thread extension added to `wandb_yaml_propagation_bugs` for the wandb-entity gotcha (4th wandb-side surface bug); memory `feedback_promotion_contract_launch.md` extended with `launch_experiment.sh` defaults caveat
2026-05-04 night — Wiki maintenance pass per AGENTS.md update protocol: 5 thread extensions (`clean_teams_identity_pairing` DEBATE block + new low-severity loop `pair-loss-asymmetric-variant-untested`; `viso_bucket_gap` 2026-05-04 evening update + new high-severity in-progress loop `data-axis-clean-single-lever-retest-in-progress`; `eval_substrate_data_hygiene` 2026-05-04 evening update; `webcam_fpr_dominance` 2026-05-04 evening update with deployability caveat; `calibration_vs_training_aug` 2026-05-04 evening update; `processing_signature_shortcut` 2026-05-04 evening update on codec aug verification; `wandb_yaml_propagation_bugs` 2026-05-04 evening update with 4th wandb surface bug); 2 new packet retro stubs (`packets/PA.md`, `packets/PC.md`); SESSION_LOG_2026-05-04.md authored as the cross-agent baton-passing handoff (the contemporaneous narrative the threads were promoted from); memory `feedback_promotion_contract_launch.md` extended; OPEN_LOOPS.md regenerated — owning thread(s): all 2026-05-04 evening threads — outcome: ✅ working-agent update per AGENTS.md protocol; closes the protocol-violation gap from earlier in this session (initial work landed only in handoff, not threads — corrected by promotion + DEBATE block + new loops with citations)
2026-05-04 late night — Loop closure: `pair-loss-asymmetric-variant-untested` (low severity, first_seen 2026-05-04) flipped open → resolved per user decision "lay off the asymmetric pair loss for now"; closed via close_criterion option (c) — explicitly closed-as-not-pursued. Resolution prose appended below the structured block in `threads/clean_teams_identity_pairing.md` documents the evidence basis (symmetric refutation: sign-of-effect reversed on E2B p=0.0019, wrong-way cohort 2-10× target cohort across τ, 86% pairs miss in BOTH versions; asymmetric upper bound ~9pp at τ=0.05; cheaper alternatives F4 substrate cleaning P8A viso 27→67% and per-substrate τ +21pp lockbox lift), what the resolution does NOT claim (asymmetric was NOT tested, only deprioritized), and reopen triggers (PA/PC-codec failing to deliver via cheaper alternatives; new model base where sign-of-effect not measured; theoretical reason to expect E2B sign to flip). Memory `project_pair_loss_premise_refuted_2026-05-04.md` frontmatter `status` flipped open → resolved + 2026-05-04 update note appended; OPEN_LOOPS.md regenerated — owning thread(s): clean_teams_identity_pairing — outcome: ✅ loop closed with documented evidence basis + reopen triggers per user instruction "make sure you have adequate evidence and write it so the next agent can be confident"
2026-05-05 early morning — Job B (`9082408392701509632`) failure diagnosed + GCS reports retrieved + analyzed (per user authorization for option 0: read existing reports, no Vertex re-run). Vertex job reached `JOB_STATE_FAILED` 2026-05-04 18:44:42 UTC at the contract-scoring tail; root cause is `arena/score_teams_promotion_contract.py:748` hardcoding `--dev_real_suite teams_real_all_dev` while Job B's suite_manifest (`proper_data_future.provisional_2026-04-19.yaml`) uses `proper_real_teams_dev` — 404 reading `teams_real_all_dev_*_videos_report.csv` because that suite was never validated. Validation phase succeeded for all 64 (suite × ckpt) pairs; reports intact at `gs://training-job-outputs/test_results/job_b_pre_rlp604/job-b-pre-rlp604-2026-05-05/reports/`. **Empirical findings (τ=0.5, FPR<5% on every real cell)**: pre-RLP6_04 chain on `proper_visomaster_enhanced_teams_dev` (n=1182): R12G_STEP14000 9.6%, RLP3_05_STEP2500 12.1%, RLP5_07_STEP20500 94.8%, RLP6_04_STEP23500 85.4%. **Production "viso 27% ceiling" reframed as substrate-specific** (v2 = Dor × diverse-swap-models per memory `project_v2_substrate_is_dor_diverse_swap.md`); on HDTF proper viso enhanced+teams the same chain hits 85% — universal-ceiling claim is REFUTED, trajectory hypothesis is REFUTED on this substrate (chain GAINED capability between RLP3_05 → RLP5_07 when proper data entered the recipe). Caveats: P8A NOT scored in Job B (the head-to-head P8A-on-HDTF-vs-P8A-on-v2 measurement is a $0-15 followup); τ=0.5 not FPR-calibrated (deployment-FPR recalls would be ≥ these); pre-RLP6_04 ckpts use pre-fix INTER_AREA. Artifacts: `analysis/job_b_pre_rlp604_2026-05-04/{FINDINGS.md,scorecard.{csv,wide.csv,json},pivot_at_tau0p5.csv,pivot_summary.json}`. Memory `project_job_b_findings_universal_vs_trajectory_2026-05-04.md` written; existing memory `project_viso_ceiling_unbroken_10_packets.md` amended with scoping note ("v2 substrate ONLY"); thread `viso_bucket_gap.md` extended with "2026-05-04 late night update" subsection. **Live runs status**: BOTH PA + PC-codec succeeded — PA `3140330896851206144` succeeded 20:49 UTC (3h25m, wandb `26u8bn1t`, 16 ckpts at `gs://training-job-outputs/best_checkpoints/26u8bn1t/`, best top_n step5600 AUC 0.9909 EER 0.0282); PC-codec `1202657157175050240` succeeded 21:58 UTC (3h55m, wandb `0ujswaad`, 17 ckpts at `gs://training-job-outputs/best_checkpoints/0ujswaad/`, best top_n step7400 AUC 0.9871 EER 0.0415). In-progress loop `data-axis-clean-single-lever-retest-in-progress` updated with the training-half-complete status; `last_verified` bumped — verdict half (F0 + F4 scoring) is the next action, gates loop closure — owning thread(s): viso_bucket_gap, processing_signature_shortcut, eval_substrate_data_hygiene, preprocessing_parity_bug — outcome: ✅ universal-vs-trajectory question closed (substrate-specific framing); 🟡 data-axis-clean-retest training complete, eval pending; ✅ Job B failure diagnosed (suite-name mismatch in contract scorer, not validation failure)
