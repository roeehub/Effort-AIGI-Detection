# WT-D Handoff

## Scope

Completed the WT-D decision-system/stability track setup on
`wt-d-decision-system-2026-04-17`.

Delivered:

- checked-in report-driven analysis tooling for:
  - threshold sweeps
  - abstain-band analysis
  - frame stability summaries
  - temporal aggregation comparison
  - hysteresis / minimum-positive-run comparison
- focused local verification using synthetic report fixtures
- explicit written judgment on same-day no-new-training leverage

## Files Added

- `DeepfakeBench/training/tools/teams_decision_report_utils.py`
- `DeepfakeBench/training/tools/teams_video_policy_analysis.py`
- `DeepfakeBench/training/tools/teams_frame_policy_analysis.py`
- `DeepfakeBench/training/tests/test_teams_video_policy_analysis.py`
- `DeepfakeBench/training/tests/test_teams_frame_policy_analysis.py`
- `DeepfakeBench/training/docs/TEAMS_DECISION_SYSTEM_REINVESTIGATION_2026-04-17.md`

## Main Judgment

Decision-layer leverage is strong enough to justify immediate no-new-training
work.

Current ranking stance:

- `R12_G_FP32` remains the incumbent safety reference
- `R13_E_BESTSOFAR`, `R13_FT7_FP32`, and `R13_FT9_FP32` stay in the calibrated
  shortlist but remain locally unresolved here
- `R13_A_STEP15500` remains a real contender with clear fake-side gains, but the
  available local frozen readout still shows worse real-side FPR than `R12_G`

Important honesty line:

- this runtime still cannot name a calibrated five-checkpoint promotion winner
  because the shortlist report artifacts are not locally accessible here

## Verification

Ran:

```bash
python3 -m unittest DeepfakeBench.training.tests.test_teams_video_policy_analysis -v
python3 -m unittest DeepfakeBench.training.tests.test_teams_frame_policy_analysis -v
```

Both passed.

## Recommended Next Operator Step

Run the five-checkpoint shortlist through:

1. `tools/teams_video_policy_analysis.py`
2. `tools/teams_frame_policy_analysis.py`

against the actual frozen report root that contains both:

- `*_videos_report.csv`
- `*_frames_report.csv`

If the reports live only in `gs://...`, run inside the training image/runtime
that already has Google storage support installed.

For the smallest recommended baseline pass before new retrains land, use:

- `DeepfakeBench/training/docs/WT_D_MINIMAL_BASELINE_PACKET_2026-04-17.md`
