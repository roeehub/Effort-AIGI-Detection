# State as of 2026-04-30 morning (overnight P14/P15 results landed)

> **Purpose**: single-page snapshot of where the Teams deepfake detector program stands at the moment the 2026-04-29 → 2026-04-30 overnight three-run slate completed and the wiki was rolled forward. **Supersedes** [STATE_2026-04-29.md](STATE_2026-04-29.md) for forward-decisions; the prior page remains as the historical anchor. Date-stamped because state moves quickly; verify against `git status` + the most recent `TIMELINE.md` entries if the wall-clock is materially later.

---

## Where we stand right now (one paragraph)

The Effort detector for Teams deepfake detection has a new in-house leader candidate from the 2026-04-29 → 2026-04-30 overnight slate: **`R13_P14_FACE_SCALE_JITTER_ISOLATED` (run id `mclioexb`, seed 2273, FT-from-P8A_step5000 + face_scale_jitter@0.50 ONLY)** at trainer-side `value_composite=0.661`. The sister variants both underperformed: P14_DATA_FIX (bundle + visomaster_teams_enhanced fw=8.0) collapsed cross-method generalization to `other_fakes_tpr=0.047`, empirically confirming the Move-1 frozen-feature linear-probe prediction (memory `project_move1_bucket_gap_refuted.md`) that the bucket-gap-as-headline-driver framing was wrong. P15 GRL (DANN λ=0.20 static, first-ever production exercise of the GRL infrastructure that has shipped in code since pre-R12) finished second at value_composite=0.516, *structurally working* — the cross-domain head room was preserved (other_fakes_tpr=0.659, the highest of the three) — but materially weaker than the jitter-isolated leader. **The headline finding from the night**: the P14 anti-shortcut bundle (anchor_aware + pipeline_random + face_scale_jitter@0.25) was net-negative vs its single load-bearing component (jitter@0.50 alone), by 5.7× on value_composite. New thread [`anti_shortcut_bundle_decomposition`](threads/anti_shortcut_bundle_decomposition.md) captures the discipline rule: future packets that stack anti-shortcut levers must include a single-lever ablation slot. The promotion-contract scorecard for `mclioexb` is in flight on us-east1 (image 1.3.233, ETA ~3h) and will deliver the production-grade verdict; until it lands, "leader at value_composite=0.661" is trainer-side directional, not deployment-grade. Three high-severity contract-policy + image-currency loops from STATE_2026-04-29 remain open.

---

## In flight / running right now

### (a) Experiments completed overnight

- **`R13_P14_FACE_SCALE_JITTER_ISOLATED`** (run `mclioexb`, us-central1, seed 2273) — 🟢 packet leader; trainer-side `value_composite=0.661`; jitter@0.50 ONLY, anchor_aware + pipeline_random DISABLED. Best ckpt: `gs://training-job-outputs/phase2r13_experiments/mclioexb/value_composite_effort_20260429_step500_auc0.9797_eer0.0521.pth`.
- **`R13_P15_GRL_FROM_P8A`** (run `w5tky6ss`, us-east1, seed 1501) — 🟡 second; value_composite=0.516; bundle + GRL λ=0.20 static, quality_domain_count=3 (PLAN.md §9 Priority 6 round-2 fix from default 4 to 3). First-ever live exercise of GRL infrastructure; smoke gates verified. See [P15 retro](packets/P15.md).
- **`R13_P14_DATA_FIX`** (run `xan4dfto`, us-west4, seed 737) — 🔴 collapse; value_composite=0.126; bundle + visomaster_teams_enhanced fw=8.0; cross-method `other_fakes_tpr` crashed to 0.047. Move-1 prediction empirically confirmed.

See [P14 retro § Results (2026-04-30)](packets/P14.md#results-2026-04-30) for the full ranking table.

### (b) Vertex jobs running 2026-04-30 morning

- **Promotion-contract scorecard for `mclioexb`** — submitted to us-east1 ~08:0X UTC after Cloud Build `46ef62da` (image 1.3.233) finished. Output GCS path: `gs://training-job-outputs/test_results/teams_promotion_contract/<job-name>/`. Will produce: reports/, diagnostic_scorecard/ (fixed-τ=0.5 sidecars), promotion_contract/ (calibrated lockbox-anchored verdict). ETA ~3h.

### (c) Analysis scripts authored (CPU-only, not yet run)

Sub-agent 2026-04-30 authored three frozen-feature probes the user can run on an M-series Mac:

- `analysis/face_size_invariance_2026-04-30/face_size_invariance.py` — re-runs the 47-frame × 5-tightness sweep on Slot 3 vs P8A; **direct measurement** of the [`face_size_label_leak`](threads/face_size_label_leak.md) close criterion's tightness-flips half.
- `analysis/domain_confusion_probe_2026-04-30/domain_probe.py` — 3-way logistic regression on frozen [CLS] features predicting quality_domain; the canonical DANN test of whether GRL's adversarial pressure actually collapsed the domain-discriminable features in P15.
- `analysis/embedding_triptych_2026-04-30/triptych.py` — t-SNE/UMAP of P8A | Slot 2 | Slot 3 colored by (real/fake | capture-mode | face-size bucket); narrative figure for wiki + reports.

### (d) Drafted but not launched

- **`R13_P14_DATA_FIX.yaml`** (`xan4dfto` ran above; loop now superseded as deployed; clean retest with single-lever discipline NOT scheduled).
- **Move-1 frozen-feature linear probe of P8A on eval bucket vs training bucket viso** — superseded by the in-vivo evidence from DATA_FIX. Move-1 was the proposed pre-spend gate for DATA_FIX; DATA_FIX itself ran and confirmed the Move-1 prediction. Memory `project_move1_bucket_gap_refuted.md` is the canonical refutation anchor.

### (e) Fixes in working tree, uncommitted (carried over from STATE_2026-04-29)

- **Contract policy v3 fix.** `+207 net lines` across `arena/score_teams_promotion_contract.py` + `arena/run_target_domain_validation_sequential.py` + `tests/test_score_teams_promotion_contract.py`. Recall floor 0.30 (recent commit `974e033` set default to 0.70; the v3 fix's 0.30-as-overrideable-floor may be uncommitted in working tree). **Third "fix" attempt in 6 days.** See [`contract_policy_bug`](threads/contract_policy_bug.md). The promotion-contract scorecard launched 2026-04-30 morning will exercise the corrected policy on a real candidate; if the readout is clean, the loop closes.
- **VisoMaster enhanced wiring tests** — `tests/test_visomaster_enhanced_wiring.py` (3 tests: domain map, sample tagging, bucket routing). Untracked. The wiring exercised cleanly in DATA_FIX's run, so empirically validated; the test file commit + image rebuild is housekeeping.
- **Three R13 yamls** — `R13_P14_FT_FROM_P8A.yaml` (already ran in slice-7), `R13_P14_DATA_FIX.yaml` (ran 2026-04-30), `R13_P14_FACE_SCALE_JITTER_ISOLATED.yaml` (ran 2026-04-30, leader), `R13_P15_GRL_FROM_P8A.yaml` (ran 2026-04-30). All four yamls untracked.
- **Checkpoint map `arena/checkpoint_maps/teams_target_domain.mclioexb_2026-04-30.yaml`** — authored by the launch sub-agent for the promotion-contract run; 854 bytes; untracked.
- **`PLAN.md`** — modified (untracked? — root-level plan, see `git status`).
- **`HANDOFF.md`** — modified.
- **`CLAUDE.md`** — modified (the US-region preference paragraph from slice 5).
- **`VERSION`** — 1.3.232 → 1.3.233 (image rebuild for the scorecard launch).

### (f) Decisions pending (mapped to user)

1. **Authorize commit of the contract policy v3 fix + image rebuild** — the scorecard run currently in flight will exercise it; if the readout is clean the commit closes two high-severity loops by construction.
2. **Authorize next packet** for whatever follows P15. Options framed in § "Recommended next actions" below.
3. **Authorize commit of the four R13 yamls + the checkpoint map** — housekeeping; the runs already exist on Vertex.
4. **Authorize execution of the three CPU analysis scripts** — close the face-size invariance + domain-confusion + embedding-narrative loops without further Vertex spend.
5. **Authorize promotion-contract scorecard re-runs** for P15 and the bundle baselines (separate spend, ~$10-20 each) — needed if the user wants apples-to-apples deployment-grade comparison vs jitter-isolated.

---

## Eval-substrate caveats (carried over from STATE_2026-04-29)

The post-Slice-7 visual audit findings still apply to every FPR / recall number on this page:

1. **Eval-vs-production crop-tightness mismatch** ([`eval_production_crop_tightness_gap`](threads/eval_production_crop_tightness_gap.md)) — eval frames carry more background context around the face than production crops do; structurally upstream of the face-size leak + camera-signature shortcut + webcam FPR investigations. Implication: translation from eval FPR to production FPR is suspect for background-driven failure modes.
2. **`sharpness_laplacian` is computed on the full image, not the face** ([`sharpness_metric_bug`](threads/sharpness_metric_bug.md)) — per-quartile FPR analyses indexed on this column are partially confounded.
3. **Eval substrate has known data-quality issues** ([`eval_substrate_data_hygiene`](threads/eval_substrate_data_hygiene.md)) — is_no_face slice (n=219) data degeneracy + 99×110-pixel source crops that pass face_pixel_area floor.

These caveats apply uniformly across baseline-vs-corrected comparisons so they do not change relative orderings, but they bear on absolute interpretation of any single AUC or recall number as a production floor.

---

## Headline metrics with caveats (2026-04-30 morning)

### Trainer-side value_composite for the four candidate ckpts

| Ckpt | Recipe | value_composite | other_fakes_tpr | max_pool_fpr | stability |
|---|---|---:|---:|---:|---:|
| **`mclioexb`** | FT-from-P8A + face_scale_jitter@0.50 ONLY | **0.661** | 0.591 | 0.04 | 0.871 |
| `w5tky6ss` (P15 GRL) | FT-from-P8A + bundle + GRL λ=0.20 | 0.516 | **0.659** | 0.04 | **0.086** |
| `xan4dfto` (DATA_FIX) | FT-from-P8A + bundle + viso_teams_enhanced fw=8.0 | 0.126 | 0.047 | 0.04 | 0.360 |
| (slice-7) P14_FT bundle | FT-from-P8A + 3-lever bundle (jitter@0.25) | 0.116 | 0.020 | 0.04 | 0.518 |

`value_composite` is trainer-side directional — see memory `project_promotion_contract.md` and STATE_2026-04-29's headline-metrics caveat: deployment promotion goes through the lockbox-anchored contract scorecard, NOT the trainer composite.

### Production-grade promotion-contract verdict for `mclioexb`

**LANDED 2026-04-30 evening — verdict: does NOT promote** (Vertex job `2402672943822798848`, us-east1, image 1.3.233; succeeded 08:59:05Z; local artifacts `analysis/scorecard_mclioexb_2026-04-30/`; load-bearing memory `project_mclioexb_does_not_promote_2026-04-30.md`).

The scorecard ran under the DEFAULT contract (recall_min=0.7, FPR=0.02, stress=0.05) — the launcher omitted `--promotion_target_fake_recall_min 0.30`, so the v3 fix was NOT exercised. Selected τ=0.975031.

| Suite | P8A baseline (corrected v3 policy, retrospective) | `mclioexb` (default policy, this run) |
|---|---:|---:|
| visomaster_enhanced_macro_dev recall | 13.6% | **1.3%** |
| deeplive_enhanced_dev recall | 23.9% | **2.4%** |
| teams_fake_all_dev recall | 52.6% | **34.7%** |
| dev_fake_macro_recall | ~30% | **12.8%** |
| dev_primary_real_fpr | 1.8% | **2.0%** (at floor) |
| lockbox_real_fpr | 1.8% | **0.22%** |
| lockbox_fake_recall | (n/a) | **18.6%** |
| anchor `dor-real-webcam-false-flag-no-virtual-bg` mean prob_fake | (P8A: ~0.93 post-fix) | (not in this run's checkpoint map) |

The two columns are NOT directly comparable (different policy choices) — but the verdict is robust to the policy choice: a Pareto-frontier scan of the 5549-point τ grid (`threshold_grid.csv`) shows that **even under the in-tree relaxed v3 policy** (recall_min=0.30, FPR=0.07, stress=0.10), zero τ qualifies for `mclioexb`. To hit recall=0.30 the model needs FPR≥0.085 and stress≥0.104, both above the relaxed gates. The trainer-side `value_composite=0.661` win does not translate to a promotable scorecard. **Implication**: the FT-from-P8A track + augmentation tuning has hit a ceiling below the contract gates on this data substrate; successor packet design must consider architectural / data-axis levers, not just augmentation tuning. See packet [`P14`](packets/P14.md) § "Production-grade scorecard verdict — does NOT promote (2026-04-30 evening)" and thread [`jitter_winner_mechanism_unknown`](threads/jitter_winner_mechanism_unknown.md) "2026-04-30 evening" subsection for the full analysis.

---

## Top-priority open loops (the 7 most load-bearing post-2026-04-30)

Running counts from `OPEN_LOOPS.md` (regenerated by `tools/regenerate_open_loops.py` at the end of this session). The ordering is unchanged from STATE_2026-04-29 except for:

- `p14-data-fix-not-launched` → CLOSED (status: superseded by negative empirical result; loop body in [`viso_bucket_gap`](threads/viso_bucket_gap.md)).
- NEW: `anti-shortcut-bundle-needs-single-lever-discipline` (medium) — opens in [`anti_shortcut_bundle_decomposition`](threads/anti_shortcut_bundle_decomposition.md).
- NEW: `bundle-failure-mode-attribution-revision` (low) — opens in same thread; P13's γ-verdict attribution wants revision under the bundle-decomposition lens.
- NEW (2026-04-30 afternoon): `jitter-winner-mechanism-unknown` (medium) — opens in new thread [`jitter_winner_mechanism_unknown`](threads/jitter_winner_mechanism_unknown.md). Direct probes refute both face-size-invariance and GRL-domain-confusion as the design-intent mechanisms; what actually moved value_composite is an open question.
- NEW (2026-04-30 afternoon): `jitter-on-training-substrate-not-yet-tested` (low) — companion loop in same thread; the cheapest of the three candidate mechanism probes.

The seven highest-load-bearing loops at 2026-04-30 morning:

1. **`shortcut-deployment-block`** (in-progress, **critical**) — the camera/ISP shortcut is the residual deployment block. The 2026-04-30 leader `mclioexb` has not yet been tested on the anchor pool (`dor-real-webcam-false-flag-no-virtual-bg` mean prob_fake ≤ 0.30 close criterion). The promotion-contract scorecard pending will provide the anchor-pool readout.
2. **`contract-policy-bug-fix-not-committed`** (open, **high**) — the v3 fix is in working tree, uncommitted. The 2026-04-30 scorecard will exercise the corrected policy on a real candidate; if the readout is clean, the loop closes by construction with the next commit + image rebuild.
3. **`fpr-minimization-no-budget-tau-collapse`** (in-progress, **high**) — paired with #2.
4. **`face-size-label-leak`** (open, **high**) — close criterion's flip-rate half **affirmatively NOT MET** on the strongest available leader as of 2026-04-30 afternoon. Direct measurement via the queued probe (`analysis/face_size_invariance_2026-04-30/`): `mclioexb` flip rate 43.9% on 180 production-honest frames > 10% close-criterion threshold; P8A baseline 39.4% on the same 180 frames. Cohen's d half intentionally not re-measured (conjunction broken). Loop stays open with affirmative no-close evidence recorded.
5. **`anti-shortcut-bundle-needs-single-lever-discipline`** (open, medium, **NEW 2026-04-30**) — discipline rule that drops out of the P14 finding. Closes when the next stacked-lever packet either follows the rule and records the comparison, OR explicit user override authorizes bundle-only packets.
6. **`eval-production-crop-tightness-mismatch`** (open, **high**) — structurally upstream of multiple shortcut/FPR investigations; unchanged.
7. **`sharpness-metric-computed-on-full-image-not-face`** (open, **high**) — unchanged.

---

## Decisions deferred / awaiting Roee

| Decision | Recommendation | User action |
|---|---|---|
| ~~Wait for promotion-contract scorecard verdict on `mclioexb`~~ | LANDED 2026-04-30 evening: does NOT promote | n/a — see "Production-grade promotion-contract verdict" above |
| Authorize P16 direction in light of scorecard verdict | Three classes of move (architectural / substrate / different FT base); cheapest preliminary is the head-only-vs-feature decomposition probe (~1h CPU, no Vertex spend) | OK / change scope / explicit pivot |
| Authorize commit + image rebuild for contract policy v3 fix | Still load-bearing for honest reporting; the 04-30 scorecard ran DEFAULT policy because launcher omitted the arg | OK / push back |
| Authorize re-run of `mclioexb` scorecard with `--promotion_target_fake_recall_min 0.30` | Optional; verdict robust to policy choice but formally closes the v3-fix component 3 | OK / skip — verdict is robust |
| ~~Authorize execution of the three CPU analysis scripts~~ | Ran 2026-04-30 afternoon; results in [`jitter_winner_mechanism_unknown`](threads/jitter_winner_mechanism_unknown.md) | n/a |
| Authorize separate scorecard runs for P15 + bundle baselines (~$30 total) | Lower priority post-scorecard verdict; bundle is presumptively also non-promotable; P15 (`w5tky6ss`) might be informative for the GRL-design path but not load-bearing | OK / not now / not at all |

---

## What changed since STATE_2026-04-29

The slice-7 plan retros for P14 and P15 transitioned to result retros with the overnight slate. The bucket-gap-as-headline-driver hypothesis is empirically refuted at the in-vivo level (DATA_FIX collapsed). The face-size leak's load-bearing role is empirically confirmed at the value_composite level (jitter@0.50 alone leads). The anti-shortcut bundle composed in P13 → P14 → P15 is empirically demonstrated to be net-negative against its single load-bearing component, generating a new discipline rule documented in [`anti_shortcut_bundle_decomposition`](threads/anti_shortcut_bundle_decomposition.md). The GRL infrastructure had its first-ever production exercise and works structurally. None of the eval-substrate caveats from 2026-04-29 afternoon have been resolved; they continue to bear on absolute-magnitude interpretation of every metric on this page.

**Update 2026-04-30 afternoon — design-intent mechanisms for both winners refuted on direct probes.** The three CPU analysis scripts ran. (a) Face-size invariance probe: `mclioexb` (jitter@0.50, the value_composite winner) flipped predicted label on 43.9% of 180 production-honest frames across the t∈[0.7,1.5] sweep — *worse* than P8A baseline's 39.4%, and median |Δprob_fake| almost tripled (0.128 → 0.303). The supposed face-size lever did NOT close the leak; it made the model more sensitive to crop tightness. (b) Domain-confusion linear probe: P8A 0.9999 vs SLOT2_GRL 0.9994 vs SLOT3_JITTER 0.9969 macro-OVR AUC (2-way fallback because lockbox omits df40). GRL at λ=0.20 static did NOT bite the [CLS] domain manifold's separability. (c) Triptych t-SNE: gross arc geometry conserved across all 3 rows; capture-mode and face-size quartile coloring patterns broadly similar. **Conclusion**: the night's value_composite ranking is robust at the metric level but undiagnosed at the causal level. New thread [`jitter_winner_mechanism_unknown`](threads/jitter_winner_mechanism_unknown.md) tracks the open puzzle (3 candidate mechanisms: decision-boundary shift / intermediate-layer features / test-substrate mismatch). The empirical recommendation to use jitter@0.50 over jitter@0.25 still holds, but the *causal story* for why it wins is now an open question. The morning memory framing (`project_face_scale_jitter_load_bearing.md`'s "Why (1)") and morning P14 "Headline finding" subsection were revised the same afternoon to reflect the refutation. The face-size leak's open loop's flip-rate close criterion is now affirmatively NOT MET on the leader — loop stays open.

**Update 2026-04-30 evening — production-grade scorecard verdict landed: `mclioexb` does NOT promote.** The promotion-contract scorecard for `mclioexb` (Vertex job `2402672943822798848`, us-east1, image 1.3.233) succeeded 08:59:05Z. Local artifacts `analysis/scorecard_mclioexb_2026-04-30/`. **Verdict: does not promote.** At the calibrated τ=0.975 (under DEFAULT policy, recall_min=0.7, FPR=0.02, stress=0.05), per-method dev recalls collapsed (visomaster 0.013, deeplive 0.024, teams_fake 0.347), macro 0.128 vs the 0.70 floor. A Pareto-frontier scan of the 5549-point τ grid shows the recall-FPR curve is fundamentally too shallow in the operating region: **even under the in-tree relaxed v3 policy** (recall_min=0.30, FPR=0.07, stress=0.10), zero τ qualifies — to hit recall=0.30 the model needs FPR≥0.085. The launcher omitted `--promotion_target_fake_recall_min 0.30`, so this run did NOT exercise the v3 fix; the `contract-policy-bug-fix-not-committed` close criterion's component 3 remains NOT MET (loop stays open). **The evening's load-bearing reframing**: the FT-from-P8A track + augmentation tuning has hit a ceiling well below the contract gates on this data substrate. The trainer-side `value_composite=0.661` is directionally meaningful within the FT-from-P8A slate but does not predict promotability. Successor-packet design (P16 and beyond) must consider architectural / data-axis levers, not augmentation tuning alone. New memory `project_mclioexb_does_not_promote_2026-04-30.md` captures the verdict; thread [`jitter_winner_mechanism_unknown`](threads/jitter_winner_mechanism_unknown.md) extended with scorecard evidence subsection (open loop downgraded medium → low — deployment half settled, mechanism question is now diagnostic-only); thread [`promotion_contract_evolution`](threads/promotion_contract_evolution.md) extended with "first deployment-grade verdict under v3-fix-era infrastructure: model-substrate is the bottleneck"; packet [`P14`](packets/P14.md) extended with scorecard subsection and Status-card verdict revised from 🟢 leader → 🟡 trainer-side leader, production-grade FAIL.

---

## Recommended next actions for the user

In rough priority order (revised 2026-04-30 evening after scorecard verdict landed):

1. **Decide P16 direction in light of the scorecard verdict.** The catalogued P16 (= FT-from-P8A + jitter@0.50 + GRL ramped λ) is plausible but the 2026-04-30 evening evidence says FT-from-P8A + augmentation tuning has hit a substrate ceiling below the contract. Three classes of move worth considering:
   - **(A) Architectural / head-side lever**: calibration-aware loss, head-only retraining on a richer fake-side distribution, or a different output-head topology. The cheapest probe to inform this is the head-only-vs-feature decomposition catalogued in [`jitter_winner_mechanism_unknown`](threads/jitter_winner_mechanism_unknown.md) (~1h CPU).
   - **(B) Substrate / data-axis change**: explore the unused +2,300 identities (`live-...-teams-v2`, `visomaster-enhanced-face-cropped-v2`, `hdtf_visomaster_*_teams`) per memory `project_data_inventory_identity_diversity.md`, OR rebuild the eval substrate to address the production crop-tightness mismatch ([`eval_production_crop_tightness_gap`](threads/eval_production_crop_tightness_gap.md)).
   - **(C) Different FT base**: P8A is the strongest baseline but FT from P8A_step5000 hits the ceiling; consider an earlier P8A step OR a P-* variant with different anchor-pool behavior as the FT origin.
2. **Run the head-only-vs-feature decomposition probe** — the cheapest of the catalogued candidate-mechanism probes (~1h CPU on M-series Mac, no Vertex spend). Its answer disambiguates whether the calibration cliff is FT-from-P8A-inherited (would reproduce on `9lmvb5b4` step 5000 baseline) vs `mclioexb`-specific. This directly informs whether move (A) head-side or move (C) different FT base is more promising.
3. **Authorize commit of the contract policy v3 fix** + image rebuild. The 2026-04-30 scorecard did NOT exercise it (launcher omitted the arg). The fix is still load-bearing for honest reporting on future scorecard runs; the close criterion's third component requires a representative scorecard run with `--promotion_target_fake_recall_min 0.30` to verify the recall-floor path lands. Re-run on P8A baseline (~$10 / ~3h) is the cleanest closing move.
4. **Authorize commit of the four R13 yamls + the new checkpoint map** — housekeeping; the runs already exist on Vertex.
5. **Optional: re-launch `mclioexb` scorecard with `--promotion_target_fake_recall_min 0.30`** to formally close the v3-fix-arg-not-passed component. Verdict will be the same (the model fails under either policy) but it formally satisfies the contract-policy-bug-fix close criterion's third component.

If you want one move first: **run the head-only-vs-feature decomposition probe** before deciding P16. It's cheap, it disambiguates the next-move class (head-side vs feature-side vs FT-base), and it's the most informative single experiment for the strategic question opened by the evening's scorecard verdict.

---

## Pointer to sub-surfaces

- **`AGENTS.md`** — read/update protocol; the working-tree-diff check is REQUIRED at every session start.
- **`OPEN_LOOPS.md`** — mechanically-generated current open-issue inventory.
- **`TIMELINE.md`** — append-only chronological master index. The 2026-04-30 entries cover the overnight slate, the scorecard launch, and this maintenance pass.
- **`packets/P14.md`** + **`packets/P15.md`** — packet retros now carry Results (2026-04-30) sections.
- **`threads/anti_shortcut_bundle_decomposition.md`** — NEW 2026-04-30; the discipline rule.
- **`threads/face_size_label_leak.md`** + **`threads/viso_bucket_gap.md`** — both extended with 2026-04-30 evidence.
- **Memory** (auto-loaded): `project_face_scale_jitter_load_bearing.md` — NEW 2026-04-30; cross-conversation persistence of the lever-attribution + discipline rule.
- **STATE_2026-04-29.md** — historical anchor; superseded for forward-decisions by this page.
