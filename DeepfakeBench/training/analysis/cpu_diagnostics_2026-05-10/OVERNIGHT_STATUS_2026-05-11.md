# Overnight Status (autonomous run, 2026-05-10 → 2026-05-11)

> Auto-generated by Claude during autonomous overnight execution.
> Updated as events occur.

## TL;DR for morning read

1. **T4 packet result: mechanism works, unstable.** T4-λ1.0 produced inv_mean 0.0414 at step 10500 — first ever above the 0.0349 ceiling shared by all 17 prior trained ckpts. But by step 11250 the encoder had drifted back to 0.0283. Oscillation between high-shortcut and low-shortcut attractors under flat λ. Train-eval AUC reached 0.9939 (best of any T4 ckpt).
2. **T4 promotion scorecard running.** Verdict expected by ~04:00-04:30 CEST. Watch `gs://training-job-outputs/test_results/teams_promotion_contract/teams-promotion-contract-20260511-002920/promotion_contract/promotion_winner.json`. The `digest_t4_scorecard.py` script will auto-generate a digest when run.
3. **T5-A launching overnight.** Same recipe as T4 mainline but with cyclic-λ schedule (triangular wave 0↔1.2, period 1500 steps after warmup) + dense periodic saves every 250 steps + relaxed early-stop patience. Direct response to the oscillation evidence — re-applies pressure each time encoder drifts back.

## Job inventory (final states)

| Run | Job ID | Region | State | Notes |
|---|---|---|---|---|
| T4-λ1.0 | `6720560659215417344` | us-east1 | **SUCCEEDED** (296 min) | Last top_n at step 11250 (AUC 0.9939, EER 0.0195) |
| T4-λ2.0 | `7226459909208932352` | us-west4 | **SUCCEEDED** (170 min) | Early-stopped step 2500; λ=2.0 suppressed train-eval AUC |
| T4 promotion scorecard | `7607488310830694400` | us-east1 | **RUNNING** | Started 00:27 CEST; expected ~04:00-04:30 |
| T5-A us-west4 | `8996374562765537280` | us-west4 | **PENDING** (35+ min) | Hit CLAUDE.md region-rotation cutoff |
| T5-A us-central1 | `8739700428029034496` | us-central1 | **PENDING** (just submitted) | Parallel submission per region-rotation policy |

**⚠ Region rotation in progress.** T5-A us-west4 was PENDING for 35+ min, hitting the CLAUDE.md 30-min cutoff. I launched a parallel T5-A in us-central1 per CLAUDE.md policy. Whichever reaches RUNNING first wins; cancellation of the other requires your authorization (per `feedback_no_cancelling_vertex_jobs.md`).

**If you wake up and both are RUNNING**: please cancel one (preference: cancel the LATER-started us-central1 if both are running, since us-west4 had submission priority).

**UPDATE 02:05 CEST**: us-central1 T5-A (job `8739700428029034496`) reached RUNNING first. us-west4 T5-A (job `8996374562765537280`) is still PENDING. Per CLAUDE.md region rotation, us-west4 should now be cancelled — but cancellation needs your authorization. Command when you wake up:

```
gcloud ai custom-jobs cancel projects/train-cvit2/locations/us-west4/customJobs/8996374562765537280 --region=us-west4 --project=train-cvit2
```

If us-west4 stays PENDING (most likely), no extra cost — Vertex doesn't charge for PENDING. If it ALSO reaches RUNNING, both will train and cost ~2× until you cancel.

## Key findings so far

### Atlas L11 inv_mean (the load-bearing metric)

Prior ceiling across 17 trained ckpts: **0.0349**

Full T4 inv_mean panorama (sorted desc, top 7 of 23 ckpts):
| Ckpt | inv_mean | forgery_AUC | vs ceiling | Notes |
|---|---|---|---|---|
| **T4_L1_step10500** | **0.0414** | 0.9882 | **+18%** | First ever above ceiling |
| T4_L2_step1500 | 0.0346 | 0.9910 | -1% | Near-ceiling EARLY (only 1000 steps post-warmup, λ=2.0) |
| T4_L2_step2500 | 0.0301 | 0.9854 | -14% | λ=2.0 degraded by step 2500; early-stop caught it |
| T4_L1_step11250 | 0.0283 | 0.9846 | -19% | Oscillation drift after step 10500 peak |
| T4_L1_step5000 | 0.0244 | 0.9882 | -30% | Below P8A baseline (0.0266) |
| T4_L1_step9000 | 0.0219 | 0.9890 | -37% | Oscillation trough |

**Both λ_max=1.0 and λ_max=2.0 produce inv_mean lift** — the mechanism is robust to λ choice. λ=2.0 reaches near-ceiling FAST (1000 steps); λ=1.0 takes 10× longer but hits higher peak. The early-stop on T4-λ2.0 turned out to be lucky timing — saved the better ckpt before degradation.

Per-axis at step 10500 vs P8A baseline:
- `is_dor`: 0.9830 vs 0.9951 = **-0.012** (GRL bit this axis)
- `is_chronic_6`: 0.9187 vs 0.9087 = +0.010 (not bit — slightly worse)
- `lap_var_high`: 0.9321 vs 0.9227 = +0.009 (not bit — slightly worse)
- `min_dim_high`: 0.9524 vs 0.9710 = **-0.019** (not a GRL'd axis, moved anyway)
- `face_size_high`: 0.9482 vs 0.9491 = ~equal

### Interpretation

The mechanism IS functional at the encoder level — first quantitative evidence in 13+ R13 packets that L11 invariance is movable through training. The lift magnitude (0.0414 vs 0.0349 = +18%, vs P8A 0.0266 = +56%) is real but smaller than pre-test 1's frozen-feature prediction (4× lift, would have been 0.048+).

The OSCILLATION evidence (step 10500 spike, step 11250 reversion) is the most actionable finding: the encoder VISITS the low-shortcut attractor but doesn't STAY there under flat λ pressure. Three explanations consistent with the data:
- (a) λ=1.0 is too weak to hold the encoder in the attractor
- (b) The forgery loss pulls back toward shortcut-using representations
- (c) The trainer's top_n save-by-AUC misses most attractor moments (only step 10500 was caught because it happened to coincide with a train-eval AUC peak)

T5-A tests (a) via cyclic λ (periodic re-pressuring) + (c) via dense saves (catches attractor moments regardless of AUC).

### NEW: GRL effect is ENTIRELY LOCALIZED at L11 (attachment point)

Per-layer Δinv_mean vs P8A baseline (analysis/cpu_diagnostics_2026-05-10
/outputs/per_layer_inv_mean.csv):

| Ckpt | L0 | L3 | L6 | L9 | **L11** |
|---|---|---|---|---|---|
| T4_L1_step10500 (best) | -0.0002 | +0.0021 | -0.0012 | +0.0047 | **+0.0147** |
| T4_L2_step1500 | -0.0002 | +0.0037 | +0.0017 | +0.0011 | **+0.0079** |

L11 lift is 3-7× larger than any earlier layer's movement. The encoder's
mid-layers (L0-L9) are essentially unchanged — the shortcut representation
is still THERE in all the earlier layers, and L11 only does a LIGHT
rearrangement to defeat the online classifier.

This is a strong negative finding for single-attachment GRL. The encoder
can always do a light L11 rearrangement to satisfy the GRL classifier
without genuinely removing shortcut from features upstream. Bigger
inv_mean lift requires upstream pressure (T5-B: multi-layer attachment).

### NEW: The GRL is biting the CLASSIFIER, not the features

T4-λ1.0 final W&B logs show online GRL classifier CE loss per axis:
- `is_dor`: 0.033 (classifier WINS, predicts is_dor at >95% accuracy)
- `sharpness_laplacian_high`: 0.637 (near 0.69 random — classifier loses)
- `color_a_approx_dev_high`: 0.676 (near-random)

But atlas (fresh LR probe on cached L11 features) shows these axes ARE
still encoded (e.g. `sharpness_laplacian_high` AUC = 0.9321 at step
10500). The features contain the shortcut; the online classifier is
the one being pushed to random by GRL gradient reversal.

This explains why the inv_mean lift is small (~0.0148 absolute above
P8A) despite the GRL "biting" hard on classifier metrics: **the
encoder doesn't actually need to change much — it only needs the
online classifier to lose its training**. A larger classifier (more
parameters / wider hidden_dim) would force the encoder to ACTUALLY
remove the shortcut, because a stronger classifier can't be defeated
by gradient noise alone.

This suggests T5-C (future packet) should increase `hidden_dim` from
256 → 512 or 1024, making the online classifier strong enough that
the encoder can only beat it by genuinely removing the shortcut from
features.

### Honest probabilities (revised from earlier in conversation)

- Probability T4 has a deployment-grade winner in scorecard: **~15-25%** (revised down from 35-45% based on oscillation evidence — the briefly-captured step 10500 ckpt is the one to watch)
- Probability T5-A captures ≥1 ckpt with inv_mean ≥ 0.045: **~50-60%**
- Probability either T4 or T5-A produces a ship-ready candidate THIS BATCH: **~25-35%**
- Probability we learn enough to design the right next packet: **~85-95%**

## Files to check when you wake up

1. **`gs://training-job-outputs/test_results/teams_promotion_contract/teams-promotion-contract-20260511-002920/promotion_contract/promotion_winner.json`** — formal T4 scorecard verdict
2. **`analysis/cpu_diagnostics_2026-05-10/T4_SCORECARD_DIGEST_2026-05-11.md`** — generated digest (will exist after the scorecard finishes + I run digest_t4_scorecard.py)
3. **`analysis/cpu_diagnostics_2026-05-10/outputs/L11_inv_mean_with_t4.csv`** — atlas trajectory
4. **`gs://training-job-outputs/best_checkpoints/<T5-A-run-id>/`** — T5-A ckpts (35 saves planned across 12000 steps if it runs to completion)
5. **W&B**: https://wandb.ai/dtect-vision/phase2-round13 — live training curves

## Morning decision tree (scenarios)

### Scenario A: Scorecard found a T4 winner (ANY ckpt promotes per v3-fix policy)
**Action**: Ship that ckpt. Document T4 as a successful packet.
**Then**: Optionally wait for T5-A — confirms reproducibility but not strictly needed.
**Don't**: Spend on T5-B unless you want robustness over a single packet's evidence.

### Scenario B: Scorecard found no winner BUT T5-A captured a strong inv_mean ckpt
**Action**: Build T5-A scorecard ckpt map (top 3-5 ckpts from T5-A by atlas inv_mean), launch second scorecard run. ~3-4h.
**Then**: Same decision tree applies.

### Scenario C: Both T4 scorecard AND T5-A miss every gate
**Action**: This is when T5-B becomes the next move. Two structural changes the data supports:
1. **T5-B-L6L11**: Dual GRL attachment at L6 + L11. Forces shortcut removal at TWO sequential layers, harder to evade. ~2h engineering + 5h training.
2. **T5-C-bigger-classifier**: Same recipe as T4, but hidden_dim=512 or 1024 instead of 256. Stronger online classifier forces encoder to actually remove shortcut from features (not just defeat the classifier). ~30min engineering + 5h training.

I'd run BOTH in parallel since they test different hypotheses. Combined cost: ~$130-160.

### Scenario D: T5-A produces a clearly stable inv_mean ckpt (≥0.05 sustained for several saves)
**Action**: That's the breakthrough. Build T5-A scorecard ckpt map of THAT ckpt + adjacent step ckpts, launch scorecard.

### Scenario E: T4 has a partial winner (passes some gates but not all)
**Action**: Note WHICH gates miss. Map to T5-B / T5-C / T5-data choice:
- Forgery AUC misses → not a GRL problem; check pipeline_randomization or batch composition
- Dor lockbox FPR misses → encoder didn't remove dor invariance enough → T5-B or T5-C
- F4 viso recall misses → shortcut removal didn't translate to F4 substrate behavior → T5 should add localization aux or different axes

## Decision points awaiting you

### IF T4 scorecard has a clear winner (any T4 ckpt promotes per v3-fix policy)
- Ship that ckpt. T5-A becomes data-gathering for future iterations.
- T5-A still useful: confirms whether the mechanism is reproducible.

### IF T4 scorecard has no winner (all 5 T4 ckpts miss gates)
- Wait for T5-A results. The cyclic-λ + dense saves may catch a better ckpt.
- Consider whether to launch T5-B (L10+L11 dual attachment + broader axes) — requires more engineering (~2h), authorize when convenient.

### IF T4 ckpts are CLOSE on most gates but miss 1-2
- Note which gates are missed.
- This informs whether T5 should add a stability loss (if AUC swings cause cell misses) or different axes (if specific shortcuts persist).

## Loop-avoidance audit

Explicitly NOT doing tonight:
- More data-axis levers (memory `project_data_axis_lever_pulled_twice_no_lift`)
- Output-level GRL retry (P15/P18 both refuted at L11 atlas)
- Augmentation-only attempt (MCLIOEXB confirmed augmentation doesn't move L11)
- Head-only retrain (memory `project_job7_head_retrain_REFUTED_2026-05-04`)
- Curriculum from-scratch (aspirational, no pre-tests run, doesn't fit in 6h)

## Cost so far (estimated)

- T4-λ1.0: ~$60 (5h A100)
- T4-λ2.0: ~$25 (2.8h A100)
- T4 scorecard: ~$30-60 (3-4h × 7 ckpts)
- T5-A (when launched): ~$60-70 (5h A100)
- HDTF (deferred, not running): $0
- Total: ~$175-215 for the night
