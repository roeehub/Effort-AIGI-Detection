# WT-A Handoff

## 1. What Was Re-Investigated

- the April 17 relaunch report and policy-impact memo
- the old Track A composition and structural lane reports
- the frozen shortlist implications
- the reviewed runtime source path under
  `DeepfakeBench/training/data/sources/{combined_paired,visomaster}.py`
- the checked-in viewer overlay path under
  `DeepfakeBench/training/viewer/{discovery,visomaster_policy}.py`

## 2. What Scope Was Refined

Two repo realities changed the WT-A scope:

1. `DeepfakeBench/training/data/` is an ignored local runtime tree in this repo
   layout, not part of the tracked Git tree on `teams-relaunch-root-2026-04-17`.
2. The raw April 17 policy CSV/JSON packet cited by the docs is not checked in.

Because of that, WT-A froze mergeable truth in tracked docs/artifacts instead of
pretending to have completed a merge-safe checked-in loader rewrite.

## 3. What Changed

- added `DeepfakeBench/training/docs/relaunch_handoffs/WT-A_policy_truth_artifact_2026-04-17.json`
  - authoritative WT-A truth packet for current classification, corrected train
    and total counts, direct Teams contamination, and family-level config
    grouping
- added `DeepfakeBench/training/tools/wt_a_policy_truth.py`
  - stdlib renderer / validator for the WT-A truth packet
- added `DeepfakeBench/training/tests/test_wt_a_policy_truth_artifact.py`
  - focused unittest for the artifact arithmetic and same-day labels
- added `DeepfakeBench/training/docs/POLICY_AWARE_SOURCE_REDESIGN_2026-04-17.md`
  - explicit lane freeze, same-day label rules, and tracked follow-up shape for
    `visomaster_hints` / `visomaster_hints_teams`

## 4. What Was Proved

- current checked-in training is **not** proved policy-aware
- current checked-in Track A / merged / teamsonly configs are `old-semantics`
- same-day retrains should **not** be labeled `policy-corrected`
- the only honest future same-day label on current data is `weak-signal-only`,
  and only after explicit hint lanes exist in the training path
- promotion-relevant corrected truth that is exact from the tracked tree:
  - corrected total paired objects: `10,388`
  - corrected train pairs: `8,932`
  - retained hint train pairs: `571`
  - retained hint total pairs: `682`
  - clean direct Teams after policy: `1,111` total / `937` train
- merged VTE remains structurally mixed and still does not emit
  `Teams real + Teams enhanced fake`

## 5. How It Was Verified

- ran the artifact validator:
  - `python3 DeepfakeBench/training/tools/wt_a_policy_truth.py --validate`
- ran the focused unittest:
  - `python3 -m unittest discover -s DeepfakeBench/training/tests -p 'test_wt_a_policy_truth_artifact.py'`

## 6. Remaining Blockers Or Follow-Ups

- exact corrected `val` / `test` hint split counts remain blocked on the missing
  raw April 17 policy packet
- tracked source integration remains blocked on the fact that
  `DeepfakeBench/training/data/` is not part of the tracked branch tree in this
  repo layout

## 7. Merge-Back Status

- pending during worktree development
