# WT-B Handoff

## 1. What Was Re-Investigated

- the WT-A lane freeze, truth artifact, and redesign doc
- the current Round 13 Track A, merged, and teamsonly configs
- the Round 2 target-domain and sampler-truth docs
- the merged WT-C / WT-D / WT-E / WT-F handoffs
- the tracked WT-B worktree itself to see whether explicit hint-lane training
  support exists in tracked code

## 2. Scope Refinement

WT-B does **not** have an honest path to a runnable three-arm family in the
tracked tree today.

The original three-arm question still stands, but only as a future explicit
hint-lane family:

- no hints
- hints only
- hints plus teams hints

WT-B therefore shipped a draft-only package instead of fake executable configs.

## 3. What Changed

Added draft-only YAML planning manifests under fresh names:

- `DeepfakeBench/training/experiments/phase2_round13/R13_WTB1_weak_signal_no_hints_draft_only.yaml`
- `DeepfakeBench/training/experiments/phase2_round13/R13_WTB2_weak_signal_hints_only_draft_only.yaml`
- `DeepfakeBench/training/experiments/phase2_round13/R13_WTB3_weak_signal_hints_plus_teams_draft_only.yaml`

Added runbook / launch notes:

- `DeepfakeBench/training/docs/R13_WTB_WEAK_SIGNAL_DRAFT_RUNBOOK_2026-04-17.md`

## 4. What Was Proved

- explicit `visomaster_hints` / `visomaster_hints_teams` training support is
  still not proved in the tracked tree
- in the WT-B worktree, `DeepfakeBench/training/data/sources/combined_paired.py`
  and `DeepfakeBench/training/data/sources/visomaster.py` are absent from the
  tracked checkout
- searching the tracked training tree finds the hint-lane names only in docs,
  viewer code, and WT-A truth artifacts, not in tracked training-source code
- the honest same-day framing therefore remains:
  - `draft-only weak-signal plan blocked on source integration`
- the best minimal future family is a conservative fine-tune family, not a
  scratch / `realboost` / `teamsonly` / `p_original` sidecar
- `visomaster_teams_enhanced` was intentionally removed from the future family
  shape because it is a mixed diagnostic lane and would confound an explicit
  weak-signal ablation

## 5. Verification

Completed:

- `rg -n "visomaster_hints|visomaster_hints_teams" DeepfakeBench/training`
- `git ls-files DeepfakeBench/training/data/sources/combined_paired.py DeepfakeBench/training/data/sources/visomaster.py`
- `rg --files DeepfakeBench/training/data`

Dry-run limitation:

- `python3 DeepfakeBench/training/train_sweep.py --config DeepfakeBench/training/experiments/phase2_round13/R13_FT7_trackA_merged_base.yaml --dry-run`
- result: fails immediately with `ModuleNotFoundError: No module named 'yaml'`

Because the host shell lacks `PyYAML`, WT-B used syntax-level and tracked-tree
evidence checks instead of a repo dry-run.

## 6. Blocking Dependency

WT-B remains blocked on WT-A-owned tracked training-source integration for:

- `combined_paired.visomaster_hints`
- `combined_paired.visomaster_hints_teams`

WT-B did **not** take over that ownership boundary.

## 7. Merge-Back Status

- ready to merge back as a blocked-but-honest draft-only package
- task-board status should be updated to `blocked`, not papered over as a
  runnable weak-signal family
