# WT-E Handoff

**Date:** April 17, 2026  
**Track:** WT-E Evaluation Contract / Promotion Tooling  
**Branch:** `wt-e-promotion-contract-2026-04-17`

## What Changed

- added an authoritative promotion-contract suite manifest:
  - `DeepfakeBench/training/arena/target_domain_suites.teams_promotion_contract_2026-04-17.yaml`
- added an authoritative reduced shortlist checkpoint map:
  - `DeepfakeBench/training/arena/checkpoint_maps/teams_target_domain.promotion_shortlist_2026-04-17.yaml`
- taught `arena/run_target_domain_validation_sequential.py` to emit calibrated promotion-contract artifacts in the same run when `--promotion_contract_dir` is provided
- extracted reusable artifact writing from `arena/score_teams_promotion_contract.py`
- added one-command local and Vertex promotion wrappers:
  - `arena/run_teams_promotion_contract.sh`
  - `arena/launch_teams_promotion_contract.sh`
- relabeled the fixed-threshold scorecard wrappers as diagnostic-only
- added a dedicated promotion runbook and updated the old scorecard runbook to point at it
- corrected the stale fake-lockbox statement in `research_2026-04-15_round2/01_evaluation_contract_and_shortlist.md`

## Promotion-Authoritative Path

Use:

- suite manifest:
  - `DeepfakeBench/training/arena/target_domain_suites.teams_promotion_contract_2026-04-17.yaml`
- checkpoint map:
  - `DeepfakeBench/training/arena/checkpoint_maps/teams_target_domain.promotion_shortlist_2026-04-17.yaml`
- local wrapper:
  - `DeepfakeBench/training/arena/run_teams_promotion_contract.sh`
- Vertex launcher:
  - `DeepfakeBench/training/arena/launch_teams_promotion_contract.sh`

Authoritative artifacts are:

- `threshold_grid.csv`
- `selected_threshold_scorecard.csv`
- `checkpoint_summary.csv`
- `promotion_contract.json`
- `promotion_winner.json`

## Diagnostic-Only Paths

These remain useful, but should not decide promotion:

- `DeepfakeBench/training/arena/run_teams_target_domain_scorecard.sh`
- `DeepfakeBench/training/arena/launch_target_domain_scorecard.sh`
- `--scorecard_*` outputs from `run_target_domain_validation_sequential.py`
- `DeepfakeBench/training/arena/target_domain_suites.r13_best_megaval_2026-04-13.yaml`
- training holdout and `ood_composite` checkpoint narratives

## Re-Investigation Findings

- the calibrated scorer in `arena/score_teams_promotion_contract.py` was already the right core implementation
- the checked-in frozen suite files already include `teams_fake_all_lockbox`, so the older missing-fake-lockbox claim was stale
- the real operational gap was that the default runnable path still stopped at fixed-threshold `0.5` scorecards and made them look more official than they are
- the current promotion shortlist is now frozen to five candidates:
  - `R12_G_FP32`
  - `R13_A_STEP15500`
  - `R13_E_BESTSOFAR`
  - `R13_FT7_FP32`
  - `R13_FT9_FP32`

## WT-A / WT-F Alignment

- current frozen Teams lane names stay unchanged here; WT-E does not invent future proper-data aliases
- this path scores current deployment-relevant frozen slices only
- future proper-data naming should still be coordinated with WT-F
- current lane-truth caveats from WT-A still apply; this tooling does not reinterpret those semantics

## Verification

Verified locally:

- `python3 -m py_compile DeepfakeBench/training/arena/run_target_domain_validation_sequential.py DeepfakeBench/training/arena/score_teams_promotion_contract.py`
- manual invocation of targeted test functions from:
  - `DeepfakeBench/training/tests/test_score_teams_promotion_contract.py`
  - `DeepfakeBench/training/tests/test_target_domain_suite_manifest_builder.py`
- `bash -n` on the new and updated shell wrappers
- `bash arena/run_teams_promotion_contract.sh --dry-run`
- `bash arena/run_teams_target_domain_scorecard.sh --checkpoint-map arena/checkpoint_maps/teams_target_domain.promotion_shortlist_2026-04-17.yaml --checkpoints R12_G_FP32 --dry-run`

Environment note:

- `pytest` is not installed in this shell environment
- `PyYAML` is also not installed here, so `run_target_domain_validation_sequential.py` now has a stdlib-only fallback for the simple YAML manifest and checkpoint-map shapes used by these checked-in promotion artifacts
