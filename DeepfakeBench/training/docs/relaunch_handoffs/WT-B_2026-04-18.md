# WT-B Handoff

## 1. What Was Re-Investigated

- the WT-A lane freeze and policy-truth artifact
- the current `combined_paired` training runtime instead of only the earlier
  tracked-tree snapshot
- the viewer-side April 17 policy overlay contract
- the FT11 scorecard-base config used as the WT-B anchor

## 2. Scope Refinement

WT-B still should be a narrow three-arm weak-signal ablation:

- no hints
- hints only
- hints plus teams hints

The important refinement is that the direct `teams` lane must also be cleaned by
policy, otherwise the retained Teams-played hint rows are silently duplicated in
the control arm and the family stops being honest.

## 3. What Changed

Local runtime support landed for:

- `combined_paired.visomaster_hints`
- `combined_paired.visomaster_hints_teams`
- `combined_paired.teams.apply_bad_data_policy`

Supporting updates landed in:

- `DeepfakeBench/training/data/sources/combined_paired.py`
- `DeepfakeBench/training/utils/grouping.py`
- `DeepfakeBench/training/data/augmentations/pipelines.py`
- `DeepfakeBench/training/tests/test_phase4_family_pipeline.py`

Portability follow-up landed in:

- `.gitignore`
- `DeepfakeBench/training/viewer/visomaster_policy.py`
- `DeepfakeBench/training/policy/visomaster_bad_data/`

Fresh runnable configs landed under new names:

- `DeepfakeBench/training/experiments/phase2_round13/R13_WTB1_weak_signal_no_hints.yaml`
- `DeepfakeBench/training/experiments/phase2_round13/R13_WTB2_weak_signal_hints_only.yaml`
- `DeepfakeBench/training/experiments/phase2_round13/R13_WTB3_weak_signal_hints_plus_teams_hints.yaml`
- `DeepfakeBench/training/experiments/phase2_round13/R13_SMOKE_WTB3_weak_signal_hints_plus_teams_hints.yaml`

Runbook / launch notes:

- `DeepfakeBench/training/docs/R13_WTB_WEAK_SIGNAL_RUNBOOK_2026-04-18.md`

The older draft-only package was left in place as historical context and marked
superseded.

## 4. What Was Proved

- the local runtime can now emit explicit `visomaster_hints` and
  `visomaster_hints_teams` lanes
- the direct `teams` lane can be policy-filtered so WT-B does not double-count
  retained Teams-played hint rows
- the new hint lanes have explicit family/group routing for reporting and
  augmentation instead of collapsing into generic `visomaster` / `deeplive_teams`
- the three WT-B YAMLs parse cleanly and encode the intended arm toggles
- the April 17 policy packet now has a tracked canonical home under
  `training/policy/visomaster_bad_data/`
- the repo-level ignore blocker for `training/data/**/*.py` is removed, so the
  runtime package is now visible to Git instead of being trapped in an ignored
  subtree
- the WT-B path now has a launcher-oriented smoke config plus discovery-cache
  wiring for DeepLive, VisoMaster hints, and Teams passthrough so the first
  remote smoke can prewarm the expensive source listings

## 5. Verification

Completed:

- `python3 -m py_compile DeepfakeBench/training/utils/grouping.py DeepfakeBench/training/data/augmentations/pipelines.py DeepfakeBench/training/data/sources/combined_paired.py DeepfakeBench/training/viewer/visomaster_policy.py DeepfakeBench/training/tests/test_phase4_family_pipeline.py`
- `pytest DeepfakeBench/training/tests/test_phase4_family_pipeline.py::test_group_key_mapping_representative_cases DeepfakeBench/training/tests/test_phase4_family_pipeline.py::test_quality_targeted_family_router_forward_pass DeepfakeBench/training/tests/test_phase4_family_pipeline.py::test_quality_targeted_family_router_registers_hint_families DeepfakeBench/training/tests/test_phase4_family_pipeline.py::test_wt_b_policy_partition_helpers_split_clean_and_hint_lanes DeepfakeBench/training/tests/test_phase4_family_pipeline.py::test_discover_teams_passthrough_samples_uses_fresh_cache_without_gcs DeepfakeBench/training/tests/test_phase4_family_pipeline.py::test_iterate_wt_b_hint_lane_samples_keep_explicit_source_names -q`
- `python3 - <<'PY' ... yaml.safe_load(...) ... PY` over the three WT-B YAMLs plus the new WT-B smoke YAML

Result:

- targeted WT-B checks passed
- YAML syntax / key toggles parsed as expected for both the real family and the smoke config
- a broader `pytest DeepfakeBench/training/tests/test_phase4_family_pipeline.py -q`
  pass is still not clean, but the remaining failures are unrelated to WT-B
  (`context_variation_block_produces_transforms` expectation drift and two
  `trainer` import-path failures)

## 6. Remaining Gaps / Follow-Up

- no remote WT-B smoke has been run yet; the next honest gate is a launcher
  smoke via `cd DeepfakeBench/training && ./launch_experiment.sh ...` after the
  training image is rebuilt from this committed tree
- the broader `test_phase4_family_pipeline.py` file still has unrelated
  non-WT-B failures outside the targeted WT-B proof set
- incoming new-bucket loader work is still outside WT-B proper and remains the
  next upgrade dependency once bucket structure details are available

## 7. Merge-Back Status

- committed on `teams-relaunch-root-2026-04-17`
- remote smoke, image publication, and new-data integration still remain
  operator follow-up rather than WT-B local-code gaps
