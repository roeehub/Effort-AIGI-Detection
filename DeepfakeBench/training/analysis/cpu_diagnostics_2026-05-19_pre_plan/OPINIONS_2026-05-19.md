# OPINIONS — Pre-plan synthesis 2026-05-19

> **OPINION doc.** Per `AGENT_GUIDE.md` Rule 5: every interpretive claim below is one agent's reading. Past framings in this codebase have been demonstrably wrong — see e.g. memory `project_p18_method_grl_does_not_bite_2026-05-01` (original verdict retracted), `project_resolution_chain_instability_2026-05-15` (probe interpretation walked back after the 2026-05-16 scorecard). Treat this doc as the synthesis I would currently make, not as ground truth. The FACTS in `FACTS_2026-05-19.md` are the load-bearing surface.

## §1 — TL;DR (one-paragraph)

The 10-job CPU battery converges on a single dominant reading: **the binding production constraint is the `dor_shkedi` under-covered IQ pocket**, and **Slot β's viso lift comes at the price of concentrating 93.5% of its lockbox FPR on exactly that pocket**. Three further findings reshape the planning surface materially: (a) **P8A's rank-1 is a tiebreak artifact** — P8A is rank-1 under exactly 1 of 8 reasonable tiebreaks, T5C wins under 5; (b) **the joint-marginal §4.2 pre-launch pocket spec is misspecified** — it contains 0% of the under-covered chronic identities it was meant to target, and 51% of an over-covered identity it should exclude; (c) **P8A and the T5C-family are operationally orthogonal on lockbox reals** (Pearson 0.10–0.34) — an ensemble path is structurally live for the first time in the packet sequence. Net: the cleanest deployment-grade move is not a new training packet — it is **ship T5C step3500 with v3-fix tiebreak amended to lockbox_fake_recall (or a small-k composite)**, then plan a targeted dor_shkedi-pocket data ingestion as the next training intervention. Confidence in TL;DR: MEDIUM-HIGH (~70%).

## §2 — Findings ranked by load-bearingness for the next decision

### (1) The §4.2 pre-launch pocket spec is REFUTED. The data-ingestion lever needs a per-identity spec, not a single-rule spec. [HIGH-confidence]

**Fact:** Job H §7 — the spec `min_dim > 350 AND (color_a_dev < 4 OR > 12) AND skin_frac < 0.35` matches **0 frames** in lockbox_real and **0 frames** of `dor_shkedi`, `Chikara_Takahashi__s22`, or `PC_Generator__s15` (the 3 named under-covered chronic identities). It matches 51% of `bla_bla_chow` (over-covered) — the wrong direction.

**Interpretation.** The joint-marginal author wrote a spec that contains the *audit's exemplar identities for the under-covered claim* but not the *actual frames the model fails on*. The under-covered identities have heterogeneous IQ profiles (dor_shkedi: high lap_var 419, mid color_a 4.6, low skin_frac 0.22; Chikara: low lap_var 115, mid color_a 5.5, mid skin_frac 0.55; PC__s15: very low lap_var 21, high color_a 18.5, high skin_frac 0.67). A single rectangular pocket in this 4-axis space cannot contain all three. The audit's own joint-marginal §4 inferences (which preserve confidence in the data lever) do not survive contact with the §4.2 specification.

**Implication.** Any "ingest data for the dor_shkedi pocket" proposal must be specified per-identity, with explicit IQ profiles per binding identity. The §4.2 spec **must not** be used as the data-ingestion target.

**Confidence:** HIGH (~85%). The data is unambiguous; the spec catches 0 lockbox_real frames in a 1500-frame pool.

### (2) Slot β over-fires concentrate 93.5% on the under-covered `dor_shkedi` identity. The "rule-rescue" mechanism is structurally aligned with the binding failure mode — but the new-failure direction is concerning. [MEDIUM-HIGH]

**Fact:** Job A §2 — 116 of Slot β's 124 lockbox over-fires are on `dor_shkedi`. Job F §5 — 86 of 87 Slot β NEW failures (vs T5C) are on `dor_shkedi`. Job G §6 — `dor_shkedi` is under-covered (joint-marginal median ratio 0.21).

**Interpretation.** Slot β's 6-axis GRL extension does something to the encoder that makes the `dor_shkedi`-pocket fail mode MORE pronounced. This is **the opposite** of what 6-axis GRL was designed to do (chronic_flag + IQ-axis adversarial penalty should pull the encoder away from chronic-FP regions). One mechanism consistent with this: the extra GRL axes (color_b_dev_high + luma_mean_high) re-distribute the encoder's discriminative capacity toward color_b / luma channels, and the score distribution on `dor_shkedi` lifts because `dor_shkedi` sits at a different point in color_b × luma space than the train pool's reals.

**Implication.** Slot β + rule-rescue (per memory `project_blend_unsharp_lever_2026-05-14` Option 3) IS structurally aligned with the failure mode IF the rule's gate threshold (`frac>0.6 AND count>0.9`) actually contains `dor_shkedi` frames. Without verifying that, the rule-rescue is speculative. Verification requires the per-identity blend/unsharp tags on this specific lockbox slice — that probe is not in this packet.

**Confidence:** MEDIUM-HIGH (~70%) on the localization fact; MEDIUM (~50%) on whether rule-rescue applies cleanly.

### (3) P8A's rank-1 is a tiebreak artifact. T5C step3500 is rank-1 under 5 of 8 tiebreaks, including the entire composite-cost-ratio family. [HIGH]

**Fact:** Job B §4 — Under v3-fix's `lockbox_real_fpr asc` tiebreak, P8A is rank-1. Under `dev_fake_macro_recall desc`, Slot β is rank-1. Under `lockbox_fake_recall desc`, T5C is rank-1. Under the composite family `lockbox_fake_recall − k × lockbox_real_fpr` for k ∈ {1, 2, 5, 10}, T5C is rank-1 in all four.

**Interpretation.** The current contract policy weights real FPR more heavily than fake recall, in a way that no reasonable cost-ratio supports unless `k > 20` (where the marginal FN cost is 20× the marginal FP cost — extreme). For the Teams use-case where FN means missing a deepfake on a live call, the production-relevant `k` is much lower (probably 1-3, i.e., FP and FN are comparable cost or FN is mildly more costly). Under any such `k`, T5C step3500 is the deployment-correct choice.

**Implication.** The v3-fix contract is mis-calibrated relative to the production cost ratio. A small amendment — swap the tiebreak from `lockbox_real_fpr asc` to `lockbox_fake_recall desc` (or composite k=1) — would promote T5C step3500. Cost: 0 GPU. Risk: low; T5C step3500 has lockbox_real_fpr 0.028 (vs P8A 0.018, T5C is +0.010 absolute FPR), lockbox_fake_recall 0.660 (vs P8A 0.387, T5C is +0.273 absolute recall). 27pp more fake catches at the cost of 1pp more real flags.

**Confidence:** HIGH (~85%) on the tiebreak math. The remaining uncertainty is whether the user's production cost ratio actually matches k≈1.

### (4) P8A and T5C-family are orthogonal on lockbox reals (Pearson 0.10–0.34). The ensemble path is structurally live. [HIGH]

**Fact:** Job J §9 — Pearson on lockbox-real scores: P8A vs T5C 0.277; P8A vs Slot α s1500 0.104; P8A vs Slot β 0.142. All T5C-derivatives pairwise 0.90+.

**Interpretation.** P8A's failure mode (Chikara + PC_Generator__s15) and T5C's (dor_shkedi + bla_bla_chow__s1) live on different encoder directions. An ensemble at inference time (P8A min OR P8A × T5C product OR per-substrate routing) could catch chronic-FPs that neither single ckpt catches.

**Implication.** The label-free ensemble was REFUTED in `project_job12_ensemble_ceiling_2026-05-04` for VISO RECALL (best non-oracle ensemble 16% vs P8A 27%). But that result was about COMPLETING fake catches — the ensemble has been UNDERVALUED for the FPR-REDUCTION direction. A "P8A AND T5C must both fire" gate would zero-out the chronic-FP set that lives only in one ckpt's failure mode, at the cost of recall (fake must pass both ckpts' τ). This is a different lever class than label-free ensembling for recall.

**Per-quartile evidence** (Job B §4): P8A's worst FPR is `face_pixel_area Q1_low` (0.21 FPR — small-face frames). Slot β's worst is `brightness_v_mean Q4_high` (0.26) and `sharpness Q4_high` (0.21). P8A and Slot β's worst quartiles are disjoint axes — a routing rule by IQ axis could halve worst-slice FPR.

**Confidence:** HIGH (~80%) on the orthogonality fact; MEDIUM-HIGH (~65%) on whether a per-IQ-quartile router is operationally implementable in deployment.

### (5) Slot β's "viso lift" comes with a baseline AUC drop on the 388-frame panel (0.704 vs T5C 0.837, P8A 0.914). [HIGH]

**Fact:** Job C §1 — Slot β baseline AUC 0.704; Slot α baseline 0.734; T5C 0.837; P8A 0.914. The drop persists at every down-size.

**Interpretation.** Per AGENT_GUIDE Rule 6: AUC preservation distinguishes encoder-level invariance from score-distribution compression. Slot β's −0.13 AUC drop relative to T5C is **NOT** preservation. This is consistent with the score-compression failure mode, just expressed at L11 differently than Slot α. The 388-frame panel is dor-heavy — but that's exactly where Slot β over-fires anyway, so this isn't a non-representative-panel artifact.

**Caveat.** The 388-frame panel is *not* the full scorecard substrate. Slot β catches more viso fakes on the contract surface (Job §0 selected_threshold_scorecard). So Slot β's encoder has learned SOMETHING useful for viso even as its dor-cohort discriminability dropped. The two facts are not contradictory — viso and dor cohort are different operating regions.

**Implication.** Slot β as-trained is not a clean deployment candidate. The viso lift is real but operationally entangled with the dor regression. A controlled re-derivation (e.g., 6-axis GRL on a substrate that explicitly preserves dor) would tell us whether the trade is the lever's signature or a one-off training artifact.

**Confidence:** HIGH on the AUC drop fact. MEDIUM on the score-compression mechanism attribution (we cannot rule out that Slot β's L11 representation legitimately re-allocated capacity in a way that helps viso and hurts dor — a not-trivially-better-or-worse re-allocation).

### (6) Anchor_aware's mechanism is not purely data-coverage-substitute. It rescued BOTH under-covered AND over-covered chronic identities. [MEDIUM]

**Fact:** Job G §6 — Of the PC_Generator sub-identities anchor_aware rescued in 2026-05-16, 1 is under-covered (PC__s15, ratio 0.32) and 4 are over-covered (PC__s4/s8/s22/s3, ratio 444–1256).

**Interpretation.** The earlier "data-coverage substitute" reading of anchor_aware (the joint-marginal audit's §4 OPINION) is partial. The mechanism does something representation-level too, OR the bimodal partition's per-identity coverage classification is noisier than its bimodal extreme makes it look (a single chronic identity can have sub-IDs spanning ratios 0.32 → 1256 — that's not a tight cluster, that's an identity with multiple distinct capture conditions).

**Implication.** anchor_aware as a lever class is more flexible than "data substitute" — it can pull encoder embeddings in over-covered regions too. The Roy_D regression (29→81%) is then NOT explained by "Roy_D is under-covered so anchor on dor doesn't help" — the explanation must be representation-level (encoder shifts toward dor-pocket, away from Roy_D-pocket in a way that's not coverage-symmetric).

**Confidence:** MEDIUM (~55%). The bimodal split's robustness is itself in question; per-identity sub-IDs may not be tightly clustered.

### (7) Roy_D is in dev + chronic-flag GRL supervision but probably not in train data pool. The Roy_D-color_b association is generalization, not memorization. [MEDIUM-HIGH; closes a HIGH-severity loop]

**Fact:** Job I §8 — Roy_D is in `arena/manifests/...split="dev"`, in `combined_paired.CHRONIC_IDENTITIES` substring constant, in 13 R13 yaml descriptions, and **not detected** in train_teams_real_pool / unified_tags / lockbox_tagging artifacts.

**Interpretation.** Loop `roy-d-color-b-dev-mechanism` (MEDIUM, 2026-05-07) asked: is P8A's r(score, color_b_dev) = −0.714 on Roy_D generalization or memorization? **Conclusion: generalization.** Roy_D is not in P8A's training data; the encoder learned the color_b association from other (named or unnamed) train identities and the association transfers to Roy_D at eval.

**Caveat.** The atlas samples 500 frames per pool; absence in atlas ≠ certainty of zero. A definitive GCS-side enumeration (similar to `analysis/cpu_diagnostics_2026-05-12_gcs_identity_audit`) would close the loop with full confidence. Status: **resolvable**, pending GCS audit.

**Implication.** The Roy_D regression in Slot A v2 (anchor_aware, 29→81%) is a genuine generalization failure — the anchor_aware loss pulled the encoder toward the dor pocket in a way that hurt the held-out Roy_D pocket. This is a clean signal that anchor-pool design matters: a dor-only anchor pool can't preserve a Roy_D-region invariance even on a Roy_D-absent train set, because the encoder's L11 distance map shifts.

**Confidence:** MEDIUM-HIGH (~70%). The fact pattern strongly supports the generalization framing; a GCS audit would lift confidence to HIGH.

## §3 — What this means for the next move (recommendation)

I see three coherent paths. Each has a clear gate-to-next-decision and a different cost profile.

### Path A — Ship T5C step3500 with amended tiebreak; defer training packet [LOWEST cost, HIGHEST confidence]

**Move.** Amend the v3-fix policy's tiebreak from `ascending lockbox_real_fpr` to either (a) `descending lockbox_fake_recall`, or (b) composite `lockbox_fake_recall − k × lockbox_real_fpr` with `k ∈ {1, 2}`. Both promote T5C step3500. Ship T5C step3500.

**Why.** Under any reasonable cost-ratio interpretation, T5C step3500 catches 1.7× more lockbox fakes at +1pp absolute FPR. The current v3-fix tiebreak weights real FPR at ratios ≥20× fake recall — implicit, not stated, and probably not what production wants.

**Cost.** $0.
**Time.** Hours, if user agrees.
**Risk.** Low. T5C step3500 has been on the rank-2 line since 2026-05-12. Memory `project_t6_t7_t5c_scorecard_2026-05-12` already framed the FP/FN trade.
**Gate.** User must accept the +1pp lockbox real FPR. The chronic identities T5C over-fires on (`dor_shkedi`, `bla_bla_chow__s1`) are different from P8A's (`Chikara_Takahashi__s22`, `PC_Generator__s15`).
**Closes:** `lockbox-real-fpr-tiebreak-is-load-bearing` (MEDIUM); changes the deployment recommendation to T5C step3500.

### Path B — Per-IQ-quartile ensemble routing (P8A ∧ T5C); no new training [LOW cost, MEDIUM confidence on payoff]

**Move.** Build an inference-time router: classify each frame by IQ-quartile signature; route to P8A or T5C based on which ckpt has lower historical FPR in that quartile. Combine with per-substrate τ-calibration.

**Why.** Job B §4 + Job J §9 — P8A and T5C overfire on disjoint IQ quartiles. A router can cherry-pick the lower-FPR ckpt per IQ region without retraining.

**Cost.** $0 GPU; ~1 week of inference-side engineering + production wiring.
**Risk.** MEDIUM. Inference-time IQ tagging adds latency and a failure mode. Requires careful calibration on a held-out non-lockbox real cohort.
**Gate.** CPU diagnostic to estimate the upper-bound router performance — a simulated oracle router on the current lockbox panel — should land before any engineering work.
**Closes:** `eval-production-crop-tightness-mismatch` partially (the router could include crop-tightness as a routing axis).

### Path C — Targeted per-identity dor_shkedi-pocket data ingestion training packet [HIGHER cost, MEDIUM confidence on payoff]

**Move.** Build a GCS-side enumeration that finds real Teams-call frames matching the per-identity dor_shkedi IQ profile (median lap_var 419, color_a_dev 4.6, skin_frac 0.22, min_dim 393). Add ~500–1000 such frames as a separate real lane (family weight ~3–5). FT from T5C step3500 for ~2000 steps. Score under v3-fix policy.

**Why.** Job A §2 + Job F §5 — Slot β's failure mode is 93% concentrated on dor_shkedi (under-covered). If targeted data ingestion in this specific IQ pocket can populate the train density there, the model should stop reading it as fake.

**Cost.** ~$25–50 GPU + ~1 day GCS-side data preparation.
**Risk.** MEDIUM. The §4.2 spec is refuted — a fresh per-identity spec is required (this is the prerequisite). Memory `project_data_axis_lever_pulled_twice_no_lift` is structurally distinct from this (those were bucket-rebalance, not pocket-targeted) but the user has standing skepticism on the data lever.
**Gate.** Pre-launch CPU step: enumerate the GCS pool for frames matching the dor_shkedi IQ pocket. If the pool has <500 candidate frames, this lever is data-poor; pivot to Path D.
**Closes:** `slot-b-viso-lift-mechanism-and-lockbox-fpr-localization` (HIGH) if the trained ckpt has lockbox FPR reduced on dor_shkedi by ≥0.05 absolute.

### Path D — Multi-anchor anchor_aware (dor + Roy_D pools) [HIGHER cost, MEDIUM confidence; structural insight from Job G]

**Move.** Re-derive anchor_aware with anchor pool = {dor false-flag, Roy_D real frames from arena dev manifest}. Train as Slot A v2's recipe but with the broader anchor pool.

**Why.** Job G §6 — anchor_aware works on both under- and over-covered chronic identities. The Roy_D regression in Slot A v2 (29→81%) was an artifact of dor-only anchor pool. Adding Roy_D to the anchor pool should preserve invariance there too.

**Cost.** ~$25–50 GPU.
**Risk.** MEDIUM. Open loop `roy-d-specific-anchor-pool-packet` (HIGH, 2026-05-16) explicitly proposes this; the implementation cost is small.
**Gate.** Verify Roy_D frame availability in arena dev manifest (Job I §8 confirms presence in dev — should be ≥50 frames; not enumerated this packet).
**Closes:** `roy-d-specific-anchor-pool-packet` (HIGH).

## §4 — Sequencing recommendation

**Today / this week:**
1. **User decision: Path A.** Amend the tiebreak. Ship T5C step3500. This is the cheapest and highest-confidence move. The under-covered chronic identities (Chikara + PC_Generator__s15) that P8A still fails on are not catastrophic — they're a 26-28% FPR on a 23-129-frame slice each, totaling ~70 frames absolute.
2. **In parallel, CPU pre-launch step for Path C/D:** GCS enumerate (a) frames matching `dor_shkedi` per-identity IQ profile in candidate buckets, (b) Roy_D frames in arena dev manifest with full identity tags. Cost: ~$0–2 in GCS list quota. Time: ~2 hours.

**Next week (decision branching after CPU step):**
3. If GCS enumeration shows ≥500 candidate dor_shkedi-pocket frames AND ≥50 Roy_D dev frames: run Path C and Path D in parallel ($50–100 total). Both have distinct lever classes.
4. If not: pivot to Path B (per-quartile router) as the operationally cheapest improvement.

**Hold off on:**
- Sister 5-axis Slot β variant. The AUC drop in Job C §1 suggests the 6-axis GRL is on the score-compression trajectory; subtracting 1 axis from 6 doesn't restore representation quality.
- Full re-training packets (LoRA, GRL extensions) until the data lever is empirically tested.

## §5 — Confidence summary

| Claim | Confidence |
|---|---:|
| P8A's rank-1 is tiebreak-artifact; T5C step3500 wins under cost-ratio k ≤ 5 | HIGH (~85%) |
| §4.2 pocket spec is misspecified; under-covered chronic identities have heterogeneous IQ profiles | HIGH (~85%) |
| 93.5% of Slot β over-fires on dor_shkedi (under-covered) | HIGH (~95% — direct measurement) |
| Roy_D is generalization not memorization | MEDIUM-HIGH (~70%, pending GCS audit) |
| anchor_aware mechanism is not purely data-substitute | MEDIUM (~55%) |
| Path A is the cleanest deployment-grade move available | MEDIUM-HIGH (~70%) |
| Per-IQ-quartile router will outperform single-ckpt deployment | MEDIUM (~50%) |
| Targeted dor_shkedi-pocket ingestion will close the loop | MEDIUM (~45%) — depends on data availability |

## §6 — Self-correction log (per AGENT_GUIDE Rule 5)

**Framings I held earlier in this session that this analysis updates:**

1. Earlier in this conversation I proposed three Tier-1 CPU jobs (Slot β per-identity decomp, tiebreak audit, Rule-6 AUC measurement). Those three are the most-load-bearing of the 10 ultimately run. Jobs H (pocket spec verification) and G (anchor_aware × bimodal cross-tab) were ADDED to this packet and provided structurally new information that the original 3 would have missed. The user's request to expand was correct — the original 3 alone would have produced a recommendation that didn't address the §4.2 spec's refutation.

2. My earlier opinion said "pocket-targeted data ingestion is structurally live, distinct from refuted bucket rebalance" with MEDIUM-HIGH confidence (~65%). Job H weakens this: the §4.2 spec as-written is refuted; pocket-targeted ingestion is still structurally live but the spec must be re-derived per-identity. Confidence drops to MEDIUM (~45%) until the per-identity spec is verified to have ≥500 candidate frames in GCS.

3. My earlier opinion ranked the agent's "Slot β decomp" recommendation as "good but incomplete." Job A actually exposes a SHARPER conclusion than the cross-tab I proposed: 93.5% of Slot β over-fires on a SINGLE under-covered identity (`dor_shkedi`), not "mixed under/over." The recommendation that "cross-tab against the bimodal partition" matters was correct, but the result was monolithic, not mixed. Updates the planning surface.

**Framings explicitly NOT updated by this analysis:**

- The forgery-signal-is-in-CLIP-but-shortcut-rotates-FT framing from `RESEARCH_HANDOFF_DATA_DEEP_DIVE_2026-05-19.md` §6.1–6.2 is consistent with Job C's AUC drop pattern.
- The chronic-6 partition is the unit of failure analysis — consistent with Job A, Job E, Job F all showing per-identity FPR concentration.
