# Job H ADDENDUM — Documentation cross-check (2026-05-21)

> Append-only addendum to `JOB_H_DEPLOYMENT_PARETO_FACTS_2026-05-21.md` after cross-checking the wiki + memory + today-dated analysis dirs. **Net effect on the deployment recommendation: same ckpt + τ; three operational details added; one independent confirmation surfaced.**

## 1. What documentation review surfaced

Cross-checked the deployment recommendation against:
- Memory `feedback_per_mode_tau_not_deployable.md` (2026-05-04)
- Memory `project_canary_below_production_resolution_2026-05-08.md` (2026-05-08)
- `analysis/iq_substrate_tau_2026-05-21/FACTS_2026-05-21.md` (today)
- `analysis/iq_substrate_tau_2026-05-21/slot_a_v2_lockbox_pareto.md` (today, frame-level analog)
- `analysis/codec_restoration_gates_2026-05-21/VERDICT_FACTS_2026-05-21.md` (today, codec triple verdict)
- `OPEN_LOOPS.md` — `deployment-vs-p8a-substrate-tradeoff-not-quantified` close criterion (HIGH)
- `OPEN_LOOPS.md` — `webcam-mode-fpr-dominance-headline-misleading` close criterion (medium)

## 2. Three updates to the deployment configuration

### 2.1 Add a quality gate to the operational policy

Per memory `project_canary_below_production_resolution_2026-05-08`:

> User's deployment policy: *"we can probably afford in production to simply ignore that level of pixelation and just reject algorithm analysis until we get a better resolution."*

The user's deployment policy is **single global τ PLUS a quality gate** that abstains (rather than scores) on bad inputs. My original FACTS section 5 only specified τ; the quality gate is the implicit second half of the policy.

Recommended quality gate parameters from the same memory:
- `face_min_dim ≥ 150-200 px`  → gates out PC_Generator__s22 (min_dim ~90), PC_Generator__s45 (~90), Q__s6 (~90). These 3 chronic-FP identities effectively leave the deployment surface.
- Optional Laplacian variance threshold for additional sharpness gating.
- Standard `no_face` gate (already in production per existing pipeline).

**With the quality gate applied**, the per-identity FPR table from FACTS §3.6 should be reread:
- PC_Generator chronic FPs: **GATED OUT** at deployment (min_dim 90 < 150)
- Q chronic FPs: **GATED OUT** at deployment (min_dim 90 < 150)
- Roy_D (min_dim 140-394): **NOT GATEABLE** — production-typical resolution. The Roy_D regression remains a real deployment cost.
- bla_bla_chow (production-typical resolution): **NOT GATEABLE** — the small bla_bla_chow regression remains.
- Chikara_Takahashi (lockbox cohort, fixed by anchor): unaffected.

This shrinks the deployment-relevant chronic-FP risk surface from 6 identities to 2 (Roy_D + bla_bla_chow). The recommendation is still Slot A v2 @ τ ≈ 0.535, but its operational cost is the Roy_D + bla_bla_chow regression cost, not the full per-identity table.

### 2.2 Cite the apples-to-apples per-mode τ refutation as the rationale for naive global τ

`analysis/iq_substrate_tau_2026-05-21/FACTS_2026-05-21.md` headline:

> Refuted as proposed... at any matched lockbox-FPR operating point, **per-mode τ (whether substrate=clip_capture_mode or substrate=IQ-feature-cluster) is WORSE OR EQUAL to a naive global τ** calibrated directly on lockbox. The May 5 "24.5pp recall lift" was an apples-to-oranges comparison.

My Option A recommendation uses naive global τ — exactly the policy this analysis confirms is optimal under fair comparison. The recommendation is independently endorsed by today's per-mode τ refutation: no offline τ-policy beats the naive global τ I recommended.

### 2.3 The contract-compliant Option B benefits from the quality gate

Option B (τ ≈ 0.780, lockbox_fpr=2%) reported `dev_real_fpr = 6.9%` (clears contract gate ≤ 7%). With the quality gate applied, some of the chronic-6 contribution to dev_real_fpr is removed — actual dev_real_fpr after gating is likely 5-6% (subject to confirmation by re-running the contract scorer with the gate active). Option B becomes more comfortable inside the contract.

This is a soft update — the exact post-gate FPR is not measured in this batch. The directional claim is solid.

## 3. Independent confirmation of the ckpt choice

`analysis/codec_restoration_gates_2026-05-21/VERDICT_FACTS_2026-05-21.md` (overnight 2026-05-21 verdict):

> **Slot A v2 step3500 remains the best deployment ckpt**; none of tonight's packets are upgrades.

The codec restoration triple packet (Slot 1, Slot 2, Slot 3 — three different (codec_dose × anchor) combinations FT'd from Slot A v2 / T5C) ran overnight 2026-05-21. None of the three closed the natural-experiment Δ to the deployment-grade criterion (|Δ| ≤ 0.05); all three regressed may6 production-drift FPR vs the Slot A v2 base. The verdict explicitly identifies Slot A v2 step3500 as the deployment ckpt.

This is independent of my Job H analysis — the codec verdict was authored by a different agent before this addendum, using different metrics (single-frame natural experiment + may6 92-frame retest). Two independent paths converge on the same ckpt selection. **No "wait a few days for a better ckpt" optionality exists** — current ckpts are the menu.

Additional mechanism finding from the codec verdict (§2.2): codec aug closes the natural-experiment Δ by uplifting Guest's score (transport-shifted real) toward Roy_D's level, NOT by lowering Roy_D toward Guest. The encoder loses the real-vs-fake discrimination on the transport-shifted population — both Roy_D and Guest score in [0.71, 0.83] after codec training, both false-flagged at τ ≥ 0.70. This reinforces that **the Roy_D regression in Slot A v2 is structurally bounded by the current training data and lever set** — codec was the strongest available transport-axis training-side intervention, and it didn't reach the deployment-grade closure.

## 4. Updated deployment options (final)

### Option A (recommended) — Production-grade ship

**Slot A v2 step3500 @ τ = 0.535 + quality gate (face_min_dim ≥ 150 + no_face)**

Numbers from the Pareto + the gate:
- lockbox_fake_recall = 0.854 (unaffected by gate — lockbox fakes are all production-typical resolution)
- lockbox_real_fpr = 0.100 (slight reduction from gating chronic-6 tiny crops; pre-gate 0.100)
- dev_real_fpr (post-gate, estimate) ≈ 0.10 (pre-gate 0.125 — PC_Generator__s22/s45 + Q__s6 removed)
- dev_fake_macro (post-gate) ≈ 0.76 (unaffected)
- viso_recall = 0.529
- deeplive_recall = 0.928
- may6 fired = 3/92 (Slot A v2 may6 cohort is at production-typical resolution — gate doesn't affect)
- **Roy_D dev FPR** = 0.962 (NOT gateable — production-typical resolution)
- bla_bla_chow dev FPR = 0.300 (NOT gateable)

### Option B (contract-compliant) — Conservative ship

**Slot A v2 step3500 @ τ = 0.780 + quality gate**

Numbers:
- lockbox_fake_recall = 0.696 (vs P8A 0.403 at same FPR — still +29pp lift)
- lockbox_real_fpr = 0.020
- dev_real_fpr (post-gate, estimate) ≈ 0.05-0.06 (well inside contract's 7% target)
- viso = 0.185, deeplive = 0.572
- may6 = 1/92
- Roy_D dev FPR = 0.831 (still bad)

### Option C (NEW — Conservative with reduced Roy_D cost)

**Slot A v2 step3500 @ τ = 0.870 + quality gate** (lockbox-cal between 1.0% and 2.5% per the frame-level iq_substrate_tau analysis)

Numbers (frame-level, from `analysis/iq_substrate_tau_2026-05-21/slot_a_v2_lockbox_pareto.md`):
- lockbox_fake_recall = 0.65 (still +33pp over P8A's 0.33 at same FPR)
- lockbox_real_fpr = 0.010
- Roy_D dev FPR likely 0.65-0.75 (extrapolated from per-identity trajectory)
- Lower fake recall but more Roy_D headroom

Use Option C if Roy_D operational cost is a hard constraint AND a lower-recall ship is acceptable.

### Status of the three options

**None of A / B / C is currently designated as the recommended ship.** They are documented operating modes parameterized by τ (a runtime configuration knob). The mode choice is deferred to a product/SLA decision (the FP/FN cost ratio λ — see §3 of `AGENT_PROPOSAL_2026-05-21.md` for the λ-to-mode mapping). τ can be changed post-deploy without retraining; the choice is reversible.

## 5. Open loops touched by this addendum

- `deployment-vs-p8a-substrate-tradeoff-not-quantified` (HIGH, since 2026-05-06): close criterion (a) "the populations on which P8A would have lower real-side FPR than the currently-deployed E2B at matched τ" — **MET** by Job H §3.3 (dev_fpr column) + §3.5 (may6 column). Close criterion (b) "the populations on which E2B would have higher fake recall than P8A at matched τ" — **MET** by Job H §3.1 (E2B 95.3% vs P8A 77.9% at lockbox_fpr=10%). Close criterion (c) "the disposition explicitly chooses the deployment model with the trade-off documented" — **PARTIALLY MET**: the ckpt is chosen (Slot A v2 step3500); the τ mode (A / B / C) is documented as a deferred deployment-config decision rather than a fixed choice. Mark resolved when a mode is selected at deploy time.

- `webcam-mode-fpr-dominance-headline-misleading` (medium): partially addressed — the quality gate proposed here is one half of the close criterion ("v2-filtered lockbox FPR reported as first-class"). Remains open pending contract-yaml change.

- `lockbox-real-fpr-tiebreak-is-load-bearing` (high, from RESCHAIN_GRL6 retro): RESOLVED 2026-05-21 — Job B paired bootstrap CI shows the 0.07pp gap is inside sampling noise (95% CI on Δ covers 0; P=0.519). The tiebreak is not load-bearing. Status: RESOLVABLE.

## 6. What the addendum does NOT change

- The ckpt choice is unchanged: **Slot A v2 step3500**.
- The recommended τ is unchanged: **τ ≈ 0.535** (Option A) or **τ ≈ 0.780** (Option B) or **τ ≈ 0.870** (Option C, new).
- The Roy_D regression risk is unchanged — quality gate does not help Roy_D.
- The Visomaster recall advantage (4.7× E2B) is unchanged.
- The 3 user-gated questions in AGENT_PROPOSAL §3 remain user-gated (Roy_D generalization, Teams-account flip SLA, FP/FN cost ratio λ).

## 7. References

- Memory: `feedback_per_mode_tau_not_deployable.md`, `project_canary_below_production_resolution_2026-05-08.md`, `project_lockbox_fpr_dominated_by_webcam_mode.md`
- Today's analyses: `analysis/iq_substrate_tau_2026-05-21/`, `analysis/codec_restoration_gates_2026-05-21/`
- Open loops: `threads/processing_signature_shortcut.md`, `threads/webcam_fpr_dominance.md`, `threads/calibration_vs_training_aug.md`
- Companion docs: `JOB_H_DEPLOYMENT_PARETO_FACTS_2026-05-21.md`, `AGENT_PROPOSAL_2026-05-21.md`
