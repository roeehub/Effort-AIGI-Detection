# Thread: Processing-signature shortcut

## The question

Has the detector learned to classify the *capture/encoding/pipeline signature* attached to a video rather than the *face-artifact content* of the frame? If yes, deployment is structurally blocked: two sessions of the same person on two different webcams can sit on opposite sides of the decision boundary without any identity or manipulation change.

## Initial belief

Through [RLP3](../packets/RLP3.md) the going assumption was that residual OOD failures were lighting- or stress-driven. The packet's A3/A3b lanes (deterministic lighting + spatial stress) framed the problem as an invariance / robustness gap. Slot-07 in RLP3 (a 0.40-scale lighting probe) was where the lighting-aug reading started to look like a symptom of something larger — but the framing stayed "augmentation and training distribution are the levers" through [RLP3.5](../packets/RLP3_5.md) and [RLP4](../packets/RLP4.md).

## What changed our mind

- [RLP5](../packets/RLP5.md) slot-07 A/B on the Dor annotated manifest. On `dor_shkedi` (1138 lockbox fakes) the checkpoint outputs `mean prob_fake = 0.457` with 14.6% > 0.9; on `real_dor` (109 frames of the *same face*, different `identity_key`) it outputs `mean = 0.038` with 0% > 0.9. Same person, different pipeline, flipped output. Codified in memory `project_signature_shortcut_finding.md`.
- [RLP6](../packets/RLP6.md) slot-04 post-deployment 2-camera controlled test (`R13_RELAUNCH_PACKET7_CAMERA_SIGNATURE_HANDOFF_2026-04-24.md:5-19`): Dor laptop no-VB → 0.02; Dor webcam same scene no-VB → 0.94; Roee Mac webcam with VB → 0.90; Roee Windows laptop → 0.01. Identity held fixed, camera/OS swap flipped the score. Undeniable.
- [WS-P0](../packets/WS_probes.md) post-fix re-score (`analysis/rlp6_04_postfix_rescore_2026-04-24.summary.json`). Anchor pool `dor-real-webcam-false-flag-no-virtual-bg` moved only −0.008 (0.940 → 0.932) after the `INTER_LINEAR` fix. Preprocessing drift ruled out as the cause; `decision_tree.verdict = "fully_structural__launch_full_subset"`. The shortcut is a *learned representation*, not an inference-path artifact.
- Fingerprint diagnostics (`analysis/dor_baseline_repro_2026-04-24.summary.json`, `dor_pool_fingerprints_2026-04-24.summary.json`) ranked `dct_hf_ratio` (max_sep 3.46), `wb_rb_ratio` (2.32), `mean_cb` (2.23), `mean_cr` (1.97) as the most consistent pool-separating signals — two codec-aligned, two chroma/lighting. Camera/ISP signature was made numerically legible.

### Slice 5 evidence — P8A breaks the anchor ceiling (2026-04-29)

- **2026-04-25 P8A** (run `9lmvb5b4`, `analysis/pool_rescore_rlp8_a.summary.json`) is the first run to break the P7 anchor ceiling at single-variable cost. Anchor pool `dor-real-webcam-false-flag-no-virtual-bg` Δ=**−0.188** vs RLP6_04 (best P7 was −0.097); Roee-mac Δ=**−0.399** (best P7 was −0.228). All three real-correct pools also moved closer to zero (no FPR regression). Single-variable delta from RLP7_02: `unfreeze_final_proj=true`, `unfreeze_final_ln=true`, `apply_svd_to_mlp=true`. See [P8A retro](../packets/P8A.md) and memory `project_p8a_breakthrough.md`.
- **The shortcut was reach-limited, not data-limited at this stage.** RLP6_04 → RLP7 chain had locked frozen CLIP backbone weights, meaning only head + SVD residuals on attention in_proj/out_proj could train. Unfreezing visual.proj (768→512 final projection) + visual.ln_post + wiring SVD residual onto MLP c_fc/c_proj layers gave FT enough reach to break the shortcut's grip. 13/30 anchor frames still flip (frac_gt_0_9 = 0.43) so the shortcut isn't eliminated — but it's no longer ceiling-bound. Anchor pool is now bimodal (mean 0.74, std 0.27) instead of flat-ceilinged.
- **P8B refuted "the shortcut is in the weight chain"** — scratch on plain CLIP-DataComp-XL performed *worse* than RLP6_04 baseline (anchor Δ=+0.066 at step 11000 / 30k). Real pools also regressed (Dor-correct whiteish 0.013 → 0.141). Confirms the shortcut lives in the **data mix**, not in R12g/R13 weights — but the CLIP-DataComp-XL prior preserved by FT acts as a regularizer scratch lacks. **Implication**: data-side intervention is the only path to *eliminate* the shortcut, but P8A's recipe goes a long way without one. P8B hung at step 12000 on `teams_ood_fake` data loader; cancelled at 03:13 UTC 2026-04-25 (memory `feedback_no_cancelling_vertex_jobs.md`).
- **The breakthrough is not free**: P8A regressed −13.6 pp aggregate fake recall on `teams_fake_all_dev` at default τ. 329 P8A fake-side videos that RLP6_04 caught are missed; **zero compensating gains** in the other direction. ~80% of misses sit at scores 0.10–0.40 (with a substantial cluster <0.10), only ~19% in the τ-recoverable [0.40, 0.50) band. **Implication**: lockbox τ-tuning will recover at most ~3.8 pp of the 13.6 pp gap; the remaining loss is **separability collapse, not threshold drift**. Visomaster regression concentrates on `_teams` sub-pool (1.5× harder than `_raw`), the deployment-distribution slice. Per-method per-checkpoint table in [P8A retro](../packets/P8A.md) and `analysis/p8a_fake_failure_analysis_2026-04-25/`.
- **Anchor-pool monitor lands** (commit `7af72b1`, 2026-04-26). `trainer/trainer.py` + `trainer/mixins/checkpointing.py` add an in-trainer anchor-spread monitor that logs `anchor/composite` every step-validation cycle, plus a checkpoint-time gate. Designed to catch any future "shortcut re-formation during training" pattern at runtime, comparing anchor metrics against an expected curve. The monitor is general — not specific to a particular checkpoint or recipe — so all subsequent packets get this hygiene by default.
- **In_proj-SVD silent zero-gradient bug discovered + fixed** (commits `2feea58` + `7af72b1`, 2026-04-26). Every R12g / RLP / P-* run with `apply_svd_to_in_proj: true` trained the q/k/v residuals on regularizer gradient only — the classification loss never propagated through the broken `forward_pre_hook` + `.data.copy_` pattern. Implication for this thread: the "FT-only is structurally bounded" reading from P7 / RLP7_08 was measured **with broken in_proj-SVD**. After the fix, the FT lever set is meaningfully larger (~3M trainable params per attention block × 12 blocks that weren't training). The shortcut-is-upstream evidence (RLP7_08 fork-point control) is unaffected; the "ceiling" claim itself needs re-validation post-fix. The Phase C overnight slate (`april-26-training-master-plan-v2.LOG.md:206-273`) was the first attempt at re-validation: C.1 (with fix) vs C-ablation (`apply_svd_to_in_proj: false`) tied within noise on the canonical P10_SYM-on-P8A recipe. The lever exists but is not exercised by this recipe. See [`in_proj_svd_gradient_bug`](in_proj_svd_gradient_bug.md).
- **2026-04-26 Phase C codec_hedge gives the strongest single-variable lift inside the P10_SYM family** (`april-26-training-master-plan-v2.LOG.md:339-343`). C.3 (`teams_codec_sim_p: 0.60`, `teams_codec_sim_quality: [15, 55]`) shows best_anchor/composite **+30%** vs canonical C.1 with similar value/ood composite and similar heldout_ood AUC. Surfaced as the highest-priority A.2-style validation target; codec aug intensity is the live lever as of end-of-Slice-5. **Note** — this is the trainer-side `anchor/composite` from the new monitor, not the lockbox-anchored crowning protocol.

### Slice 6 evidence — codec_hedge fails the lockbox readout; P11 portfolio launched; face-size leak surfaces as third axis (2026-04-29)

- **2026-04-27 09:50 UTC — codec_hedge A.2-style validation: NOT a Phase D promotion candidate** (`april-26-training-master-plan-v2.LOG.md:390-422`). The +30% trainer-side anchor composite from Slice 5's Phase C does not survive lockbox-anchored evaluation. At τ=0.5 diagnostic: P8A reference dominates BOTH lockbox axes vs all 3 C-slate checkpoints (lockbox real FPR 6.2% vs 13.3-17.7%; lockbox fake recall 65.2% vs 41.9-61.7%). At calibrated τ ≈ 0.97-0.99 all 4 checkpoints fired the contract-policy bug regime. Verdict: codec aug intensity moved the trainer-side metric but not the deployment-side readout. The "live lever" framing in Slice-5's codec_hedge surfacing was correct on a trainer-side anchor but did not translate.
- **2026-04-27 14:55 UTC — Source-bucket linear probe says P8A is NOT shortcut-clean** (`april-26-training-master-plan-v2.LOG.md:441-449`). 10-class source-bucket probe (chance=0.10, plan §6 PASS gate ≤ 0.25):
  - RLP6_04_BASELINE_STEP23500 (control): 0.97 test_acc — known-shortcut model saturates the probe.
  - P8A_REFERENCE_STEP5000: 0.461 test_acc — 4.6× chance, FAIL.
  - C3_CODEC_HEDGE_VC_STEP2000: 0.416 test_acc — 4.2× chance, FAIL.
  Implication: P8A's 65% τ=0.5 lockbox fake recall is partially shortcut-attributable. Codec aug pulled some source-pipeline signal out (~9% relative reduction P8A → C3) but didn't clear the ≤0.25 PASS gate. The probe gradient (P8A 0.46 vs C3 0.42) shows codec aug as a **partial** lever.
- **2026-04-27 21:30 CEST — Crop-tightness shortcut empirically confirmed** (`april-26-training-master-plan-v2.LOG.md:750-817`). 47-frame × 5-tightness sweep: 53% of frames flip predicted label across the tightness range. FAIL regime sits in a "FAKE valley" at native crop; any perturbation drops FPR ~30-40pp. Per-FAIL-tag breakdown is uneven (2 of 3 FAIL tags are crop-shortcut artifacts; the third uses webcam-specific signature). This is the **third shortcut axis** alongside camera/ISP signature and capture-mode tail. See [`face_size_label_leak`](face_size_label_leak.md).
- **2026-04-27 22:40 CEST — Face-pixel-area training data leak measured** (`april-26-training-master-plan-v2.LOG.md:825`). Per-method face-size signatures: deeplive_enhanced clusters at 22.6-24.6k px² (single-point distribution); teams_capture methods range 14k-154k. Cohen's d ≈ 0.37 between dev_real / dev_fake. The camera-signature shortcut is **also** a face-size leak from a different axis: each method's frames come from a fixed pipeline that crops at fixed tightness; same shortcut viewed via the geometric crop tightness rather than the camera/ISP signal. Empirical confirmation: the crop-sweep findings show the same person flips real ↔ fake under tightness perturbation alone. Memory `project_face_size_label_leak.md`. See [`face_size_label_leak`](face_size_label_leak.md).
- **2026-04-27 23:13 CEST — P11 4-run portfolio launches with face-size + codec axis interventions** (`april-26-training-master-plan-v2.LOG.md:1033-1086`). MILD (`context_variation_scale=0.30 + real_codec_uplift + teams_codec_sim_p=0.65`), HEAVY (same + scale=0.50), HEAVY_DEEPLIVE (HEAVY + deeplive_enhanced 6×), WEBCAM_HARDEN (MILD + new `webcam_harden_p=0.30` aug primitive). Smoke test 200 steps at 22:14 CEST passed (val_holdout AUC 0.9906). Overnight launch in 3 distinct US regions (`us-east1`, `us-west4`, `us-central1`).
- **2026-04-28 09:00 — P11 morning verdict: HEAVY recipe direction confirmed at trainer-side; checkpoint policy artifact distorts deployment readout** (`april-26-training-master-plan-v2.LOG.md:1156-1212`). MILD / HEAVY / HEAVY_DEEPLIVE all SUCCEEDED; WEBCAM_HARDEN FAILED at step 5683 (`RuntimeError: NaN/Inf loss detected` — webcam_harden aug primitive compounded numerical instability; dropped from next-wave). HEAVY_DEEPLIVE deeplive recall regressed to 75.76% with 6× weight (counter-intuitive; aggressive sampling broke pos/neg pair structure for contrastive negatives; drop the 6× weight). HEAVY's anchor_mean trajectory continuously improved through step 6000 (0.392 final) — distinguishes it from MILD / HEAVY_DEEPLIVE which peak at step 2000 then oscillate. The trainer's AUC-ranked save policy froze at step 1000 = the WORST anchor moment for all 3 SUCCEEDED runs, so the saved-checkpoint readout is a structurally adverse readout.
- **2026-04-28 11:53 — P11 inference verdict β: HEAVY beats P8A by +16/+25/+9 pp recall on viso/deeplive/teams_fake at τ=0.5, at the cost of 4× v2 FPR** (`april-26-training-master-plan-v2.LOG.md:1275-1277`). Recipe direction confirmed (HEAVY > MILD > HEAVY_DEEPLIVE > WEBCAM_HARDEN); the trade-off is structural (anchor-worst step 1000 artifact predicted by the trajectory analysis). Plan v3 §6 verdict: β/γ ASSESS — none ship. The β/γ verdict is what motivates Plan v4 (anchor-aware loss + viso 5× + screen-capture aug + working periodic_saves). See [P11 retro](../packets/P11.md).
- **2026-04-28 12:25 — P12_HEAVY_LONG dud + periodic_saves silent failure** (`april-26-training-master-plan-v2.LOG.md:1278-1310`). P12_HEAVY_LONG was supposed to test "let HEAVY recipe run to step 8000 with periodic checkpoints saving every 1000 steps." Two compounding problems: (a) early stopping fired at step 6000 (auc patience metric saturated); (b) `periodic_saves` patch silently failed (no `periodic_*.pth` files saved despite yaml + trainer code being in place). **P12 produced no new information** vs P11_HEAVY step 1000. The anchor recovery rate from W&B history (~0.04 per 1000 steps from step 1500 onward) implies pure-FT-extension of HEAVY to reach P8A's anchor floor would take ~17,000 more steps from step 6000 — far beyond a single run. The "anchor recovers by step 3000" hypothesis from P11 was wrong.
- **2026-04-28 13:30 — Option A (inference-time stack) verdict γ: substrate confirmed bounded** (`april-26-training-master-plan-v2.LOG.md:1337-1383`). Subagent ran 85 configurations (30 single-candidate × 3 calibrations + 45 noisy-OR + 10 weighted-noisy-OR) on existing P8A + 4 P11 predictions. All 85 failed the 90/5 gate. Best ensemble: `p8a_reference + p11_heavy_step1000` isotonic-noisy-OR, τ=0.846 → viso 31.3%, deeplive 58.2%, teams_fake_all 75.0%, modern_v2 FPR 4.98%, teams_real_all_dev FPR 10.9% (fails 7% cap). **AUC(visomaster fake vs modern_v2 real) caps at ~0.65 across all candidates and ensembles** (best ensemble AUC = 0.647). Calibration moves probabilities, not AUC; ensembles of same-deficit candidates can't break the substrate ceiling. Plan v4 authored: G — substrate redesign (P13_ANCHOR_AWARE), H — data-axis investigation (no GPU), I — honest report + scratch-retrain proposal.
- **2026-04-28 — P13 anti-shortcut interventions yaml drafted** (commit `cab2909`). Three new training-time levers stacked: (1) **anchor-aware loss** — penalty on `anchor_pool_mean_prob > 0.10` with weight 5.0 + 16 samples per step (the loss-side answer to "the model should never confidently misclassify the anchor pool"); (2) **pipeline-random aug** — symmetric chain of jpeg/downscale/chroma_blur/yuv_roundtrip/gamma stochastic perturbations applied to both real and fake at training time (the data-side answer to "real and fake should not be distinguishable by capture pipeline"); (3) **face-scale jitter** — `scale_limit: 0.25` symmetric crop variation (the geometry-side answer to the face-pixel-area leak). All three blocks were silently DISABLED on the first P13 launch by the wandb-flattening bug; the post-fix run is Slice 7's verdict. See [`wandb_flattening`](wandb_flattening.md), [`face_size_label_leak`](face_size_label_leak.md), and [P13 retro](../packets/P13.md). **Build-only this slice; verdict is Slice 7.**

### Slice 7 evidence — frame-level AUC reframing + P13 γ verdict + bucket-gap layer (2026-04-29)

This subsection consolidates Slice-7 evidence: the slice where the **headline-metric framing materially shifts**. Three independent findings converge:

#### Frame-level AUC reframing (load-bearing)

- **2026-04-29 — Frame-level AUCs from cached P8A predictions reframe "low recall" as scorer artifact** (memory `project_p8a_frame_level_auc_2026-04-29.md`). Computed directly from cached P8A_step5000 predictions on the dev eval suites, no τ involved:

  | Suite | Frame-level AUC |
  |---|---:|
  | visomaster_enhanced_macro_dev | **0.7527** |
  | deeplive_enhanced_dev | **0.8614** |
  | teams_fake_all_dev | **0.9106** |

  These numbers are *dispositive evidence* that the contract scorer's "viso recall = 1.1%" pre-fix reports were a measurement artifact (the FPR-minimization no-budget τ-policy collapsing onto the score-distribution tail), not a model failure. Under the corrected policy with `target_fake_recall_min=0.30` (the v3 fix in working tree, see [`contract_policy_bug`](contract_policy_bug.md)), P8A reports viso recall **13.6%** / deeplive **23.9%** / teams_fake **52.6%** — consistent with these AUCs and with the (1 − AUC) margin inversion.

- **The narrative shift this enables.** Through Slices 4-6 the dominant framing was "the model has learned a shortcut and consequently underperforms on cross-domain fakes." The frame-level AUC evidence reframes this as: **the model has substantial cross-domain signal (especially on deeplive: AUC 0.86; and teams_fake: AUC 0.91), but the contract scorecard was reading near-zero recall because the τ-policy was operating in a regime where the model's confident-fake region sat below the selected threshold.** The shortcut is real (Axis 3 Δ ~0.5 on best P13), but it was not the proximate cause of the headline-metric failure that motivated three packets of redesign. **The model is meaningfully less broken than headline metrics suggested** — though still not deployment-ready, because (a) the corrected viso 13.6% is well below the 90% gate, (b) the shortcut residual at Axis 3 Δ = 0.52 still triggers the deployment block, and (c) the modern_v2 FPR explodes on P13 from-scratch.

- **The structural ceiling is bounded by the AUCs.** Layer-2 (bucket-gap closure, see [`viso_bucket_gap`](viso_bucket_gap.md)) plausibly lifts viso recall to ≈ deeplive's substrate-matched ~24% under the corrected contract policy, **not** to the 90% headline target. The gap from 24% to 90% is the residual that the shortcut + architectural-class interventions (P15 GRL, future representation-loss work) need to close. Memory `project_p8a_frame_level_auc_2026-04-29.md` codifies the operational rule: *"Always sanity-check operating-point recall against frame-level AUC before concluding a checkpoint is bad. If contract recall ≪ AUC-implied recall, suspect the τ-policy first."*

#### P13_FROM_SCRATCH γ verdict — interventions are directionally right but cross-domain capability collapsed

- **2026-04-29 02:30 UTC — P13 Day-4 verdict γ** (`docs/relaunch_handoffs/P13_DAY4_VERDICT_2026-04-29.md`; scorecard job `978674200371789824` us-west4 `JOB_STATE_SUCCEEDED` 2026-04-29 ~02:14 UTC). All 9 P13 candidates fail Axis 1 by ≥15pp on at least one component; Axis 3 Δ exceeds the γ-trigger 0.30 for every candidate (range 0.52-0.71 vs gate 0.15). **The from-scratch + anti-shortcut interventions did NOT break the camera-signature shortcut.** Best Axis 3 = step 18000 = 0.520, IS measurably better than P8A (0.659) and RLP6_04 (0.830) — directional progress, but still 3.5× the gate.
- **From-scratch made cross-domain fake detection materially worse.** At any τ-policy (contract, 5%-FPR-calibrated, or τ=0.5), P13 trails P8A and RLP6_04 by 30-60pp on viso / deeplive / teams_fake_dev recall. At τ=0.5 P13_step18000 has lockbox fake recall **88.5%** (best of all candidates, in-domain training distribution learned cleanly) but cross-domain recall craters: viso 5.5%, deeplive 48.8%, modern_v2 FPR explodes to 30.2% (P13 hallucinates "fake" on a third of webcam-style modern_v2 real frames). See `docs/relaunch_handoffs/P13_DAY4_VERDICT_2026-04-29.md`.
- **The γ verdict is robust to τ choice.** Two independent re-runs (calibrated 5%-FPR τ on `teams_real_all_dev`; τ=0.5 diagnostic) both confirm P13 cross-domain fake recall is 5-10× lower than baselines. The model is fundamentally not calibrated to cross-domain target distributions — a from-scratch consequence, not a τ-tail artifact.
- **Three of three anti-shortcut interventions lived this time** (memory `project_wandb_flattens_nested_dicts.md` 2026-04-28 fix `c366026`): anchor-aware loss, pipeline-random aug, face-scale jitter all logged ENABLED at trainer init for the post-fix P13 launch. The verdict reflects the recipe's true behavior, not a silent-DISABLED artifact. **This is the first packet where all three Plan-v4 substrate-redesign levers actually fired.**
- **Axis 3 directional progress is real and small.** P13_step18000 Δ=0.520 vs P8A 0.659 = ~14pp improvement on the same-face-different-pipeline probe; the late-training ckpts converge on this metric (steps 12000/15000/18000 all sit 0.52-0.64 vs the early ckpts at 0.57-0.71). The interventions weakened the shortcut by ~14pp but were not strong enough to break it on their own; combining them with cross-domain capability (FT-from-P8A path) is the recommended Day-5 move (`R13_P14_FT_FROM_P8A.yaml`, currently in flight).

#### Source-bucket / shortcut layer untouched by the bucket-gap finding

- **The bucket-gap finding is layered on top of the shortcut, not a replacement** (see [`viso_bucket_gap`](viso_bucket_gap.md), `docs/relaunch_handoffs/TRAINING_EVAL_VISO_BUCKET_GAP_2026-04-29.md`). The eval suite `visomaster_enhanced_macro_dev` reads from a Teams-recapture bucket whose distribution is not in training; that explains the magnitude of the headline-metric failure under the buggy contract policy. But **Axis 3 Δ ~0.5 on every candidate** is the shortcut, and that is unchanged by the bucket gap — it is a same-face-different-pipeline probe, not a bucket-distribution probe. So the deployment block survives even when the contract reads correctly and the bucket gap is closed; the shortcut residual still false-flags the camera-signature pool.
- **The narrative reordering for Slice 7**: (1) the contract-policy bug masked how good the model actually was on the cross-domain fake suites (frame-level AUC evidence); (2) the bucket gap explains the residual headline-metric magnitude (Layer 2 closes via yaml-only wiring); (3) the camera-signature shortcut is the residual deployment block at Axis 3, and it requires a structurally-different intervention class than P11/P13's input-space perturbations (P15 GRL is the cleanest available next lever, but its expected gain is much higher *after* the bucket gap is closed). See [`viso_bucket_gap`](viso_bucket_gap.md) and [P15 readiness note](../packets/P15.md).

## Current stance (2026-04-29)

The detector has learned a **camera/ISP processing signature** that is layered on top of a working cross-domain fake-detection capability — not a manipulation-content boundary, but also not a model-of-nothing as the contract scorecard's near-zero recall numbers had implied. The Slice-7 reframing collapses three previously-distinct findings into one consistent picture: (a) the model's frame-level cross-domain AUC is meaningfully high (P8A: viso 0.75 / deeplive 0.86 / teams_fake 0.91); (b) the contract scorer's pre-fix τ-policy was reading near-zero recall as a **scorer artifact**, not a model failure; (c) the camera-signature shortcut is real (Axis 3 Δ ~0.5 on best P13) and is the **residual deployment block** even after the contract-policy fix and bucket-gap closure both land. Direct consequences:

- Lockbox 90/90 on RLP6_04 is **not** threshold-reachable: at τ catching 90% of fakes, real FPR is 63.6%. This is a representation ceiling, not a calibration gap. Trainer's `value_composite` cannot see it (see [promotion_contract_evolution](promotion_contract_evolution.md), [`value_composite_semantics`](value_composite_semantics.md)).
- Training-aug is the dominant remaining lever ([calibration_vs_training_aug](calibration_vs_training_aug.md) — WS-P1 closed only 30.1% of the gap).
- **P8A's CLIP backbone unfreeze (proj + ln_post + MLP-SVD) breaks the P7 anchor ceiling** (Δ=−0.188 vs RLP6_04, ~2× the best P7 result), but at the cost of −13.6 pp aggregate fake recall on `teams_fake_all_dev`. Failure-mode analysis says the regression is separability loss, not threshold drift — lockbox τ-tuning will not rescue it. Implication: the soften axes (P9_01 magnitude, P9_03 topology, P9_05 data) are the right next moves; the trade-off is real and may force a different intervention class entirely (P10 anti-shortcut packet — symmetric router + GRL slate — is drafted as the orthogonal alternative).
- **P8B refuted "the shortcut is in the weight chain"** — scratch on plain CLIP performed *worse* than RLP6_04. Combined with P8A: the shortcut is **data-bound**; capacity helps; scratch is wrong. Data-side intervention is the only path to *eliminate* the shortcut (e.g., camera-diversified Teams real captures), but P8A's recipe goes a long way without one.
- Per-identity reducer (WS-P2.b) is the diagnosis primitive: any RLP7+ checkpoint can be sliced to surface "did this variant manufacture a new Dor?" without ad-hoc analysis.
- The "FT-only is structurally bounded" P7 ceiling was measured **with broken in_proj-SVD** (every R12g / RLP / P-* run with `apply_svd_to_in_proj: true` trained the q/k/v residuals on zero classification gradient until commit `2feea58` 2026-04-26). Post-fix, the FT lever set is meaningfully larger; the ceiling has not been re-measured from a clean fork point. See [`in_proj_svd_gradient_bug`](in_proj_svd_gradient_bug.md).
- **The shortcut is now understood as multi-axis, not single-axis.** The Slice-6 evidence converges on three interlocking shortcut axes: (1) camera/ISP signature (Slice-4 origin), (2) capture-mode tail (lockbox webcam mode = 65.7% FPR; see [`webcam_fpr_dominance`](webcam_fpr_dominance.md)), (3) face-pixel-area label leak (per-method face-size single-point distributions; see [`face_size_label_leak`](face_size_label_leak.md)). The crop-tightness sweep (53% prediction-flip rate across t∈[0.7, 1.5]) demonstrates that all three are entangled at the per-frame level — perturbing geometric crop alone flips real ↔ fake on the same person. Training-aug interventions must address all three axes; P11 attacked face-size + codec; P13 stacks anchor-aware loss + pipeline-random aug + face-scale jitter (build-only this slice; verdict Slice 7).
- **Inference-time stack is exhausted as a recipe lever** (Option A subagent verdict 2026-04-28 13:30). 85 calibration + ensemble configurations on existing P8A + 4 P11 predictions all fail the 90/5 gate; AUC(visomaster fake vs modern_v2 real) caps at ~0.65. The substrate is bounded; redesign-class intervention is required (P13 anchor-aware) or scratch-retrain.

### Post-Slice-7 update (2026-04-29 afternoon) — eval-substrate may amplify shortcut signal vs production

A 2026-04-29 morning visual audit of eval-substrate slices (the very-sharp-FP and is_no_face slices from the 2026-04-27 investigation) surfaced a structurally-upstream finding: **eval frames carry more background context around the face than production crops do** (see [`eval_production_crop_tightness_gap`](eval_production_crop_tightness_gap.md)). Direct implication for this thread:

- The camera/ISP-signature shortcut lives in pipeline metadata (codec, color profile, ISP noise, virtual-background objects) that surrounds the face. **If the eval substrate gives those cues a larger surface than production does, the shortcut's measured magnitude on eval is partly an eval-substrate artifact.**
- The `shortcut-deployment-block` (in-progress, critical) loop's close criterion reads `dor-real-webcam-false-flag-no-virtual-bg ≤ 0.30` against the eval substrate. **Under the crop-tightness gap, hitting that target on eval may not translate one-to-one to production**: the eval substrate's looser crop gives more pipeline-signal surface, so a checkpoint that breaks the shortcut on eval might face an easier-or-harder production translation depending on the exact crop-tightness delta.
- This is **not a reason to revise the close criterion silently**. The 2-camera controlled test (2026-04-24, `R13_RELAUNCH_PACKET7_CAMERA_SIGNATURE_HANDOFF_2026-04-24.md:5-19`) was not eval-substrate — it was direct production-side captures (Dor laptop, Dor webcam, Roee Mac, Roee Windows), and the score-flip happened on those. So the shortcut is real on production captures, not just on the eval substrate. The eval-substrate amplification (if real) compounds with the production-side mechanism rather than replacing it. **Do not unilaterally revise the close criterion.** This update logs the open question for the disposition decision tracked by the `shortcut-block-criterion-may-be-scorer-artifact-bound` sub-loop above.
- The audit also refines two adjacent eval-substrate findings (see [`sharpness_metric_bug`](sharpness_metric_bug.md), [`eval_substrate_data_hygiene`](eval_substrate_data_hygiene.md)) that bear on how to read existing per-slice FPR analyses: `sharpness_laplacian` is computed on the full image not the face crop (so the very-sharp-FP slice mixes two populations), and the eval substrate contains 99×110-pixel source crops that pass the existing face-area floor but no production deployment would feed the model.

### 2026-05-04 evening update — `TeamsCodecSimulation` aug calibration empirically verified vs actual transport

The codec_hedge work in Slice 5/6 (Phase C C.3, P11_HEAVY codec axis) used `data/augmentations/teams_simulation.py:104` `TeamsCodecSimulation` calibrated against a documented 18-video × 132-frame sample of measured Teams transport deltas (sharpness −50.9%, brightness +19.0%, HF energy −77.4%, etc.). The Slice-6 codec_hedge lockbox-readout failure (`april-26-training-master-plan-v2.LOG.md:390-422`) was on the lockbox SCORECARD, not on the aug calibration itself. The aug had not been independently verified against actual paired (clean, teams) transport on a different sample.

**On 2026-05-04 evening** (`analysis/codec_aug_verification_2026-05-05/run_aug_vs_actual.py` + `FINDINGS.md`), 30 paired (raw, teams) viso frames where the same source frame exists in both clean and teams-transported buckets were processed:
- Applied `TeamsCodecSimulation` to the raw frames
- Computed 8 IQ feature deltas (laplacian, sobel_edge, luma_mean, luma_std, saturation, contrast, hf_energy)
- Compared to the actual transport deltas measured between the same source frame's clean and teams versions

**Result**:
- Cosine similarity (aug delta direction vs actual transport delta direction): **0.87 - 0.99** across all 8 features
- Magnitude ratio (aug delta magnitude / actual transport magnitude): **97% - 106%**

**Interpretation**: the aug class is a faithful simulation of actual Teams transport on the IQ axes the original calibration targeted. The Slice-6 finding that codec_hedge did NOT promote is not invalidated — that finding was about the lockbox-readout effect of training with codec aug at higher probability/quality range, NOT about whether the aug class faithfully models transport. The two findings are independent.

**Implication for the in-flight Packet C-codec** (`R13_PC_CODEC_PLUS_DATA.yaml`, run `1202657157175050240`, JOB_STATE_RUNNING in us-east1, image 1.3.257): the aug intervention now sits on a stronger empirical foundation than codec_hedge had — calibration is independently re-verified on a different sample. Whether THIS recipe (E2B base + visomaster_enhanced + visomaster_teams_enhanced data + adaptive_mixture codec aug at p=0.5) translates to a contract-grade lift is the open question. ETA ~24h. F4 substrate-cleaning re-eval pipeline ready (`analysis/substrate_cleaning_eval_2026-05-05/`).

## Packet timeline

- [RLP3](../packets/RLP3.md) — slot-07 lighting probe; A3/A3b lanes make lighting/processing axes instrumentable. First hint.
- [RLP3.5](../packets/RLP3_5.md) — slot-07 stack_top3 "don't fire" decision implicitly predicts robustness augmentation is under-powered; foreshadow.
- [RLP4](../packets/RLP4.md) — not a direct contributor (all jobs failed; carry-forward only).
- [RLP5](../packets/RLP5.md) — slot-07 `dor_shkedi` vs `real_dor` flip. The shortcut goes from suspicion to fact. *Note: slot-01 was never probed separately; its E3 recipe is identical to slot-07 at a different seed.*
- [RLP6](../packets/RLP6.md) — slot-04 at `value_composite=0.9006` becomes the 2-camera test victim; promotes the shortcut from lockbox curiosity to deployment blocker.
- [WS probes](../packets/WS_probes.md) — WS-P0 rules out preprocessing as cause; WS-P1 measures calibration's partial lever; WS-P2.b ships the per-identity reducer.
- [RLP7](../packets/RLP7.md) — the structural response (spatial + codec training-aug). Pre-launch gate cleared with verdict `fully_structural__launch_full_subset`; launch pending. P7 hits anchor ceiling at ~−0.10.
- [P8A](../packets/P8A.md) — first run to break the P7 anchor ceiling. Single-variable delta from RLP7_02 (unfreeze visual.proj + visual.ln_post + apply_svd_to_mlp). Anchor Δ=−0.188 on `dor-real-webcam-no-VBG` and Δ=−0.399 on Roee-mac. **Trade**: aggregate fake recall on `teams_fake_all_dev` regressed −13.6 pp; per-method failures concentrated on `_teams` sub-pool of visomaster_enhanced_macro and on deeplive_enhanced. Verdict: shortcut weakened (frac > 0.9 = 0.43, not eliminated), shortcut now data-bound rather than reach-bound.
- [P9](../packets/P9.md) — soften P8A along three single-variable axes (magnitude / topology / data), plus replication and freeze-native control. In-flight at end of Slice 5 (promotion-contract scorecard `2877145196556976128` running). Crowning protocol explicitly excludes `value_composite`; gates on lockbox per-method recall floors.
- [P10](../packets/P10.md) — anti-shortcut packet (symmetric router + GRL slate, commit `2c9778b`). Phase C overnight slate ran the symmetric-router half: C.1 vs C-ablation tied within noise (in_proj-SVD lever not exercised by recipe); C.4b vs C.4a tied within noise (FT-depth crystallization not measurable); C.3 codec_hedge gives best_anchor/composite +30% — codec aug intensity is the live lever as of end of Slice 5.
- [P13](../packets/P13.md) — from-scratch + anti-shortcut interventions (anchor-aware loss, pipeline-random aug, face-scale jitter). Day-4 verdict γ (2026-04-29): all 9 candidates fail, Axis 3 Δ best 0.520 (vs gate 0.15) — directional progress, not breakthrough. Cross-domain capability collapsed (the from-scratch trade); modern_v2 FPR explodes to 30.2% on best ckpt.
- [P14](../packets/P14.md) — currently in flight (`R13_P14_FT_FROM_P8A.yaml`, FT-from-P8A + interventions). Designed to preserve P8A's cross-domain capability while applying the only intervention class that moved Axis 3 in the right direction. Companion `R13_P14_DATA_FIX.yaml` is drafted but not launched (the bucket-fix variant; see [`viso_bucket_gap`](viso_bucket_gap.md)).
- [P15](../packets/P15.md) — GRL readiness note drafted (`docs/relaunch_handoffs/P15_GRL_READINESS_NOTE_2026-04-29.md`). The cleanest-available structurally-different anti-shortcut lever (gradient reversal on a quality-domain classifier head, DANN). Smoke-test required. Decision logic: launch on P14 verdict β/γ; do not launch on P14 verdict α.

## Evidence locations

- `analysis/rlp6_04_postfix_rescore_2026-04-24.summary.json` — the pre-launch gate data; anchor delta −0.008
- `analysis/calibration_probe_2026-04-24.summary.json` — WS-P1 30.1% gap closure
- `analysis/dor_baseline_repro_2026-04-24.summary.json`, `analysis/dor_pool_fingerprints_2026-04-24.summary.json` — fingerprint ranking (V3_combined recommendation)
- `analysis/lockbox_failure_contact_2026-04-24.index.json` — `Cam_Test__s33` fakes at `frame_prob` 0.01–0.05
- `analysis/feature_space_2026-04-23/per_source_summary.csv` — RLP5 slot-07 per-source readouts
- `arena/checkpoint_maps/teams_target_domain.r13_rlp5_07_sanity_dor_2026-04-23.yaml` + sibling — Dor-annotated sanity maps
- Memory: `project_signature_shortcut_finding.md` — decisive summary; `project_promotion_contract.md` — why `value_composite` cannot see it
- Handoffs: `docs/relaunch_handoffs/R13_RELAUNCH_PACKET7_CAMERA_SIGNATURE_HANDOFF_2026-04-24.md:5-19` (2-camera evidence)

## Open loops

- **RLP5 slot-01 was never probed separately.** Slot-01 and slot-07 are seed replicates (seed 737 vs 742) on the same E3 recipe; slot-07 beats slot-01 by +0.0045, inside seed noise. The "breakthrough" slot became the retro-score subject; if slot-01 carries the same shortcut, the E3 lift is even more shortcut-driven than recorded.
- **Seven of nine prior packets have not been re-scored** through the INTER_LINEAR path. Whether any pre-fix leader ranking shuffles materially under the fix is unknown. See [preprocessing_parity_bug](preprocessing_parity_bug.md).
- **FaceDancer blind spot** remains open per convmem mining. No RLP has attacked FaceDancer pipelines directly; whether the shortcut generalizes across manipulation families is untested.
- **Deployment-server preprocessing is untouched.** The original 0.94 Dor false-flag came through `http://34.16.217.28:8999`; WS-P0 fixed the two repo paths but not production. Separate workstream.
- **70% of cross-pool FPR gap remains** after per-camera calibration. What closes it beyond training-aug is unknown.

### Cross-thread refs

- [`promotion_contract_evolution`](promotion_contract_evolution.md) — RLP6_04 with `value_composite=0.9006` is the load-bearing concrete proof that `value_composite` cannot see the camera/ISP failure surface; the shortcut and the contract-policy bug land in Slice 4 in the same week and are interlocking failures.
- [`preprocessing_parity_bug`](preprocessing_parity_bug.md) — WS-P0 ruled the INTER_AREA→INTER_LINEAR drift OUT as the cause of the shortcut (anchor delta −0.008). The shortcut is structural, not preprocessing.
- [`calibration_vs_training_aug`](calibration_vs_training_aug.md) — WS-P1's 30.1% closure on the cross-pool FPR gap places per-camera calibration as a complement, not a cure; the shortcut needs a training-side fix.
- Memory `project_signature_shortcut_finding.md` (Slice 4 origin), `project_shortcut_is_upstream.md` (Slice 5 follow-up — the shortcut is upstream of RLP6_04 itself, not a late-FT consolidation).
- [`viso_bucket_gap`](viso_bucket_gap.md) (Slice 7) — the headline-metric magnitude is dominated by the bucket gap, not the shortcut alone; closing one without the other still leaves the candidate non-deployable. The two threads' open loops `shortcut-deployment-block` (critical) and `p14-data-fix-not-launched` (medium) are layered, not redundant.
- Memory `project_p8a_frame_level_auc_2026-04-29.md` (Slice 7) — frame-level AUC evidence reframes the shortcut narrative: the model is meaningfully less broken than the buggy contract scorer suggested. The shortcut is real (Axis 3 Δ ~0.5) but is not the sole proximate cause of the headline-metric failure.
- [`eval_production_crop_tightness_gap`](eval_production_crop_tightness_gap.md) (2026-04-29 audit) — eval substrate may amplify shortcut signal vs production because eval frames carry more background context around the face than production crops do. The 2-camera controlled test (2026-04-24) was production-side captures so the shortcut is real on production, but the eval-substrate amplification is a separately-open question that bears on how to read the close criterion of `shortcut-deployment-block`.
- [`sharpness_metric_bug`](sharpness_metric_bug.md) (2026-04-29 audit) — `sharpness_laplacian` is computed on the full image not the face crop, so any prior per-quartile FPR analysis indexed on it (notably the 2026-04-27 investigation's *"very sharp (>402): 45% FPR"* table) is partially confounded.
- [`eval_substrate_data_hygiene`](eval_substrate_data_hygiene.md) (2026-04-29 audit) — is_no_face slice is data degeneracy not model weakness; eval substrate contains 99×110-pixel source crops that pass face-area floor but no production deployment would score.

### Open loop: shortcut-block-criterion-may-be-scorer-artifact-bound
status: open
severity: medium
first_seen: 2026-04-29
last_verified: 2026-04-29
close_criterion: a written disposition is recorded — either (a) the `shortcut-deployment-block` close criterion's `lockbox_fake_recall ≥ 0.60` half is reaffirmed under the corrected contract scorer policy (`target_fake_recall_min=0.30`) and a downstream packet hits both halves at the same τ, OR (b) the criterion is explicitly revised to a frame-level-AUC-based version (e.g. `dor-real-webcam-false-flag-no-virtual-bg` mean prob ≤ 0.30 AND `visomaster_enhanced_macro_dev` frame-level AUC ≥ 0.85) with a documented rationale referencing memory `project_p8a_frame_level_auc_2026-04-29.md`. Surfaced as a sub-question to the parent `shortcut-deployment-block` (critical) loop, not a replacement for it.

This loop exists because Slice-7 frame-level AUC evidence (memory `project_p8a_frame_level_auc_2026-04-29.md`) reframes "low fake recall" from a model failure to (largely) a scorer artifact. The parent loop's close criterion was authored under the buggy contract policy; under the corrected policy P8A already hits viso 13.6% / deeplive 23.9% / teams_fake 52.6% which is consistent with the underlying frame-level AUCs. The question is whether the criterion's `lockbox_fake_recall ≥ 0.60` half was measuring the actual signal or the scorer artifact. **Do not change the criterion silently.** This sub-loop is the explicit surface for the disposition decision, expected to close in the same packet that closes its parent — but the disposition needs to be written down (in the thread, on a packet retro, or in a handoff) so a future agent does not re-derive the question from scratch.

**Note:** the paired `frame-level-vs-clip-level-scorer-mismatch` open loop is canonically defined in [`viso_bucket_gap`](viso_bucket_gap.md) (Slice 7). Cited here because the frame-level AUC reframing originates from this thread's evidence; the loop itself is owned upstream to avoid duplicate parser blocks.

### Open loop: shortcut-deployment-block
status: in-progress
severity: critical
first_seen: 2026-04-24
last_verified: 2026-04-29
close_criterion: a Packet-7+ checkpoint achieves `dor-real-webcam-false-flag-no-virtual-bg` mean prob_fake ≤ 0.30 (vs RLP6_04's 0.932 post-fix) AND `lockbox_fake_recall ≥ 0.60` at a τ that holds `teams_ood_real` FPR ≤ 5%, demonstrating the camera/ISP shortcut has been broken without sacrificing fake recall

The 2026-04-24 controlled 2-camera test (`docs/relaunch_handoffs/R13_RELAUNCH_PACKET7_CAMERA_SIGNATURE_HANDOFF_2026-04-24.md:5-19`) promoted the camera/ISP-signature shortcut from "lockbox curiosity" to a hard deployment blocker: same person, same lighting, swap the camera and the score flips 0.02 ↔ 0.94 on Dor (laptop vs webcam) and 0.01 ↔ 0.90 on Roee (Windows vs Mac). RLP6_04 with `value_composite=0.9006` is contract-legal under the corrected gate but unshippable: lockbox 90/90 is not threshold-reachable (memory `project_signature_shortcut_finding.md` — "at τ catching 90% fakes, real FPR is 63.6%"). The post-fix re-score (`analysis/rlp6_04_postfix_rescore_2026-04-24.summary.json`) ruled out preprocessing drift as a cause (anchor delta −0.008); the shortcut is structural representation. WS-P1 calibration probe (0.301 average gap closure across {5,10,15,25}% target FPRs) ruled out per-camera calibration alone as a cure. RLP7 (5 yamls, training-aug response) is the first structural attempt; the loop closes only when a downstream packet (RLP7+ or whatever supersedes it) hits the close criterion above. Cross-references: this loop interlocks with `fpr-minimization-no-budget-tau-collapse` ([`promotion_contract_evolution`](promotion_contract_evolution.md)) — the contract bug masks the deployment block in current scorecards; a contract fix without a representation fix would surface the same shortcut at the lockbox readout. The two must close together. Memory `project_shortcut_is_upstream.md` (Slice 5 follow-up) notes RLP7_08's evidence that the shortcut lives upstream of RLP6_04 (R12g + CLIP base), so light FT from any RLP6_04 step is structurally bounded; the close criterion may require unfreeze-CLIP / scratch / data-side intervention rather than further FT axes.

**Slice 5 verification (2026-04-29) — moves to `in-progress`.** P8A produced the first run that materially weakens the shortcut: anchor mean 0.932 → 0.744 (Δ=−0.188) on `dor-real-webcam-no-VBG`, with all three real-correct pools also moving closer to zero and Roee-mac dropping decisively (0.7515 → 0.3528, Δ=−0.399). At the lockbox layer, `lockbox_real_fpr` halved from 0.441% → 0.147% (per `PACKET_9_MID_FLIGHT_HANDOFF_2026-04-26.md:84-91`). **However, the close criterion is not met.** (1) The anchor mean target is ≤0.30; P8A is at 0.744. The shortcut is weakened but not broken — frac > 0.9 still 0.43 (13/30 anchor frames pin). (2) The `lockbox_fake_recall ≥ 0.60` half is more decisively missed: P8A regressed −13.6 pp aggregate fake recall on `teams_fake_all_dev`, with per-method losses of −26.6 pp (deeplive_enhanced), −33.3 pp (xiang_xiang flat), −21.6 pp (visomaster_enhanced_macro). 329 fake-side videos that RLP6_04 caught are missed by P8A; **zero compensating gains**. ~80% of misses sit below the τ-recoverable band — separability collapse, not threshold drift. The trade is structural. P8B refuted the "shortcut is in the weight chain" hypothesis (scratch is worse than RLP6_04). Combined: the shortcut is data-bound; capacity helps; scratch is wrong. Loop stays `critical` and moves to `in-progress` rather than `resolved` because the close criterion explicitly requires *both* halves (anchor weakening AND fake recall retention) — P8A delivers the first half while breaking the second, so the deployment block is **not** closed. Packet-9 is the soften-attempt (single-variable disentangle of magnitude / topology / data); P10 is the orthogonal anti-shortcut intervention class (symmetric router + GRL slate). The Phase C overnight slate's C.3 codec_hedge looks like the next live lever (best_anchor/composite +30% vs canonical), but the lockbox per-method recall has not been validated for it. The close criterion will require a packet whose pre-launch contract makes both halves first-class gates simultaneously.

**Slice 7 verification (2026-04-29) — stays `in-progress`; close criterion remains valid but its framing is reframed.** P13_FROM_SCRATCH Day-4 verdict γ (`docs/relaunch_handoffs/P13_DAY4_VERDICT_2026-04-29.md`): all 9 P13 candidates fail Axis 1 by ≥15pp; Axis 3 Δ exceeds the γ-trigger 0.30 for every candidate (best step18000 = 0.520, vs gate 0.15). The from-scratch + anti-shortcut interventions did NOT break the camera-signature shortcut, but did move it directionally (P13_step18000 Δ=0.520 vs P8A 0.659, ~14pp improvement). All three Plan-v4 levers (anchor-aware loss, pipeline-random aug, face-scale jitter) actually fired this time post-`c366026` wandb-flattening fix; the verdict reflects the recipe's true behavior. **The reframing**: frame-level AUC evidence (memory `project_p8a_frame_level_auc_2026-04-29.md`: viso 0.75 / deeplive 0.86 / teams_fake 0.91) shows the model is meaningfully less broken than the buggy contract scorer was reporting; the close criterion's "lockbox_fake_recall ≥ 0.60" half is more achievable than it looked under the buggy policy (P13_step18000 hits 88.5% lockbox fake recall at τ=0.5; P8A hits 65%). **But the close criterion still does not close** because (a) the anchor mean target ≤0.30 is missed everywhere (best Δ=0.52), (b) the lockbox fake recall half is met at τ=0.5 only by sacrificing modern_v2 FPR which explodes to 30.2% (worse than P8A's 3.6%), and (c) the close criterion requires both halves on the **same τ that holds `teams_ood_real` FPR ≤ 5%** — that joint constraint is not met by any candidate to date. Critical judgment: the close criterion stays the same; what moved is the path to it. The expected next lever is no longer "a P11/P12-style recipe-tuning packet" — it is the layered combination of **bucket-gap closure** (cheap, mostly-built — see [`viso_bucket_gap`](viso_bucket_gap.md)) + **architectural-class anti-shortcut** (P15 GRL drafted; future representation-loss work). See sub-loop `shortcut-block-criterion-may-be-scorer-artifact-bound` below for the open question on whether the criterion's lockbox-recall half should be revised.
